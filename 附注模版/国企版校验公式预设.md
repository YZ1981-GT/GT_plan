# 国企版 — 全量校验公式预设

> 本文档逐科目、逐表格列出每一条校验公式。
> 表格序号对应 `国企版科目对照模板.md` 中的表格编号。
> 核心原则：**本地规则引擎优先，LLM 兜底**
>
> 公式类型：
> - `余额` — 报表科目余额 vs 附注表格合计行（仅用于纯取合计行的简单表格）
> - `宽表` — 横向公式（期初±变动=期末），合计行同时隐含余额核对
> - `纵向` — 多段表纵向勾稽（原值-折旧-减值=账面价值）
> - `交叉` — 同科目多表之间金额核对
> - `跨科目` — 不同科目之间金额核对
> - `其中项` — 明细行之和 = 合计行，合计行同时隐含余额核对
> - `二级明细` — 报表二级子明细行 vs 附注明细表
> - `完整性` — 数据行非空完整性校验（如账面余额≠0时其他列不应为空）
> - `LLM审核` — 需调用LLM对文本内容进行合理性判断（规则引擎无法处理，LLM兜底）
>
> **互斥规则：** 标注 `[余额]` 的表格不含其中项加总或宽表横向公式等表格内计算。
> 含 `[其中项]` 或 `[宽表]` 的表格，余额核对从计算结果的合计行自然得出。
>
> **其中项校验通用规则：**
> 实际附注中的明细行名称可能与模板不完全一致（行数多少、名称措辞均可能不同），
> 因此其中项校验统一采用通用规则：**`sum(合计行以外的所有明细行) = 合计行`**，
> 按每个数值列独立校验。不硬编码具体子项名称，由引擎自动识别合计行与明细行。
> **若表格仅有一行数据（无明细行可加总），则自动跳过其中项校验。**
>
> **账龄衔接校验通用规则：**
> 适用于所有按账龄披露的表格（如应收账款按账龄表、预付款项按账龄表等）。
> 核心逻辑：本期某账龄段的金额不应超过上期前一个账龄段的金额（仅部分结转，不可能凭空增加）。
> 引擎需根据实际表格中识别到的账龄分段动态适配，不写死段数。
>
> **衔接比较使用的列：** 引擎需自动识别表格中的"账面余额"列（而非坏账准备或账面价值列）。
> 若表格同时有"期末账面余额"和"期初账面余额"两列（如应收账款①按账龄表），
> 则用"期末账面余额"列的各账龄段 vs "期初账面余额"列的前一账龄段进行衔接比较。
> 若表格仅有"期末余额"和"期初余额"各一列（如预付款项按账龄表），则直接使用该列。
>
> 常见示例：
>
> **3年段示例（1年以内/1至2年/2至3年/3年以上）：**
> - 期末"1至2年" ≤ 期初"1年以内"
> - 期末"2至3年" ≤ 期初"1至2年"
> - 期末"3年以上" ≤ 期初"2至3年" + 期初"3年以上"（兜底段）
>
> **5年段示例（1年以内/1至2年/2至3年/3至4年/4至5年/5年以上）：**
> - 期末"1至2年" ≤ 期初"1年以内"
> - 期末"2至3年" ≤ 期初"1至2年"
> - 期末"3至4年" ≤ 期初"2至3年"
> - 期末"4至5年" ≤ 期初"3至4年"
> - 期末"5年以上" ≤ 期初"4至5年" + 期初"5年以上"（兜底段）
>
> **通用规则：** 引擎自动识别表格中实际的账龄分段（可能是3段、5段或其他），
> 按顺序逐段衔接：中间各段 期末 ≤ 期初前一段；最后兜底段 期末 ≤ 期初同段 + 期初前一段。
>
> **账龄段内"其中"细分处理：**
> 某些账龄段下可能带有"其中"细分行（如"1年以内"下细分"0-3个月"、"3-6个月"、"6个月-1年"），
> 引擎需识别这种父子关系并校验：
> - "其中"子项之和 = 父项金额（如 0-3个月 + 3-6个月 + 6个月-1年 = 1年以内）
> - 合计行 = 各主段（父项）之和，不含"其中"子项（避免重复加总）
> - 细分粒度不固定，引擎需动态识别实际出现的子项数量
>
> 注意：此校验为合理性校验（≤），不是精确等式；若不满足，标记为异常提示而非硬性错误。
>
> **期末/期初合并表与拆分表的处理：**
> 某些表格（如②按坏账准备计提方法分类表）在实际文档中可能有两种呈现方式：
> - **大表模式：** 期末数和期初数并排在同一个表格中（如11列：类别+期末5列+期初5列）
> - **小表模式：** 期末数和期初数拆分为两个独立表格（各6列：类别+5列数据）
>
> 引擎需自动识别：若同一科目下出现两个结构相同、标题分别含"期末"和"期初"的表格，
> 视为小表模式，分别校验；若为一个包含期末+期初的宽表，视为大表模式，按列分组校验。
> 两种模式下校验逻辑相同，仅数据提取方式不同。
>
> **组合计提多子表处理：**
> 应收账款④组合计提和其他应收款⑤组合计提在实际文档中可能包含多个子表（而非单一表格）。
> 例如应收账款的组合计提可能拆分为"应收中央企业客户"按账龄、"应收海外企业客户"按账龄、
> "采用余额百分比或其他组合方法"等多个独立子表，每个子表各有自己的合计行。
> 引擎需自动识别同一"组合计提"标题下的所有子表，在做交叉校验时将所有子表的合计行求和，
> 在做其中项校验时对每个子表独立校验。
> **注意：** 应收票据④组合计提只有一个表（商业承兑/银行承兑在同一表内），不需要多子表处理。
>
> **行名匹配规则：**
> 引擎在匹配报表行名与附注表格行名时，应进行模糊匹配而非精确匹配。具体处理：
> - 忽略前缀：去除"其中："、"其中："（全角冒号）、"减："等前缀后再匹配
>   （如报表中"其中：原材料"应匹配附注中"原材料"）
> - 忽略空格：去除行名中的所有空格（全角/半角）后再匹配
>   （如"小  计"应匹配"小计"，"合  计"应匹配"合计"）
> - 全半角统一：将全角字符转为半角后再匹配（如全角括号"（）"与半角"()"视为相同）
> - 同义词适配：常见同义表达视为匹配（如"库存商品"与"产成品"、"库存商品（产成品）"）
> - 匹配顺序：先尝试精确匹配，再尝试去前缀匹配，最后尝试模糊匹配

---

## 一、资产负债表科目

### 1. 货币资金

**① 货币资金分类表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F1-1 | 余额 | `报表.货币资金期末 = ①货币资金分类表.合计行.期末余额` |
| F1-2 | 余额 | `报表.货币资金期初 = ①货币资金分类表.合计行.期初余额` |
| F1-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**② 受限制的货币资金明细表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F1-4 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F1-5 | 跨科目 | `②表.合计行.期末 = 报表.货币资金期末 - 补充资料③表."期末现金及现金等价物余额"` |
| F1-6 | 跨科目 | `②表.合计行.期初 = 报表.货币资金期初 - 补充资料③表."期初现金及现金等价物余额"` |

### 2. 交易性金融资产

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F2-1 | 余额 | `报表.交易性金融资产期末 = ①分类表.合计行.期末余额` |
| F2-2 | 余额 | `报表.交易性金融资产期初 = ①分类表.合计行.期初余额` |
| F2-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 3. 衍生金融资产

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F3-1 | 余额 | `报表.衍生金融资产期末 = ①明细表.合计行.期末余额` |
| F3-2 | 余额 | `报表.衍生金融资产期初 = ①明细表.合计行.期初余额` |
| F3-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 4. 应收票据

**① 应收票据分类表（银行承兑/商业承兑/合计）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F4-1 | 余额 | `报表.应收票据期末 = ①分类表.合计行.期末账面价值` |
| F4-2 | 余额 | `报表.应收票据期初 = ①分类表.合计行.期初账面价值` |
| F4-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F4-3a | 纵向 | `①分类表: 账面余额 - 坏账准备 = 账面价值`（期末/期初各独立校验） |
| F4-6 | 交叉 | `①分类表.坏账准备期末合计 = ⑤坏账准备变动表.期末余额合计` |
| F4-6a | 交叉 | `①分类表.坏账准备期初合计 = ⑤坏账准备变动表.期初余额合计` |
| F4-8 | 交叉 | `①分类表.账面余额期末合计 = ②按计提方法分类表.账面余额期末合计` |
| F4-8a | 交叉 | `①分类表.账面余额期初合计 = ②按计提方法分类表.账面余额期初合计`（仅当期初数存在时校验，否则跳过） |
| F4-9 | 交叉 | `①分类表.坏账准备期末合计 = ②按计提方法分类表.坏账准备期末合计` |
| F4-9a | 交叉 | `①分类表.坏账准备期初合计 = ②按计提方法分类表.坏账准备期初合计`（仅当期初数存在时校验，否则跳过） |
| F4-10 | 交叉 | `①分类表.账面价值期末合计 = ②按计提方法分类表.账面价值期末合计` |
| F4-10a | 交叉 | `①分类表.账面价值期初合计 = ②按计提方法分类表.账面价值期初合计`（仅当期初数存在时校验，否则跳过） |

**②③④ 按坏账准备计提方法分类（单项/组合）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F4-16 | 其中项 | `②按计提方法分类表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；大表模式下期末/期初列分别校验，小表模式下两个表各自校验） |
| F4-17 | 其中项 | `③单项计提表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F4-18 | 其中项 | `④组合计提表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F4-4 | 交叉 | `②按计提方法分类表.账面余额合计 = ③单项计提表.账面余额小计 + ④组合计提表.账面余额小计`（期末） |
| F4-5 | 交叉 | `②按计提方法分类表.坏账准备合计 = ③单项计提表.坏账准备小计 + ④组合计提表.坏账准备小计`（期末） |
| F4-4a | 交叉 | `②按计提方法分类表.账面余额合计 = ③单项计提表.账面余额小计 + ④组合计提表.账面余额小计`（期初；仅当期初数存在时校验，否则跳过） |
| F4-5a | 交叉 | `②按计提方法分类表.坏账准备合计 = ③单项计提表.坏账准备小计 + ④组合计提表.坏账准备小计`（期初；仅当期初数存在时校验，否则跳过） |
| F4-11 | 纵向 | `②按计提方法分类表: 账面余额 - 坏账准备 = 账面价值`（期末/期初各独立校验） |
| F4-12 | 纵向 | `②按计提方法分类表: 预期信用损失率(%) = 坏账准备金额 / 账面余额金额 × 100`（每行独立校验，期末/期初） |
| F4-25 | 纵向 | `②按计提方法分类表: 比例(%) = 该行账面余额金额 / 合计行账面余额金额 × 100`（每行独立校验，期末/期初各列独立；合计行按公式计算应为100） |
| F4-13 | 纵向 | `③单项计提表: 预期信用损失率(%) = 坏账准备 / 账面余额 × 100`（每行独立校验） |
| F4-14 | 完整性 | `③单项计提表（排除合计行）: 若账面余额 ≠ 0，则承兑人名称、坏账准备、预期信用损失率、计提理由列均不应为空` |
| F4-15 | LLM审核 | `③单项计提表（排除合计行，仅账面余额 ≠ 0 且对应列非空的行）: (1)"承兑人名称"列需调用LLM判断是否为公司全称、叫法是否恰当；(2)"计提理由"列需调用LLM判断表述是否合理` |

**⑤ 本期计提、收回或转回的坏账准备**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F4-7 | 宽表 | `⑤坏账准备变动表: 期初 + 计提 - 收回或转回 - 核销 + 其他变动 = 期末`（每行独立） |
| F4-19 | 交叉 | `⑤坏账准备变动表: 单项计提行 + 按组合计提行 = 合计行`（每个数值列独立校验） |
| F4-20 | 其中项 | `⑤坏账准备变动表: "其中："下方所有明细行之和 = 按组合计提行`（每个数值列独立校验） |
| F4-26 | 其中项 | `⑤收回或转回的坏账准备子表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F4-27 | 完整性 | `⑤收回或转回的坏账准备子表（排除合计行）: 若转回或收回金额 ≠ 0，则承兑人名称、转回或收回原因/方式列均不应为空` |
| F4-28 | LLM审核 | `⑤收回或转回的坏账准备子表（排除合计行，仅转回或收回金额 ≠ 0 且对应列非空的行）: (1)"承兑人名称"列需调用LLM判断是否恰当；(2)"转回或收回原因、方式"列需调用LLM判断表述是否合理` |

**⑥ 期末已质押的应收票据**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F4-21 | 其中项 | `⑥表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**⑦ 期末已背书或贴现且尚未到期的应收票据**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F4-22 | 其中项 | `⑦表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**⑧ 期末因出票人未履约而转为应收账款的票据**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F4-23 | 其中项 | `⑧表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**⑨ 本期实际核销的应收票据**
| F4-24 | 其中项 | `⑨表（含子表）: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F4-29 | 完整性 | `⑨核销子表（排除合计行）: 若核销金额 ≠ 0，则单位名称、应收票据性质、核销原因、履行的核销程序、是否因关联交易产生列均不应为空` |
| F4-30 | LLM审核 | `⑨核销子表（排除合计行，仅核销金额 ≠ 0 且对应列非空的行）: (1)"单位名称"列需调用LLM判断是否恰当；(2)"核销原因"列需调用LLM判断表述是否合理` |

### 5. 应收账款

**① 按账龄披露应收账款**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F5-1 | 余额 | `报表.应收账款期末 = ①按账龄表.合计行.(期末账面余额 - 期末坏账准备)`（①表无账面价值列，需计算得出） |
| F5-2 | 余额 | `报表.应收账款期初 = ①按账龄表.合计行.(期初账面余额 - 期初坏账准备)` |
| F5-24 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F5-25 | 账龄衔接 | `①按账龄表: 账龄衔接校验`（使用"账面余额"列，期末账面余额各段 vs 期初账面余额前一段，按通用规则；引擎自动适配3段或5段；若"1年以内"下有"其中"细分行，按通用规则校验子项之和=父项） |
| F5-8 | 交叉 | `①按账龄表.账面余额期末合计 = ②按计提方法分类表.账面余额期末合计`（若②为大表取期末列，若②拆为两个小表则取期末小表的合计） |
| F5-8a | 交叉 | `①按账龄表.账面余额期初合计 = ②按计提方法分类表.账面余额期初合计`（若②为大表取期初列，若②拆为两个小表则取期初小表的合计；仅当期初数存在时校验） |



**②③④ 按坏账准备计提方法分类（单项/组合）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F5-16 | 其中项 | `②按计提方法分类表: 按单项计提 + 按组合计提 = 合计行`（每个数值列独立校验；②表含"其中："子项时，子项之和应等于"按组合计提"行，合计行校验不含"其中"子项避免重复加总；大表模式下期末/期初列分别校验，小表模式下两个表各自校验） |
| F5-17 | 其中项 | `③单项计提表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F5-18 | 其中项 | `④组合计提: 每个组合子表各自校验 sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；④组合计提可能有多个子表，每个子表独立做其中项校验） |
| F5-26 | 账龄衔接 | `④组合计提中按账龄分类的子表: 账龄衔接校验`（使用"账面余额"列，按通用规则；引擎自动适配3段或5段；若"1年以内"下有"其中"细分行，按通用规则校验子项之和=父项；仅适用于行为账龄段的子表如"应收中央企业客户""应收海外企业客户"等，"采用余额百分比或其他组合方法"表行为组合名称不适用） |
| F5-27 | 纵向 | `④组合计提中按账龄分类的子表: 比例(%) = 该行账面余额金额 / 合计行账面余额金额 × 100`（每行独立校验，期末/期初各列独立；合计行按公式计算应为100） |
| F5-28 | 纵向 | `④组合计提中"采用余额百分比或其他组合方法"子表: 计提比例(%) = 坏账准备 / 账面余额 × 100`（每行独立校验，期末/期初各列独立；合计行跳过） |
| F5-3 | 交叉 | `②按计提方法分类表.账面余额合计 = ③单项计提表.账面余额小计 + sum(所有④组合子表的合计行.账面余额)`（期末；④组合计提可能有多个子表如"应收中央企业客户""应收海外企业客户""采用余额百分比或其他组合方法"等，需将所有子表的合计行求和） |
| F5-4 | 交叉 | `②按计提方法分类表.坏账准备合计 = ③单项计提表.坏账准备小计 + sum(所有④组合子表的合计行.坏账准备)`（期末；同上，多个组合子表求和） |
| F5-5 | 交叉 | `②按计提方法分类表.坏账准备期末合计 = ⑤坏账准备变动表.期末余额合计` |
| F5-5a | 交叉 | `②按计提方法分类表.坏账准备期初合计 = ⑤坏账准备变动表.期初余额合计` |
| F5-3a | 交叉 | `②按计提方法分类表.账面余额合计 = ③单项计提表.账面余额小计 + sum(所有④组合子表的合计行.账面余额)`（期初；仅当期初数存在时校验，否则跳过） |
| F5-4a | 交叉 | `②按计提方法分类表.坏账准备合计 = ③单项计提表.坏账准备小计 + sum(所有④组合子表的合计行.坏账准备)`（期初；仅当期初数存在时校验，否则跳过） |
| F5-11 | 纵向 | `②按计提方法分类表: 账面余额 - 坏账准备 = 账面价值`（期末/期初各独立校验） |
| F5-12 | 纵向 | `②按计提方法分类表: 预期信用损失率(%) = 坏账准备金额 / 账面余额金额 × 100`（每行独立校验，期末/期初） |
| F5-29 | 纵向 | `②按计提方法分类表: 比例(%) = 该行账面余额金额 / 合计行账面余额金额 × 100`（每行独立校验，期末/期初各列独立；合计行按公式计算应为100） |
| F5-13 | 纵向 | `③单项计提表: 预期信用损失率(%) = 坏账准备 / 账面余额 × 100`（每行独立校验） |
| F5-14 | 完整性 | `③单项计提表（排除合计行）: 若账面余额 ≠ 0，则债务人名称、坏账准备、预期信用损失率、计提理由列均不应为空` |
| F5-15 | LLM审核 | `③单项计提表（排除合计行，仅账面余额 ≠ 0 且对应列非空的行）: (1)"债务人名称"列需调用LLM判断是否为公司全称、叫法是否恰当；(2)"计提理由"列需调用LLM判断表述是否合理` |

**⑤ 本期计提、收回或转回的坏账准备**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F5-6 | 宽表 | `⑤坏账准备变动表: 期初 + 计提 - 收回或转回 - 转销或核销 = 期末`（每行独立 |
| F5-19 | 交叉 | `⑤坏账准备变动表: 单项计提行 + 按组合计提行 = 合计行`（每个数值列独立校验） |
| F5-20 | 其中项 | `⑤坏账准备变动表: "其中："下方所有明细行之和 = 按组合计提行`（每个数值列独立校验） |
| F5-7 | 跨科目 | `⑤坏账准备变动表.(计提 - 收回或转回) ≈ 信用减值损失附注."应收账款坏账损失"行` |
| F5-30 | 其中项 | `⑤收回或转回的坏账准备子表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F5-31 | 完整性 | `⑤收回或转回的坏账准备子表（排除合计行）: 若转回或收回金额 ≠ 0，则债务人名称、转回或收回原因/方式列均不应为空` |
| F5-32 | LLM审核 | `⑤收回或转回的坏账准备子表（排除合计行，仅转回或收回金额 ≠ 0 且对应列非空的行）: (1)"债务人名称"列需调用LLM判断是否为公司全称、叫法是否恰当；(2)"转回或收回原因、方式"列需调用LLM判断表述是否合理` |

**⑥ 本期实际核销的应收账款**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F5-21 | 其中项 | `⑥表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F5-33 | 完整性 | `⑥表（排除合计行）: 若核销金额 ≠ 0，则债务人名称、应收账款性质、核销原因、履行的核销程序、是否因关联交易产生列均不应为空` |
| F5-34 | LLM审核 | `⑥表（排除合计行，仅核销金额 ≠ 0 且对应列非空的行）: (1)"债务人名称"列需调用LLM判断是否为公司全称、叫法是否恰当；(2)"核销原因"列需调用LLM判断表述是否合理` |

**⑦ 按欠款方归集的期末余额前五名的应收账款**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F5-22 | 其中项 | `⑦表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F5-35 | 交叉 | `⑦前五名表.合计行.账面余额 ≤ ①按账龄表.合计行.期末账面余额`（前五名不超过全部合计） |
| F5-36 | 交叉 | `⑦前五名表.合计行.坏账准备 ≤ ①按账龄表.合计行.期末坏账准备`（前五名不超过全部合计） |
| F5-37 | 纵向 | `⑦前五名表: 占应收账款合计的比例(%) = 该行账面余额 / ①按账龄表.合计行.期末账面余额 × 100`（每行独立校验；合计行比例 = ⑦合计行账面余额 / ①合计行期末账面余额 × 100） |

**⑧ 由金融资产转移而终止确认的应收账款**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F5-23 | 其中项 | `⑧表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 6. 应收款项融资

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F6-1 | 余额 | `报表.应收款项融资期末 = ①明细表.合计行.期末余额` |
| F6-2 | 余额 | `报表.应收款项融资期初 = ①明细表.合计行.期初余额` |
| F6-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

> **应收款项融资附属子表预留校验：**
> 应收款项融资下可能出现与应收账款/应收票据相同结构的附属子表，引擎需动态识别并按对应规则校验：
>
> **应收账款类（若有数据）：**
> - 坏账准备变动表 → 参照 F5-6 宽表校验（期初+计提-收回或转回-转销或核销=期末）
> - 收回或转回的坏账准备子表 → 参照 F5-30~32（其中项+完整性+LLM审核）
> - 本期实际核销表 → 参照 F5-33~34（完整性+LLM审核）
>
> **应收票据类（若有数据）：**
> - 按坏账准备计提方法分类表 → 参照 F4-16~18（其中项）、F4-4/5/4a/5a（交叉）、F4-11~13（纵向）
> - 坏账准备变动表 → 参照 F4-7 宽表校验（期初+计提-收回或转回-核销+其他变动=期末）
> - 收回或转回子表 → 参照 F4-26~28（其中项+完整性+LLM审核）
> - 期末已质押表 → 参照 F4-21（其中项）
> - 期末已背书或贴现表 → 参照 F4-22（其中项）
> - 期末因出票人未履约而转为应收账款表 → 参照 F4-23（其中项）
> - 本期实际核销表 → 参照 F4-29~30（完整性+LLM审核）
>
> 以上子表不一定出现，引擎仅在识别到对应表格且有数据时才执行校验。

### 7. 预付款项

**① 预付款项按账龄列示**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F7-1 | 余额 | `报表.预付款项期末 = ①按账龄表.合计行.期末金额` |
| F7-2 | 余额 | `报表.预付款项期初 = ①按账龄表.合计行.期初金额` |
| F7-6 | 其中项 | `①按账龄表: sum(所有账龄段明细行) = 小计行`（期末/期初各独立校验） |
| F7-7 | 纵向 | `①按账龄表: 合计行 = 小计行 - 减值准备行`（期末/期初各独立校验） |
| F7-8 | 纵向 | `①按账龄表: 比例(%) = 该行金额 / 小计行金额 × 100`（每行独立校验，期末/期初各列独立；小计行按公式计算应为100；减值准备行和合计行不参与比例校验） |
| F7-5 | 账龄衔接 | `①按账龄表: 账龄衔接校验`（使用"金额"列，按通用规则逐段比较期末 vs 期初前一账龄段，合理性校验；引擎自动适配3段或5段） |

**② 账龄超过1年的大额预付款项**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F7-3 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（期末余额列独立校验） |
| F7-9 | 完整性 | `②表（排除合计行）: 若期末余额 ≠ 0，则债权单位、债务单位、账龄、未结算的原因列均不应为空` |
| F7-10 | LLM审核 | `②表（排除合计行，仅期末余额 ≠ 0 且对应列非空的行）: (1)"债权单位"列需调用LLM判断名称是否恰当；(2)"债务单位"列需调用LLM判断名称是否恰当；(3)"账龄"列需调用LLM判断表达是否规范一致（如"1-2年""2-3年"等格式）；(4)"未结算的原因"列需调用LLM判断表述是否合理` |
| F7-11 | 交叉 | `②表.合计行.期末余额 ≤ ①按账龄表中1年以上各段期末金额之和`（即①表"1至2年"+"2至3年"+"3年以上"期末金额之和；若①表为5段则为"1至2年"+"2至3年"+"3至4年"+"4至5年"+"5年以上"之和；引擎自动适配） |

**③ 按欠款方归集的期末余额前五名的预付款项**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F7-4 | 其中项 | `③表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F7-12 | 交叉 | `③前五名表.合计行.账面余额 ≤ ①按账龄表.小计行.期末金额`（前五名不超过全部小计） |
| F7-13 | 交叉 | `③前五名表.合计行.减值准备 ≤ ①按账龄表.减值准备行.期末金额`（前五名不超过全部减值准备） |
| F7-14 | 纵向 | `③前五名表: 占预付款项合计的比例(%) = 该行账面余额 / ①按账龄表.小计行.期末金额 × 100`（每行独立校验；合计行比例 = ③合计行账面余额 / ①小计行期末金额 × 100） |

### 7A. 应收资金集中管理款

**① 应收资金集中管理款明细表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F7A-1 | 余额 | `报表.应收资金集中管理款期末 = ①明细表.合计行.期末余额` |
| F7A-2 | 余额 | `报表.应收资金集中管理款期初 = ①明细表.合计行.期初余额` |
| F7A-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

> **应收资金集中管理款附属子表预留校验：**
> 应收资金集中管理款下【提示：坏账准备的计提，参考附注八、9、（3）进行披露】，
> 即坏账准备计提方法分类表结构与其他应收款③④⑤相同，引擎需动态识别并按对应规则校验：
>
> **坏账准备计提方法分类（若有数据）：**
> - 按计提方法分类表 → 参照 F8-15（其中项）、F8-10~12（纵向：账面价值/预期信用损失率/比例%）、F8-26（比例%）
> - 单项计提表 → 参照 F8-16（其中项）、F8-12~14（纵向/完整性/LLM审核）
> - 组合计提表 → 参照 F8-17（其中项）、F8-23~25（账龄衔接/比例%/计提比例%）
> - 坏账准备变动表 → 参照 F8-8 宽表校验（三阶段ECL表或简单变动表，按实际结构适配）
> - 收回或转回子表 → 参照 F8-27~29（其中项+完整性+LLM审核）
> - 本期实际核销表 → 参照 F8-30~31（完整性+LLM审核）
>
> 以上子表不一定出现，引擎仅在识别到对应表格且有数据时才执行校验。

### 8. 其他应收款

**汇总表（应收利息/应收股利/其他应收款项/合计）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-1 | 余额 | `报表.其他应收款期末 = 汇总表.合计行.期末余额`（合计 = 应收利息 + 应收股利 + 其他应收款项） |
| F8-2 | 余额 | `报表.其他应收款期初 = 汇总表.合计行.期初余额` |
| F8-48 | 其中项 | `汇总表: sum(合计行以外的所有明细行) = 合计行`（期末/期初各独立校验） |
| F8-3 | 二级明细 | `报表."其中：应收利息"行 = 汇总表."应收利息"行.期末`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F8-3a | 二级明细 | `报表."其中：应收利息"行(期初) = 汇总表."应收利息"行.期初`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F8-4 | 二级明细 | `报表."其中：应收股利"行 = 汇总表."应收股利"行.期末`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F8-4a | 二级明细 | `报表."其中：应收股利"行(期初) = 汇总表."应收股利"行.期初`（仅当报表中存在该二级明细行时校验，否则跳过） |

**应收利息**

**应收利息①分类表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-35 | 其中项 | `应收利息①分类表: sum(所有明细行) = 小计行`（期末/期初各独立校验） |
| F8-36 | 纵向 | `应收利息①分类表: 合计行 = 小计行 - 减值准备行`（期末/期初各独立校验） |
| F8-37 | 交叉 | `汇总表."应收利息"行 = 应收利息①分类表.合计行`（期末/期初各独立校验） |

**应收利息②重要逾期利息**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-38 | 其中项 | `应收利息②重要逾期利息表: sum(合计行以外的所有明细行) = 合计行`（期末余额列独立校验） |
| F8-39 | 完整性 | `应收利息②表（排除合计行）: 若期末余额 ≠ 0，则借款单位、逾期时间、逾期原因、是否发生减值及其判断依据列均不应为空` |
| F8-40 | LLM审核 | `应收利息②表（排除合计行，仅期末余额 ≠ 0 且对应列非空的行）: (1)"借款单位"列需调用LLM判断名称是否恰当；(2)"逾期原因"列需调用LLM判断表述是否合理；(3)"是否发生减值及其判断依据"列需调用LLM判断表述是否合理` |

**应收利息③坏账准备计提情况（三阶段ECL表）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-41 | 纵向 | `应收利息③坏账准备变动表（三阶段ECL表）: 期初余额 + 本期计提 - 本期转回 - 本期转销 - 本期核销 + 其他变动 = 期末余额`（每列独立校验：第一阶段/第二阶段/第三阶段/合计；阶段间转入转出在同一列内正负抵消） |
| F8-41a | 交叉 | `应收利息③坏账准备变动表: 每行的第一阶段 + 第二阶段 + 第三阶段 = 合计列`（每行独立校验） |
| F8-42 | 交叉 | `应收利息①分类表.减值准备行.期末 = 应收利息③坏账准备变动表.期末余额.合计列` |
| F8-42a | 交叉 | `应收利息①分类表.减值准备行.期初 = 应收利息③坏账准备变动表.期初余额.合计列` |

**应收股利**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-43 | 其中项 | `应收股利表: "账龄一年以内的应收股利" + "账龄一年以上的应收股利" = 小计行`（期末/期初余额列各独立校验；含"其中"子项时，子项之和应等于对应父项，合计不含"其中"子项避免重复加总） |
| F8-44 | 纵向 | `应收股利表: 合计行 = 小计行 - 减值准备行`（小计行即F8-43中"账龄一年以内"+"账龄一年以上"之和；期末/期初余额列各独立校验） |
| F8-45 | 完整性 | `应收股利表（排除小计/减值准备/合计行）: 若期末余额或期初余额 ≠ 0，则"未收回的原因"和"是否发生减值及其判断依据"列不应为空`（"账龄一年以内/一年以上"汇总行除外，仅校验其下"其中"明细行） |
| F8-46 | LLM审核 | `应收股利表（仅"其中"明细行，余额 ≠ 0 且对应列非空的行）: (1)"未收回的原因"列需调用LLM判断表述是否合理；(2)"是否发生减值及其判断依据"列需调用LLM判断表述是否合理` |
| F8-47 | 交叉 | `汇总表."应收股利"行 = 应收股利表.合计行`（期末/期初各独立校验） |

**其他应收款项**

**按账龄披露其他应收款项**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-49 | 其中项 | `按账龄披露其他应收款项表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F8-50 | 交叉 | `按账龄披露其他应收款项表.合计行.(期末账面余额 - 期末坏账准备) = 汇总表."其他应收款项"行.期末余额` |
| F8-50a | 交叉 | `按账龄披露其他应收款项表.合计行.(期初账面余额 - 期初坏账准备) = 汇总表."其他应收款项"行.期初余额` |
| F8-22 | 账龄衔接 | `按账龄披露其他应收款项表: 账龄衔接校验`（使用"账面余额"列，期末账面余额各段 vs 期初账面余额前一段，按通用规则；引擎自动适配3段或5段；若"1年以内"下有"其中"细分行，按通用规则校验子项之和=父项） |

**按坏账准备计提方法分类（单项/组合）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-15 | 其中项 | `按计提方法分类表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；大表模式下期末/期初列分别校验，小表模式下两个表各自校验） |
| F8-51 | 交叉 | `按账龄披露其他应收款项表.合计行.期末账面余额 = 按计提方法分类表.账面余额期末合计` |
| F8-51a | 交叉 | `按账龄披露其他应收款项表.合计行.期初账面余额 = 按计提方法分类表.账面余额期初合计`（仅当期初数存在时校验） |
| F8-52 | 交叉 | `按账龄披露其他应收款项表.合计行.期末坏账准备 = 按计提方法分类表.坏账准备期末合计` |
| F8-52a | 交叉 | `按账龄披露其他应收款项表.合计行.期初坏账准备 = 按计提方法分类表.坏账准备期初合计`（仅当期初数存在时校验） |
| F8-16 | 其中项 | `单项计提表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F8-17 | 其中项 | `组合计提: 每个组合子表各自校验 sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；组合计提可能有多个子表，每个子表独立做其中项校验） |
| F8-23 | 账龄衔接 | `组合计提中按账龄分类的子表: 账龄衔接校验`（使用"账面余额"列，按通用规则；引擎自动适配3段或5段；若"1年以内"下有"其中"细分行，按通用规则校验子项之和=父项；仅适用于行为账龄段的子表如"账龄组合"，"采用余额百分比法或其他组合方法"表行为组合名称不适用） |
| F8-24 | 纵向 | `组合计提中按账龄分类的子表: 比例(%) = 该行账面余额金额 / 合计行账面余额金额 × 100`（每行独立校验，期末/期初各独立校验；合计行按公式计算应为100） |
| F8-25 | 纵向 | `组合计提中"采用余额百分比法或其他组合方法"子表: 计提比例(%) = 坏账准备 / 账面余额 × 100`（每行独立校验，期末/期初各独立校验；合计行跳过） |
| F8-5 | 交叉 | `按计提方法分类表.账面余额合计 = 单项计提表.账面余额小计 + sum(所有组合子表的合计行.账面余额)`（期末；组合计提可能有多个子表如"账龄组合""采用余额百分比法或其他组合方法"等，需将所有子表的合计行求和） |
| F8-6 | 交叉 | `按计提方法分类表.坏账准备合计 = 单项计提表.坏账准备小计 + sum(所有组合子表的合计行.坏账准备)`（期末；同上，多个组合子表求和） |
| F8-7 | 交叉 | `按计提方法分类表.坏账准备期末合计 = 坏账准备计提情况表（三阶段ECL表）.期末余额.合计列` |
| F8-7a | 交叉 | `按计提方法分类表.坏账准备期初合计 = 坏账准备计提情况表（三阶段ECL表）.期初余额.合计列` |
| F8-5a | 交叉 | `按计提方法分类表.账面余额合计 = 单项计提表.账面余额小计 + sum(所有组合子表的合计行.账面余额)`（期初；仅当期初数存在时校验，否则跳过） |
| F8-6a | 交叉 | `按计提方法分类表.坏账准备合计 = 单项计提表.坏账准备小计 + sum(所有组合子表的合计行.坏账准备)`（期初；仅当期初数存在时校验，否则跳过） |
| F8-10 | 纵向 | `按计提方法分类表: 账面余额 - 坏账准备 = 账面价值`（期末/期初各独立校验） |
| F8-11 | 纵向 | `按计提方法分类表: 预期信用损失率(%) = 坏账准备金额 / 账面余额金额 × 100`（每行独立校验，期末/期初） |
| F8-26 | 纵向 | `按计提方法分类表: 比例(%) = 该行账面余额金额 / 合计行账面余额金额 × 100`（每行独立校验，期末/期初各独立校验；合计行按公式计算应为100） |
| F8-12 | 纵向 | `单项计提表: 预期信用损失率(%) = 坏账准备 / 账面余额 × 100`（每行独立校验） |
| F8-13 | 完整性 | `单项计提表（排除合计行）: 若账面余额 ≠ 0，则款项性质/债务人名称、坏账准备、预期信用损失率、计提理由列均不应为空` |
| F8-14 | LLM审核 | `单项计提表（排除合计行，仅账面余额 ≠ 0 且对应列非空的行）: (1)"款项性质/债务人名称"列需调用LLM判断是否为公司全称、叫法是否恰当；(2)"计提理由"列需调用LLM判断表述是否合理` |

**坏账准备计提情况（三阶段ECL表）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-8 | 纵向 | `坏账准备变动表（三阶段ECL表）: 期初余额 + 本期计提 - 本期转回 - 本期转销 - 本期核销 + 其他变动 = 期末余额`（每列独立校验：第一阶段/第二阶段/第三阶段/合计；阶段间转入转出在同一列内正负抵消） |
| F8-8a | 交叉 | `坏账准备变动表: 每行的第一阶段 + 第二阶段 + 第三阶段 = 合计列`（每行独立校验） |
| F8-18 | 交叉 | `坏账准备变动表: 单项计提行 + 按组合计提行 = 合计行`（每个数值列独立校验） |
| F8-19 | 其中项 | `坏账准备变动表: "其中："下方所有明细行之和 = 按组合计提行`（每个数值列独立校验） |
| F8-9 | 跨科目 | `坏账准备变动表.(计提 - 收回或转回) ≈ 信用减值损失附注."其他应收款坏账损失"行` |

**收回或转回的坏账准备**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-27 | 其中项 | `收回或转回的坏账准备子表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F8-28 | 完整性 | `收回或转回的坏账准备子表（排除合计行）: 若转回或收回金额 ≠ 0，则债务人名称、转回或收回原因/方式列均不应为空` |
| F8-29 | LLM审核 | `收回或转回的坏账准备子表（排除合计行，仅转回或收回金额 ≠ 0 且对应列非空的行）: (1)"债务人名称"列需调用LLM判断是否为公司全称、叫法是否恰当；(2)"转回或收回原因、方式"列需调用LLM判断表述是否合理` |

**本期实际核销的其他应收款项**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-20 | 其中项 | `核销表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F8-30 | 完整性 | `核销表（排除合计行）: 若核销金额 ≠ 0，则债务人名称、其他应收款项性质、核销原因、履行的核销程序、是否因关联交易产生列均不应为空` |
| F8-31 | LLM审核 | `核销表（排除合计行，仅核销金额 ≠ 0 且对应列非空的行）: (1)"债务人名称"列需调用LLM判断是否为公司全称、叫法是否恰当；(2)"核销原因"列需调用LLM判断表述是否合理` |

**按欠款方归集的期末余额前五名的其他应收款项**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F8-21 | 其中项 | `前五名表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F8-32 | 交叉 | `前五名表.合计行.账面余额 ≤ 按账龄披露其他应收款项表.合计行.期末账面余额`（前五名不超过全部合计） |
| F8-33 | 交叉 | `前五名表.合计行.坏账准备 ≤ 按账龄披露其他应收款项表.合计行.期末坏账准备`（前五名不超过全部合计） |
| F8-34 | 纵向 | `前五名表: 占其他应收款项合计的比例(%) = 该行账面余额 / 按账龄披露其他应收款项表.合计行.期末账面余额 × 100`（每行独立校验） |

> 涉及政府补助的应收款项为纯文字描述，不参与数值校验。

### 9. 存货

**① 存货分类表（原材料/在产品/库存商品/发出商品/合计）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F9-1 | 余额 | `报表.存货期末 = ①分类表.合计行.期末账面价值` |
| F9-2 | 余额 | `报表.存货期初 = ①分类表.合计行.期初账面价值` |
| F9-2a | 二级明细 | `报表."其中：原材料"行 = ①分类表."原材料"行.期末账面价值`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F9-2b | 二级明细 | `报表."其中：库存商品"行 = ①分类表."库存商品（产成品）"行.期末账面价值`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F9-2c | 二级明细 | `报表."其中：原材料"行.期初 = ①分类表."原材料"行.期初账面价值`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F9-2d | 二级明细 | `报表."其中：库存商品"行.期初 = ①分类表."库存商品（产成品）"行.期初账面价值`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F9-3 | 其中项 | `①表: sum(合计行以外的所有主项明细行) = 合计行`（每个数值列独立校验；含"其中"子项时，子项之和 ≤ 对应父项，合计行不含"其中"子项避免重复加总） |
| F9-3a | 纵向 | `①分类表: 账面余额 - 跌价准备 = 账面价值`（期末/期初各独立校验，每行独立） |
| F9-4 | 交叉 | `①分类表.跌价准备期末合计 = ②跌价准备变动表.期末余额合计` |
| F9-4a | 交叉 | `①分类表.跌价准备期初合计 = ②跌价准备变动表.期初余额合计` |

**② 存货跌价准备及合同履约成本减值准备**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F9-5 | 宽表 | `②跌价准备变动表: 期初 + 计提 + 其他增加 - 转回 - 转销 - 其他减少 = 期末`（每行独立） |
| F9-5a | 其中项 | `②跌价准备变动表: sum(合计行以外的所有主项明细行) = 合计行`（每个数值列独立校验；含"其中"子项时，子项之和 ≤ 对应父项，合计行不含"其中"子项避免重复加总） |
| F9-6 | 跨科目 | `②跌价准备变动表.(计提 - 转回或转销) ≈ 资产减值损失附注."存货跌价损失"行` |

**③ 确认为存货的数据资源**

> 此表格依据《企业数据资源相关会计处理暂行规定》披露，不一定出现。
> 引擎仅在识别到该表格且有数据时才执行以下校验。
> 表格结构为三段式（账面原值/跌价准备/账面价值），列为分类（外购/自行加工/其他方式/合计）。

| 编号 | 公式类型 | 校验公式 |
| F9-7 | 纵向 | `⑤数据资源存货表.账面原值段: 1.期初余额 + 2.本期增加金额 - 3.本期减少金额 = 4.期末余额`（每列独立校验：外购/自行加工/其他方式/合计） |
| F9-8 | 纵向 | 数据资源存货表.跌价准备段: 1.期初余额 + 2.本期增加金额 - 3.本期减少金额 = 4.期末余额（每列独立校验） |
| F9-9 | 纵向 | `⑤数据资源存货表: 期末账面价值 = 账面原值期末余额 - 跌价准备期末余额`（每列独立） |
| F9-9a | 纵向 | `⑤数据资源存货表: 期初账面价值 = 账面原值期初余额 - 跌价准备期初余额`（每列独立） |
| F9-10 | 其中项 | `⑤账面原值段: "本期增加金额"行的"其中"子项之和 ≤ "本期增加金额"行`（每列独立；子项为"其中："下方缩进行，部分列示） |
| F9-10a | 其中项 | `⑤账面原值段: "本期减少金额"行的"其中"子项之和 ≤ "本期减少金额"行`（每列独立；同上） |
| F9-10b | 其中项 | `⑤跌价准备段: "本期减少金额"行的"其中"子项之和 ≤ "本期减少金额"行`（每列独立；同上） |
| F9-11 | 交叉 | `⑤数据资源存货表: 每行的合计列 = sum(外购 + 自行加工 + 其他方式)`（每行独立校验） |
| F9-12 | 交叉 | `⑤数据资源存货表.账面原值段.期末余额.合计列 = ①分类表."数据资源"行.期末账面余额`（仅当①分类表中存在"数据资源"行时校验） |
| F9-12a | 交叉 | `⑤数据资源存货表.账面原值段.期初余额.合计列 = ①分类表."数据资源"行.期初账面余额`（同上） |
| F9-13 | 交叉 | `⑤数据资源存货表.跌价准备段.期末余额.合计列 = ①分类表."数据资源"行.期末跌价准备`（仅当①分类表中存在"数据资源"行时校验） |
| F9-13a | 交叉 | `⑤数据资源存货表.跌价准备段.期初余额.合计列 = ①分类表."数据资源"行.期初跌价准备`（同上） |

### 10. 合同资产

**① 合同资产情况表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F10-1 | 余额 | `报表.合同资产期末 = ①合同资产情况表.合计行.期末账面价值` |
| F10-2 | 余额 | `报表.合同资产期初 = ①合同资产情况表.合计行.期初账面价值` |
| F10-4 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F10-5 | 纵向 | `①表: 账面余额 - 减值准备 = 账面价值`（期末/期初各独立校验） |

**② 合同资产减值准备**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F10-6 | 宽表 | `②减值准备变动表: 期初 + 计提 - 转回 - 转销/核销 ± 其他变动 = 期末`（每行独立） |
| F10-7 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F10-3 | 交叉 | `①情况表.减值准备期末合计 = ②减值准备变动表.期末余额合计` |
| F10-3a | 交叉 | `①情况表.减值准备期初合计 = ②减值准备变动表.期初余额合计` |

> ③ 本期合同资产账面价值的重大变动为纯文字描述，不参与数值校验。

### 11. 持有待售资产

**① 持有待售资产情况表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F11-1 | 余额 | `报表.持有待售资产期末 = ①情况表.合计行.期末账面价值` |
| F11-1a | 余额 | `报表.持有待售资产期初 = ①情况表.合计行.期初账面价值` |
| F11-3 | 其中项 | `①情况表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；含"其中"子项时，子项之和 ≤ 对应父项） |
| F11-4 | 纵向 | `①情况表: 账面余额 - 减值准备 = 账面价值`（期末/期初各独立校验） |

**② 持有待售资产减值准备**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F11-2 | 宽表 | `②减值准备变动表: 期初 + 本期增加 - 本期转回 - 本期出售 = 期末`（每行独立） |
| F11-5 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；含"其中"子项时，子项之和 ≤ 对应父项） |
| F11-6 | 交叉 | `①情况表.减值准备期末合计 = ②减值准备变动表.期末合计` |
| F11-6a | 交叉 | `①情况表.减值准备期初合计 = ②减值准备变动表.期初合计` |

**③ 持有待售非流动资产明细**

> 此表为补充披露（期末账面价值/公允价值/预计处置费用/时间安排），不参与数值校验。
> 【提示：期末账面价值 ≤ 期末公允价值 - 预计处置费用】，引擎可做合理性校验。

### 12. 一年内到期的非流动资产

> 模板中一年内到期的非流动资产明细表包含：一年内到期的长期应收款/一年内到期的债权投资/
> 一年内到期的其他债权投资/其他/合计。
> 各明细行应与对应科目附注中的"减：一年内到期"行交叉核对（若对应科目附注中存在该行）。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F12-1 | 余额 | `报表.一年内到期的非流动资产期末 = ①明细表.合计行.期末余额` |
| F12-1a | 余额 | `报表.一年内到期的非流动资产期初 = ①明细表.合计行.期初余额` |
| F12-2 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F12-3 | 跨科目 | `①表."一年内到期的长期应收款"行.期末 = 长期应收款①按性质表."减：1年内到期的长期应收款"行.期末账面价值`（仅当①表有该行且长期应收款①表有对应减项行时校验，否则跳过） |
| F12-3a | 跨科目 | `①表."一年内到期的长期应收款"行.期初 = 长期应收款①按性质表."减：1年内到期的长期应收款"行.期初账面价值`（同上） |
| F12-4 | 跨科目 | `①表."一年内到期的债权投资"行.期末 = 债权投资附注中"一年内到期"相关金额`（仅当①表有该行且债权投资附注中有对应数据时校验，否则跳过；债权投资①情况表本身无减项行，引擎需在附注中动态查找是否有单独的一年内到期披露） |
| F12-4a | 跨科目 | `①表."一年内到期的债权投资"行.期初 = 债权投资附注中"一年内到期"相关金额(期初)`（同上） |
| F12-5 | 跨科目 | `①表."一年内到期的其他债权投资"行.期末 = 其他债权投资附注中"一年内到期"相关金额`（仅当①表有该行且其他债权投资附注中有对应数据时校验，否则跳过） |
| F12-5a | 跨科目 | `①表."一年内到期的其他债权投资"行.期初 = 其他债权投资附注中"一年内到期"相关金额(期初)`（同上） |

### 13. 其他流动资产

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F13-1 | 余额 | `报表.其他流动资产期末 = ①明细表.合计行.期末余额` |
| F13-1a | 余额 | `报表.其他流动资产期初 = ①明细表.合计行.期初余额` |
| F13-2 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 14. 债权投资

**① 债权投资情况表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F14-1 | 余额 | `报表.债权投资期末 = ①债权投资情况表.合计行.期末账面价值` |
| F14-2 | 余额 | `报表.债权投资期初 = ①债权投资情况表.合计行.期初账面价值` |
| F14-5 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F14-6 | 纵向 | `①表: 账面余额 - 减值准备 = 账面价值`（期末/期初各独立校验） |

**② 期末重要的债权投资**

> 模板列结构：债权项目 / 面值 / 票面利率% / 实际利率(%) / 到期日

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F14-7 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（面值列独立校验） |
| F14-7a | 完整性 | `②表（排除合计行）: 若面值 ≠ 0，则债权项目、票面利率、实际利率、到期日列均不应为空` |
| F14-7b | LLM审核 | `②表（排除合计行，仅面值 ≠ 0 且到期日非空的行）: "到期日"列需调用LLM判断是否为合理的日期格式（如"2025年12月31日""2025-12-31""2025/12/31"等常见格式均可）` |

**③ 减值准备计提情况**

> 模板中按三阶段分别披露（第一/第二/第三阶段各一个表），以及一个三阶段ECL变动表。
> 各阶段表结构相同：按单项计提坏账准备 / 其中：明细 / 按组合计提坏账准备 / 其中：明细 / 合计。
> 列结构：账面余额 / 预期信用损失率(%) / 减值准备 / 账面价值 / 理由。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F14-8 | 其中项 | `③各阶段减值准备表: 按单项计提 + 按组合计提 = 合计行`（每个阶段表各自校验，每个数值列独立；"其中："子项之和 = 对应父项"按单项计提"或"按组合计提"行，合计行不含"其中"子项避免重复加总） |
| F14-9 | 纵向 | `③各阶段减值准备表: 账面余额 - 减值准备 = 账面价值`（每个阶段表各自校验，每行独立） |
| F14-10 | 纵向 | `③各阶段减值准备表: 预期信用损失率(%) = 减值准备 / 账面余额 × 100`（每行独立校验） |
| F14-10a | 完整性 | `③各阶段减值准备表（"其中"明细行）: 若账面余额 ≠ 0，则减值准备、预期信用损失率、理由列均不应为空` |
| F14-10b | LLM审核 | `③各阶段减值准备表（"其中"明细行，仅账面余额 ≠ 0 且理由非空的行）: "理由"列需调用LLM判断表述是否合理` |
| F14-10c | 交叉 | `③第一阶段表.减值准备合计 = ③三阶段ECL变动表.期末余额.第一阶段列`（仅当两表均存在时校验） |
| F14-10d | 交叉 | `③第二阶段表.减值准备合计 = ③三阶段ECL变动表.期末余额.第二阶段列` |
| F14-10e | 交叉 | `③第三阶段表.减值准备合计 = ③三阶段ECL变动表.期末余额.第三阶段列` |
| F14-10f | 交叉 | `③三个阶段表.减值准备合计之和 = ③三阶段ECL变动表.期末余额.合计列` |
| F14-4 | 纵向 | `③三阶段ECL变动表: 期初余额 + 本期计提 - 本期转回 - 本期转销 - 本期核销 + 其他变动 = 期末余额`（每列独立校验：第一阶段/第二阶段/第三阶段/合计；阶段间转入转出在同一列内正负抵消） |
| F14-4a | 交叉 | `③三阶段ECL变动表: 每行的第一阶段 + 第二阶段 + 第三阶段 = 合计列`（每行独立校验） |
| F14-3 | 交叉 | `①情况表.减值准备期末合计 = ③三阶段ECL变动表.期末余额.合计列` |
| F14-3a | 交叉 | `①情况表.减值准备期初合计 = ③三阶段ECL变动表.期初余额.合计列` |

### 15. 其他债权投资

**① 其他债权投资情况表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F15-1 | 余额 | `报表.其他债权投资期末 = ①情况表.合计行.期末余额` |
| F15-2 | 余额 | `报表.其他债权投资期初 = ①情况表.合计行.期初余额` |
| F15-5 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**② 期末重要的其他债权投资**

> 模板列结构：其他债权投资项目 / 面值 / 摊余成本 / 公允价值 / 累计计入其他综合收益的公允价值变动金额 / 已计提减值准备金额

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F15-6 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（面值/摊余成本/公允价值等数值列独立校验） |
| F15-6a | 完整性 | `②表（排除合计行）: 若面值 ≠ 0，则其他债权投资项目、摊余成本、公允价值列均不应为空` |

**③ 减值准备计提情况**

> 模板注明"参照债权投资③减值准备计提情况进行披露"，即三阶段ECL结构。
> 各阶段表结构同债权投资③：按单项计提 / 其中：明细 / 按组合计提 / 其中：明细 / 合计。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F15-7b | 其中项 | `③各阶段减值准备表: 按单项计提 + 按组合计提 = 合计行`（每个阶段表各自校验，每个数值列独立；"其中："子项之和 = 对应父项，合计行不含"其中"子项避免重复加总） |
| F15-7c | 纵向 | `③各阶段减值准备表: 账面余额 - 减值准备 = 账面价值`（每个阶段表各自校验，每行独立） |
| F15-7d | 纵向 | `③各阶段减值准备表: 预期信用损失率(%) = 减值准备 / 账面余额 × 100`（每行独立校验） |
| F15-7e | 完整性 | `③各阶段减值准备表（"其中"明细行）: 若账面余额 ≠ 0，则减值准备、预期信用损失率、理由列均不应为空` |
| F15-7f | LLM审核 | `③各阶段减值准备表（"其中"明细行，仅账面余额 ≠ 0 且理由非空的行）: "理由"列需调用LLM判断表述是否合理` |
| F15-7g | 交叉 | `③第一阶段表.减值准备合计 = ③三阶段ECL变动表.期末余额.第一阶段列`（仅当两表均存在时校验） |
| F15-7h | 交叉 | `③第二阶段表.减值准备合计 = ③三阶段ECL变动表.期末余额.第二阶段列` |
| F15-7i | 交叉 | `③第三阶段表.减值准备合计 = ③三阶段ECL变动表.期末余额.第三阶段列` |
| F15-7j | 交叉 | `③三个阶段表.减值准备合计之和 = ③三阶段ECL变动表.期末余额.合计列` |
| F15-7 | 纵向 | `③三阶段ECL变动表: 期初余额 + 本期计提 - 本期转回 - 本期转销 - 本期核销 + 其他变动 = 期末余额`（每列独立校验：第一阶段/第二阶段/第三阶段/合计） |
| F15-7a | 交叉 | `③三阶段ECL变动表: 每行的第一阶段 + 第二阶段 + 第三阶段 = 合计列`（每行独立校验） |
| F15-3 | 交叉 | `①情况表.减值准备期末合计 = ③三阶段ECL变动表.期末余额.合计列`（仅当①表有减值准备列时校验） |
| F15-3a | 交叉 | `①情况表.减值准备期初合计 = ③三阶段ECL变动表.期初余额.合计列` |

### 16. 长期应收款

**① 长期应收款按性质披露**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F16-1 | 余额 | `报表.长期应收款期末 = ①按性质披露表.合计行.期末账面价值` |
| F16-2 | 余额 | `报表.长期应收款期初 = ①按性质披露表.合计行.期初账面价值` |
| F16-5 | 其中项 | `①表: sum(各主项明细行) = 小计行`（每个数值列独立校验；含"其中"子项时，子项之和 ≤ 对应父项如"融资租赁款"，合计行不含"其中"子项避免重复加总） |
| F16-5a | 纵向 | `①表: 合计行 = 小计行 - "减：1年内到期的长期应收款"行`（每个数值列独立校验；仅当表中有该减项行时校验） |
| F16-6 | 纵向 | `①表: 账面余额 - 坏账准备 = 账面价值`（期末/期初各独立校验） |
| F16-3 | 交叉 | `①按性质表.坏账准备期末合计 = ④坏账准备变动表.期末余额合计`（此处取小计行的坏账准备，即含1年内到期部分的总坏账准备） |
| F16-3a | 交叉 | `①按性质表.坏账准备期初合计 = ④坏账准备变动表.期初余额合计` |

**② 终止确认的长期应收款**

> 模板列结构：项目 / 转移方式 / 终止确认金额 / 与终止确认相关的利得或损失。
> 不一定出现，引擎仅在识别到该表格且有数据时才执行校验。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F16-7 | 其中项 | `②终止确认表: sum(合计行以外的所有明细行) = 合计行`（终止确认金额列独立校验） |

**③ 转移长期应收款且继续涉入形成的资产、负债**

> 模板列结构：项目 / 期末数。含资产小计/负债小计。
> 不一定出现，引擎仅在识别到该表格且有数据时才执行校验。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F16-8 | 其中项 | `③继续涉入表: 资产部分各明细行之和 = 资产小计行；负债部分各明细行之和 = 负债小计行`（期末数列独立校验） |

**④ 坏账准备计提情况**

> 模板注明"采用简化方法参考应收账款，采用三阶段模型参考其他应收款"，引擎按实际表格结构适配。
> 两种方法的完整校验规则通过索引引用，不重复展开。
>
> **简化方法（参照应收账款⑤，若有数据）：**
> - 坏账准备变动表 → 参照 F5-6 宽表校验（期初+计提-收回或转回-转销或核销=期末）
> - 变动表其中项 → 参照 F5-19（单项计提+按组合计提=合计行）、F5-20（"其中"子项之和=按组合计提行）
> - 收回或转回子表 → 参照 F5-30~32（其中项+完整性+LLM审核）
> - 核销子表 → 参照 F5-33~34（完整性+LLM审核）
> - 跨科目 → 参照 F5-7（计提-收回或转回 ≈ 信用减值损失."长期应收款坏账损失"行）
>
> **三阶段模型（参照其他应收款坏账准备，若有数据）：**
> - 三阶段ECL变动表 → 参照 F8-8 宽表校验（每列独立：第一阶段/第二阶段/第三阶段/合计）
> - 横向交叉 → 参照 F8-8a（每行三阶段之和=合计列）
> - 变动表其中项 → 参照 F8-18（单项计提+按组合计提=合计行）、F8-19（"其中"子项之和=按组合计提行）
> - 跨科目 → 参照 F8-9（计提-收回或转回 ≈ 信用减值损失."长期应收款坏账损失"行）
>
> 引擎根据实际表格列数自动判断：4列（第一阶段/第二阶段/第三阶段/合计）为三阶段模型，否则为简化方法。

### 17. 长期股权投资

**① 长期股权投资分类表（合营/联营）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F17-1 | 余额 | `报表.长期股权投资期末 = ①分类表.合计行.期末余额` |
| F17-2 | 余额 | `报表.长期股权投资期初 = ①分类表.合计行.期初余额` |
| F17-8 | 宽表 | `①分类表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F17-8a | 纵向 | `①分类表: 合计行 = 小计行 - "减：长期股权投资减值准备"行`（每个数值列独立校验） |
| F17-9 | 其中项 | `①分类表: sum(小计行以外的所有明细行) = 小计行`（每个数值列独立校验；不含"减：减值准备"行） |
| F17-3 | 交叉 | `①分类表.期末余额合计 = ②明细表.期末账面价值合计` |
| F17-4 | 交叉 | `①分类表.期初余额合计 = ②明细表.期初账面价值合计` |

**② 长期股权投资明细表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F17-5 | 宽表 | `②明细表: 期初账面价值 + 追加投资 - 减少投资 + 投资损益 + 其他综合收益 + 其他权益变动 - 现金股利 - 计提减值 ± 其他 = 期末账面价值`（每行独立） |
| F17-10 | 其中项 | `②明细表: "一、合营企业"分组合计 + "二、联营企业"分组合计 = 合计行`（每个数值列独立校验；每个分组内：若分组标题行下有明细行和独立小计行，则明细行之和 = 小计行，分组合计取小计行；若分组标题行下无独立小计行，则分组标题行本身即为小计行，明细行之和 = 该标题行；若某分组无数据则跳过） |
| F17-6 | 跨科目 | `②明细表."权益法下确认的投资损益"列合计 = 投资收益附注."权益法核算的长期股权投资收益"行` |
| F17-7 | 跨科目 | `②明细表."权益法下确认的投资损益"列合计 = 利润表."投资收益"中权益法部分` |

> ③④ 重要合营/联营企业的主要财务信息为被投资单位的财务数据，不参与本公司报表数值校验。
> 但引擎可做以下合理性校验（仅当表格存在时）：
> - `净资产 = 资产合计 - 负债合计`（每家公司独立校验）
> - `对权益投资的账面价值 = 按持股比例计算的净资产份额 + 调整事项`（每家公司独立校验）
> ⑤ 不重要合营/联营企业汇总信息为补充披露，不参与数值校验。

**⑥ 超额亏损分担表**

> 不一定出现，引擎仅在识别到该表格且有数据时才执行校验。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F17-11 | 其中项 | `⑥超额亏损分担表: "合营企业"分组各明细行之和 = "合营企业"小计行；"联营企业"分组各明细行之和 = "联营企业"小计行；合营企业小计 + 联营企业小计 = 合计行`（每个数值列独立校验；若某分组无数据则跳过） |

### 18. 其他权益工具投资

**① 其他权益工具投资情况表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F18-1 | 余额 | `报表.其他权益工具投资期末 = ①情况表.合计行.期末余额` |
| F18-2 | 余额 | `报表.其他权益工具投资期初 = ①情况表.合计行.期初余额` |
| F18-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**② 期末其他权益工具投资情况**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F18-4 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 19. 其他非流动金融资产

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F19-1 | 余额 | `报表.其他非流动金融资产期末 = ①明细表.合计行.期末公允价值` |
| F19-2 | 余额 | `报表.其他非流动金融资产期初 = ①明细表.合计行.期初公允价值` |
| F19-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |


### 20. 投资性房地产

**① 以成本计量（原价/累计折旧摊销/净值/减值准备/账面价值）**

> 模板列结构：期初余额 / 购置或计提 / 自用房地产或存货转入 / 处置 / 转为自用房地产 / 期末余额。
> 宽表公式中"本期增加"= 购置或计提 + 自用转入，"本期减少"= 处置 + 转为自用。
> 净值段和账面价值段仅有期初/期末值，中间列为空，不做宽表校验。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F20-1 | 余额 | `报表.投资性房地产期末 = ①成本计量表."五、账面价值合计"行.期末余额`（或②公允价值计量表."三、账面价值合计"行.期末公允价值，二选一取实际采用模式） |
| F20-2 | 余额 | `报表.投资性房地产期初 = ①成本计量表."五、账面价值合计"行.期初余额` |
| F20-3 | 宽表 | `①原价段: 期初 + 购置或计提 + 自用转入 - 处置 - 转为自用 = 期末`（每行独立） |
| F20-4 | 宽表 | `①累计折旧和累计摊销段: 期初 + 购置或计提 + 自用转入 - 处置 - 转为自用 = 期末`（每行独立） |
| F20-4a | 宽表 | `①减值准备段: 期初 + 购置或计提 + 自用转入 - 处置 - 转为自用 = 期末`（每行独立） |
| F20-5 | 纵向 | `①: 账面净值 = 原价 - 累计折旧和累计摊销`（仅期初/期末列校验；"三、净值合计" = "一、原价合计" - "二、折旧摊销合计"） |
| F20-5a | 纵向 | `①: 账面价值 = 账面净值 - 减值准备`（仅期初/期末列校验；"五、账面价值合计" = "三、净值合计" - "四、减值准备合计"） |
| F20-8 | 其中项 | `①各段: sum(合计行以外的所有明细行) = 合计行`（每段每列独立校验） |
| F20-8a | LLM审核 | `①附注文字"本期折旧和摊销额XX元": 需调用LLM提取文字中的金额，校验该金额 ≤ "二、累计折旧和累计摊销合计"行."购置或计提"列`（仅当附注中存在该文字说明时校验；若"购置或计提"列仅含计提无购置，则应相等） |
| F20-8b | LLM审核 | `①附注文字"本期减值准备计提额XX元": 需调用LLM提取文字中的金额，校验该金额 = "四、减值准备累计金额合计"行."购置或计提"列`（仅当附注中存在该文字说明时校验） |

**② 以公允价值计量（二选一）**

> 模板列结构：期初公允价值 / 购置 / 自用转入 / 公允价值变动损益 / 处置 / 转为自用 / 期末公允价值。
> 三段（成本/公允价值变动/账面价值）均有中间列数据。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F20-9 | 宽表 | `②成本段: 期初 + 购置 + 自用转入 + 公允价值变动损益 - 处置 - 转为自用 = 期末`（每行独立） |
| F20-10 | 宽表 | `②公允价值变动段: 期初 + 购置 + 自用转入 + 公允价值变动损益 - 处置 - 转为自用 = 期末`（每行独立） |
| F20-11 | 纵向 | `②: 账面价值 = 成本 + 公允价值变动`（每列独立；"三、账面价值合计" = "一、成本合计" + "二、公允价值变动合计"） |
| F20-11a | 余额 | `报表.投资性房地产期末 = ②公允价值计量表."三、账面价值合计"行.期末公允价值`（仅当采用公允价值模式时校验，与F20-1互斥） |
| F20-11b | 余额 | `报表.投资性房地产期初 = ②公允价值计量表."三、账面价值合计"行.期初公允价值`（同上） |
| F20-12 | 其中项 | `②各段: sum(合计行以外的所有明细行) = 合计行`（每段每列独立校验；含"其中"子项时，子项之和 ≤ 对应父项） |

**③ 未办妥产权证书的投资性房地产**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F20-7 | 其中项 | `③表: sum(合计行以外的所有明细行) = 合计行`（账面价值列独立校验） |
| F20-7a | 完整性 | `③表（排除合计行）: 若账面价值 ≠ 0，则原因列不应为空` |
| F20-7b | LLM审核 | `③表（排除合计行，仅账面价值 ≠ 0 且原因非空的行）: "原因"列需调用LLM判断未办妥产权证书的原因表述是否合理` |

### 21. 固定资产

> 模板中固定资产科目有一个汇总表（固定资产/固定资产清理/合计），引擎需先校验汇总表。

**固定资产汇总表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F21-0 | 余额 | `报表.固定资产期末 = 固定资产汇总表.合计行.期末账面价值`（仅当汇总表存在时校验；若无汇总表则用①情况表."五、固定资产账面价值合计"行） |
| F21-0b | 余额 | `报表.固定资产期初 = 固定资产汇总表.合计行.期初账面价值`（同上） |
| F21-0a | 其中项 | `固定资产汇总表: "固定资产"行 + "固定资产清理"行 = 合计行`（期末/期初各独立校验；仅当汇总表存在时校验） |

**① 固定资产情况表（原价/累计折旧/净值/减值准备/账面价值）**

> 模板列结构：期初余额 / 本期增加 / 本期减少 / 期末余额。
> 净值段和账面价值段的本期增加/本期减少列为空（`--`），不做宽表校验。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F21-1 | 交叉 | `①固定资产情况表."五、固定资产账面价值合计"行.期末余额 = 固定资产汇总表."固定资产"行.期末账面价值`（仅当汇总表存在时校验；若无汇总表则 F21-0 直接用①情况表校验报表余额） |
| F21-2 | 交叉 | `①固定资产情况表."五、固定资产账面价值合计"行.期初余额 = 固定资产汇总表."固定资产"行.期初账面价值`（同上） |
| F21-3 | 宽表 | `①原价段: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F21-4 | 宽表 | `①累计折旧段: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F21-5 | 宽表 | `①减值准备段: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F21-6 | 纵向 | `①账面净值 = 原价 - 累计折旧`（仅期初/期末列校验；"三、固定资产账面净值合计" = "一、账面原值合计" - "二、累计折旧合计"） |
| F21-6a | 纵向 | `①账面价值 = 账面净值 - 减值准备`（仅期初/期末列校验；"五、固定资产账面价值合计" = "三、账面净值合计" - "四、减值准备合计"） |
| F21-11 | 其中项 | `①各段: sum(合计行以外的所有明细行) = 合计行`（每段每列独立校验；含"其中"子项时，子项之和 ≤ 对应父项） |
| F21-13 | 二级明细 | `报表."其中：固定资产原价"行.期末 = ①情况表."一、账面原值合计"行.期末余额`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F21-13a | 二级明细 | `报表."其中：固定资产原价"行.期初 = ①情况表."一、账面原值合计"行.期初余额`（同上） |
| F21-14 | 二级明细 | `报表."其中：累计折旧"行.期末 = ①情况表."二、累计折旧合计"行.期末余额`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F21-14a | 二级明细 | `报表."其中：累计折旧"行.期初 = ①情况表."二、累计折旧合计"行.期初余额`（同上） |
| F21-15 | 二级明细 | `报表."其中：固定资产减值准备"行.期末 = ①情况表."四、减值准备合计"行.期末余额`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F21-15a | 二级明细 | `报表."其中：固定资产减值准备"行.期初 = ①情况表."四、减值准备合计"行.期初余额`（同上） |

**⑥ 固定资产清理**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F21-7 | 交叉 | `⑥固定资产清理表.合计行.期末 = 固定资产汇总表."固定资产清理"行.期末`（仅当汇总表存在时校验） |
| F21-7a | 其中项 | `⑥固定资产清理表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F21-7b | 完整性 | `⑥固定资产清理表（排除合计行）: 若期末账面价值 ≠ 0，则转入清理的原因列不应为空` |
| F21-7c | LLM审核 | `⑥固定资产清理表（排除合计行，仅期末账面价值 ≠ 0 且原因非空的行）: "转入清理的原因"列需调用LLM判断表述是否合理` |
| F21-8 | 跨科目 | `①原价段."在建工程转入"行.期末列 = 在建工程②项目变动表."转入固定资产"合计行`（仅当①表有"在建工程转入"明细行时校验） |
| F21-8a | LLM审核 | `若①情况表中无"在建工程转入"明细行，但表格下方文字说明中提及在建工程转入金额，则调用LLM提取该金额并与在建工程②项目变动表."转入固定资产"合计行核对`（F21-8不适用时的兜底校验） |

**② 暂时闲置的固定资产**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F21-9 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F21-12 | 纵向 | `②暂时闲置表: 账面价值 = 账面原值 - 累计折旧 - 减值准备`（每行独立校验） |

> ③ 通过融资租赁租入的固定资产、④ 通过经营租赁租出的固定资产为纯文字描述，不参与数值校验。

**⑤ 未办妥产权证书的固定资产**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F21-10 | 其中项 | `⑤表: sum(合计行以外的所有明细行) = 合计行`（账面价值列独立校验） |
| F21-10a | 完整性 | `⑤表（排除合计行）: 若账面价值 ≠ 0，则未办妥产权证书原因列不应为空` |
| F21-10b | LLM审核 | `⑤表（排除合计行，仅账面价值 ≠ 0 且原因非空的行）: "未办妥产权证书原因"列需调用LLM判断表述是否合理` |

### 22. 在建工程

> 模板中在建工程科目有一个汇总表（在建工程/工程物资/合计），引擎需先校验汇总表。

**在建工程汇总表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F22-0 | 余额 | `报表.在建工程期末 = 在建工程汇总表.合计行.期末账面价值`（仅当汇总表存在时校验；若无汇总表则用①情况表.合计行.期末账面价值） |
| F22-0c | 余额 | `报表.在建工程期初 = 在建工程汇总表.合计行.期初账面价值`（同上） |
| F22-0a | 其中项 | `在建工程汇总表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；含"在建工程"和"工程物资"行） |
| F22-0b | 纵向 | `在建工程汇总表: 账面余额 - 减值准备 = 账面价值`（期末/期初各独立校验） |

**① 在建工程情况表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F22-1 | 交叉 | `①在建工程情况表.合计行.期末账面价值 = 在建工程汇总表."在建工程"行.期末账面价值`（仅当汇总表存在时校验；若无汇总表则 F22-0 直接用①情况表校验报表余额） |
| F22-2 | 交叉 | `①在建工程情况表.合计行.期初账面价值 = 在建工程汇总表."在建工程"行.期初账面价值`（同上） |
| F22-8 | 其中项 | `①情况表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F22-9 | 纵向 | `①情况表: 账面余额 - 减值准备 = 账面价值`（期末/期初各独立校验） |
| F22-3 | 交叉 | `①情况表.账面余额期末合计 = ②项目变动表.期末余额合计` |
| F22-3a | 交叉 | `①情况表.账面余额期初合计 = ②项目变动表.期初余额合计` |

**② 重要在建工程项目本期变动情况**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F22-4 | 宽表 | `②项目变动表: 期初 + 本期增加 - 转入固定资产 - 其他减少 = 期末`（每行独立） |
| F22-10 | 其中项 | `②项目变动表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F22-10a | 完整性 | `②项目变动表（排除合计行）: 若期末余额 ≠ 0，则预算数、工程进度、资金来源列不应为空` |
| F22-10b | 交叉 | `②项目变动表.合计行.期末余额 ≤ ①在建工程情况表.合计行.期末账面余额`（②为重要项目子集，合计不应超过①；合理性校验，不满足则标记异常提示） |
| F22-10c | 交叉 | `②项目变动表.合计行.期初余额 ≤ ①在建工程情况表.合计行.期初账面余额`（同上） |

**③ 本期计提在建工程减值准备情况**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F22-5 | 其中项 | `③减值准备计提表: sum(合计行以外的所有明细行) = 合计行`（本期计提金额列独立校验） |
| F22-5a | 完整性 | `③减值准备计提表（排除合计行）: 若本期计提金额 ≠ 0，则计提原因列不应为空` |
| F22-5b | LLM审核 | `③减值准备计提表（排除合计行，仅本期计提金额 ≠ 0 且计提原因非空的行）: "计提原因"列需调用LLM判断表述是否合理` |
| F22-6 | 交叉 | `③减值准备计提表.本期计提金额合计 = ①情况表.减值准备期末合计 - ①情况表.减值准备期初合计`（若本期无处置减少，则计提金额=期末-期初；仅做合理性校验） |
| F22-7 | 跨科目 | `②项目变动表."转入固定资产"合计 = 固定资产①原价段."在建工程转入"合计` |

### 23. 生产性生物资产

> 模板中生产性生物资产为简单4列表（期初账面价值/本期增加额/本期减少额/期末账面价值），
> 不含原价-折旧-减值三段式结构。以成本计量和以公允价值计量二选一。

**① 以成本计量的生产性生物资产**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F23-1 | 余额 | `报表.生产性生物资产期末 = ①表.合计行.期末账面价值`（或②公允价值计量表，二选一） |
| F23-2 | 余额 | `报表.生产性生物资产期初 = ①表.合计行.期初账面价值` |
| F23-3 | 宽表 | `①表: 期初账面价值 + 本期增加额 - 本期减少额 = 期末账面价值`（每行独立） |
| F23-5 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；含"其中"子项时，子项之和 ≤ 对应父项） |

**② 以公允价值计量的生产性生物资产**（二选一）

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F23-6 | 宽表 | `②表: 期初账面价值 + 本期增加额 - 本期减少额 = 期末账面价值`（每行独立） |
| F23-7 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；含"其中"子项时，子项之和 ≤ 对应父项） |

### 24. 油气资产

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F24-1 | 余额 | `报表.油气资产期末 = ①油气资产分类表.账面价值段.期末合计` |
| F24-2 | 余额 | `报表.油气资产期初 = ①油气资产分类表.账面价值段.期初合计` |
| F24-3 | 宽表 | `①原价段/累计折耗段/减值准备段: 期初 + 本期增加 - 本期减少 = 期末`（每行独立；账面价值段中间列为空，不做宽表校验） |
| F24-4 | 纵向 | `①账面价值 = 原价 - 累计折耗 - 减值准备`（仅期初/期末列校验；"四、账面价值合计" = "一、原价合计" - "二、累计折耗合计" - "三、减值准备合计"） |
| F24-5 | 其中项 | `①各段: sum(合计行以外的所有明细行) = 合计行`（每段每列独立校验） |

### 25. 使用权资产

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F25-1 | 余额 | `报表.使用权资产期末 = ①使用权资产情况表."五、使用权资产账面价值合计".期末余额` |
| F25-2 | 余额 | `报表.使用权资产期初 = ①使用权资产情况表."五、使用权资产账面价值合计".期初余额` |
| F25-3 | 宽表 | `①原价段: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F25-4 | 宽表 | `①累计折旧段: 期初 + 本期增加 - 本期减少 = 期末` |
| F25-5 | 宽表 | `①减值准备段: 期初 + 本期增加 - 本期减少 = 期末` |
| F25-6 | 纵向 | `①账面净值 = 原价 - 累计折旧`（仅期初/期末列校验；"三、使用权资产账面净值合计" = "一、账面原值合计" - "二、累计折旧合计"） |
| F25-6a | 纵向 | `①账面价值 = 账面净值 - 减值准备`（仅期初/期末列校验；"五、使用权资产账面价值合计" = "三、账面净值合计" - "四、减值准备合计"） |
| F25-7 | 其中项 | `①各段: sum(合计行以外的所有明细行) = 合计行`（每段每列独立校验） |

### 26. 无形资产

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F26-1 | 余额 | `报表.无形资产期末 = ①无形资产情况表.账面价值段.期末合计` |
| F26-2 | 余额 | `报表.无形资产期初 = ①无形资产情况表.账面价值段.期初合计` |
| F26-3 | 宽表 | `①原价段: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F26-4 | 宽表 | `①累计摊销段: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F26-5 | 宽表 | `①减值准备段: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F26-6 | 纵向 | `①账面价值 = 原价 - 累计摊销 - 减值准备`（仅期初/期末列校验；"四、账面价值合计" = "一、原价合计" - "二、累计摊销合计" - "三、减值准备合计"） |
| F26-8 | 其中项 | `①各段: sum(合计行以外的所有明细行) = 合计行`（每段每列独立校验） |
| F26-7 | 跨科目 | `①原价段."内部研发"增加合计 = 开发支出①明细表."确认为无形资产"合计`（仅当①表有"内部研发"明细行时校验） |
| F26-7a | LLM审核 | `若①情况表中无"内部研发"明细行，但表格下方文字说明中提及内部研发转入金额，则调用LLM提取该金额并与开发支出①明细表."确认为无形资产"合计核对`（F26-7不适用时的兜底校验） |

**② 确认为无形资产的数据资源**（若存在则校验）

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F26-9 | 纵向 | `②数据资源无形资产表.账面原值段: 期初余额 + 本期增加金额 - 本期减少金额 = 期末余额`（每列独立：外购/自行开发/其他方式/合计） |
| F26-10 | 纵向 | `②数据资源无形资产表.累计摊销段: 期初余额 + 本期增加金额 - 本期减少金额 = 期末余额`（每列独立） |
| F26-11 | 纵向 | `②数据资源无形资产表.减值准备段: 期初余额 + 本期增加金额 - 本期减少金额 = 期末余额`（每列独立） |
| F26-12 | 纵向 | `②数据资源无形资产表: 期末账面价值 = 账面原值期末余额 - 累计摊销期末余额 - 减值准备期末余额`（每列独立） |
| F26-12a | 纵向 | `②数据资源无形资产表: 期初账面价值 = 账面原值期初余额 - 累计摊销期初余额 - 减值准备期初余额`（每列独立） |
| F26-13 | 其中项 | `②账面原值段: "本期增加金额"行 = sum(其中子项: 购入 + 内部研发 + 其他增加)`（每列独立；"其中"子项之和 ≤ 父项，部分列示） |
| F26-13a | 其中项 | `②账面原值段: "本期减少金额"行 = sum(其中子项: 处置 + 失效且终止确认 + 其他减少)`（每列独立；"其中"子项之和 ≤ 父项，部分列示） |
| F26-13b | 其中项 | `②累计摊销段: "本期减少金额"行 = sum(其中子项: 处置 + 失效且终止确认 + 其他减少)`（每列独立；"其中"子项之和 ≤ 父项，部分列示） |
| F26-14 | 交叉 | `②数据资源无形资产表.合计列各行 = sum(外购 + 自行开发 + 其他方式)`（每行独立校验） |
| F26-15 | 交叉 | `②数据资源无形资产表.减值准备期末合计 ≤ ①无形资产情况表.减值准备段.期末合计`（数据资源减值准备是无形资产减值准备的子集） |
| F26-15a | 交叉 | `②数据资源无形资产表.减值准备期初合计 ≤ ①无形资产情况表.减值准备段.期初合计` |
| F26-16 | 交叉 | `②数据资源无形资产表.账面原值期末合计 ≤ ①无形资产情况表.原价段.期末合计`（数据资源原值是无形资产原价的子集） |
| F26-16a | 交叉 | `②数据资源无形资产表.账面原值期初合计 ≤ ①无形资产情况表.原价段.期初合计` |

### 27. 开发支出

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F27-1 | 余额 | `报表.开发支出期末 = ①开发支出明细表.合计行.期末余额` |
| F27-2 | 余额 | `报表.开发支出期初 = ①开发支出明细表.合计行.期初余额` |
| F27-3 | 宽表 | `①明细表: 期初 + 内部开发支出 + 其他增加 - 确认为无形资产 - 转入当期损益 - 其他减少 = 期末`（每行独立） |
| F27-5 | 其中项 | `①明细表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F27-4 | 跨科目 | `①明细表."确认为无形资产"合计 = 无形资产①原价段."内部研发"增加合计` |

### 28. 商誉

**① 商誉账面原值**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F28-1 | 余额 | `报表.商誉期末 = ①商誉原值表.期末合计 - ②商誉减值准备表.期末合计` |
| F28-2 | 余额 | `报表.商誉期初 = ①商誉原值表.期初合计 - ②商誉减值准备表.期初合计` |
| F28-3 | 宽表 | `①商誉原值表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F28-6 | 其中项 | `①商誉原值表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**② 商誉减值准备**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F28-4 | 宽表 | `②商誉减值准备表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F28-7 | 其中项 | `②商誉减值准备表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F28-5 | 交叉 | `①原值期末合计 - ②减值准备期末合计 = 报表.商誉期末` |

### 29. 长期待摊费用

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F29-1 | 余额 | `报表.长期待摊费用期末 = ①明细表.合计行.期末余额` |
| F29-2 | 余额 | `报表.长期待摊费用期初 = ①明细表.合计行.期初余额` |
| F29-3 | 宽表 | `①明细表: 期初 + 本期增加 - 本期摊销 - 其他减少 = 期末`（每行独立） |
| F29-4 | 其中项 | `①明细表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 30. 递延所得税资产 / 递延所得税负债

**① 未经抵销的递延所得税资产和负债**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F30-1 | 余额 | `报表.递延所得税资产期末 = ①未经抵销表.资产段小计行.期末余额`（若无小计行但"递延所得税资产"行有数据则该行即为小计） |
| F30-2 | 余额 | `报表.递延所得税资产期初 = ①未经抵销表.资产段小计行.期初余额` |
| F30-3 | 余额 | `报表.递延所得税负债期末 = ①未经抵销表.负债段小计行.期末余额`（若无小计行但"递延所得税负债"行有数据则该行即为小计） |
| F30-4 | 余额 | `报表.递延所得税负债期初 = ①未经抵销表.负债段小计行.期初余额` |
| F30-5 | 其中项 | `①未经抵销表.资产段: sum(小计行以外的所有明细行) = 小计行`（每个数值列独立校验） |
| F30-6 | 其中项 | `①未经抵销表.负债段: sum(小计行以外的所有明细行) = 小计行`（每个数值列独立校验） |

**② 未确认递延所得税资产明细**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F30-7 | 其中项 | `②未确认递延所得税资产明细表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**③ 未确认递延所得税资产的可抵扣亏损到期年度**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F30-8 | 其中项 | `③可抵扣亏损到期年度表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F30-8a | 交叉 | `③可抵扣亏损到期年度表: 同一到期年度行的期末余额 = 期初余额`（逐行比较，排除合计行和已到期年度行；不一致时标记异常提示） |
| F30-8b | 完整性 | `③可抵扣亏损到期年度表: 若同一到期年度行的期末余额 ≠ 期初余额，则备注列不应为空`（需说明差异原因，如税务调整、更正等） |

> 以抵销后净额列示表为补充披露，不参与数值校验。

### 31. 其他非流动资产

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F31-1 | 余额 | `报表.其他非流动资产期末 = ①明细表.合计行.期末余额` |
| F31-2 | 余额 | `报表.其他非流动资产期初 = ①明细表.合计行.期初余额` |
| F31-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |

### 32. 短期借款

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F32-1 | 余额 | `报表.短期借款期末 = ①分类表.合计行.期末余额` |
| F32-2 | 余额 | `报表.短期借款期初 = ①分类表.合计行.期初余额` |
| F32-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**② 已逾期未偿还的短期借款**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F32-4 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 33. 交易性金融负债

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F33-1 | 余额 | `报表.交易性金融负债期末 = ①分类表.合计行.期末公允价值` |
| F33-2 | 余额 | `报表.交易性金融负债期初 = ①分类表.合计行.期初公允价值` |
| F33-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 34. 衍生金融负债

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F34-1 | 余额 | `报表.衍生金融负债期末 = ①明细表.合计行.期末余额` |
| F34-2 | 余额 | `报表.衍生金融负债期初 = ①明细表.合计行.期初余额` |
| F34-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 35. 应付票据

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F35-1 | 余额 | `报表.应付票据期末 = ①分类表.合计行.期末余额` |
| F35-2 | 余额 | `报表.应付票据期初 = ①分类表.合计行.期初余额` |
| F35-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F35-4 | LLM审核 | `①分类表下方若有文字说明"本期已到期未支付的应付票据总金额为XX元"，调用LLM提取该金额并校验是否为合理数值（≥0且≤合计行期末余额）；若无该文字说明则跳过` |

### 36. 应付账款

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F36-1 | 余额 | `报表.应付账款期末 = ①明细表.合计行.期末余额` |
| F36-2 | 余额 | `报表.应付账款期初 = ①明细表.合计行.期初余额` |
| F36-5 | 其中项 | `①应付账款按账龄表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F36-4 | 账龄衔接 | `①应付账款按账龄表: 账龄衔接校验`（按通用规则，逐段比较期末 vs 期初前一账龄段，合理性校验；引擎自动适配3段或5段；若"1年以内"下有"其中"细分行，按通用规则校验子项之和=父项） |

**② 账龄超过1年的重要应付账款**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F36-3 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F36-3a | 完整性 | `②表（排除合计行）: 若期末余额 ≠ 0，则未偿还原因列不应为空` |
| F36-3b | LLM审核 | `②表（排除合计行，仅期末余额 ≠ 0 且原因非空的行）: "未偿还原因"列需调用LLM判断表述是否合理` |
| F36-6 | 交叉 | `②表.合计行.期末余额 ≤ ①按账龄表中账龄超过1年的各段期末余额之和`（②为重要项目子集，合计不应超过①中超1年部分；合理性校验，不满足则标记异常提示） |

### 37. 预收款项

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F37-1 | 余额 | `报表.预收款项期末 = ①明细表.合计行.期末余额` |
| F37-2 | 余额 | `报表.预收款项期初 = ①明细表.合计行.期初余额` |
| F37-5 | 其中项 | `①预收款项按账龄表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F37-4 | 账龄衔接 | `①预收款项按账龄表: 账龄衔接校验`（按通用规则，逐段比较期末 vs 期初前一账龄段，合理性校验；引擎自动适配3段或5段；若"1年以内"下有"其中"细分行，按通用规则校验子项之和=父项） |

**② 账龄超过1年的重要预收款项**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F37-3 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F37-3a | 完整性 | `②表（排除合计行）: 若期末余额 ≠ 0，则未结转原因列不应为空` |
| F37-3b | LLM审核 | `②表（排除合计行，仅期末余额 ≠ 0 且原因非空的行）: "未结转原因"列需调用LLM判断表述是否合理` |
| F37-6 | 交叉 | `②表.合计行.期末余额 ≤ ①按账龄表中账龄超过1年的各段期末余额之和`（②为重要项目子集，合计不应超过①中超1年部分；合理性校验，不满足则标记异常提示） |

### 38. 合同负债

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F38-1 | 余额 | `报表.合同负债期末 = ①明细表.合计行.期末余额` |
| F38-2 | 余额 | `报表.合同负债期初 = ①明细表.合计行.期初余额` |
| F38-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**② 本期合同负债账面价值的重大变动**（若存在则校验）

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F38-4 | 其中项 | `②表: sum(合计行以外的所有明细行) = 合计行`（变动金额列独立校验） |
| F38-4a | 完整性 | `②表（排除合计行）: 若变动金额 ≠ 0，则变动原因列不应为空` |
| F38-4b | LLM审核 | `②表（排除合计行，仅变动金额 ≠ 0 且变动原因非空的行）: "变动原因"列需调用LLM判断合同负债重大变动的原因表述是否合理` |

### 39. 应付职工薪酬

**① 应付职工薪酬总表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F39-1 | 余额 | `报表.应付职工薪酬期末 = ①总表.合计行.期末余额` |
| F39-2 | 余额 | `报表.应付职工薪酬期初 = ①总表.合计行.期初余额` |
| F39-3 | 宽表 | `①总表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F39-10 | 其中项 | `①总表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F39-4 | 交叉 | `①总表."短期薪酬"行.各列 = ②短期薪酬明细表.合计行.各列`（期初/本期增加/本期减少/期末各列独立校验） |
| F39-5 | 交叉 | `①总表."设定提存计划"行.各列 = ③设定提存计划表.合计行.各列`（期初/本期增加/本期减少/期末各列独立校验） |

**② 短期薪酬明细表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F39-6 | 宽表 | `②短期薪酬明细表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F39-8 | 其中项 | `②表: sum(合计行以外的所有主项明细行) = 合计行`（每个数值列独立校验；含"其中"子项时，子项之和 = 对应父项如"社会保险费"（社会保险费 = 医疗保险费 + 工伤保险费 + 其他），合计行不含"其中"子项避免重复加总） |
| F39-11 | 二级明细 | `报表."其中：应付工资"行.期末 = ②短期薪酬明细表."工资、奖金、津贴和补贴"行.期末余额`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F39-11a | 二级明细 | `报表."其中：应付工资"行.期初 = ②短期薪酬明细表."工资、奖金、津贴和补贴"行.期初余额`（同上） |
| F39-12 | 二级明细 | `报表."其中：应付福利费"行.期末 = ②短期薪酬明细表."职工福利费"行.期末余额`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F39-12a | 二级明细 | `报表."其中：应付福利费"行.期初 = ②短期薪酬明细表."职工福利费"行.期初余额`（同上） |

**③ 设定提存计划表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F39-7 | 宽表 | `③设定提存计划表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F39-9 | 其中项 | `③表: sum(合计行以外的所有主项明细行) = 合计行`（每个数值列独立校验；含"其中"子项时，子项之和 = 对应父项如"离职后福利"，合计行不含"其中"子项避免重复加总） |

### 40. 应交税费

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F40-1 | 余额 | `报表.应交税费期末 = ①明细表.合计行.期末余额` |
| F40-2 | 余额 | `报表.应交税费期初 = ①明细表.合计行.期初余额` |
| F40-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F40-4 | 二级明细 | `报表."其中：应交税金"行.期末 = ①明细表中属于"税"的各行期末余额之和`（仅当报表中存在该二级明细行时校验，否则跳过；"税"包括增值税、消费税、资源税、企业所得税、城市维护建设税、房产税、土地使用税、个人所得税等，不含教育费附加等"费"类项目；引擎按行名关键字"税"筛选，排除含"费"/"附加"的行） |
| F40-4a | 二级明细 | `报表."其中：应交税金"行.期初 = ①明细表中属于"税"的各行期初余额之和`（同上） |
| F40-4b | LLM审核 | `若①明细表中存在无法通过行名关键字明确区分"税"或"费"的项目（如"其他税费"），则调用LLM判断该项目应归入税金还是费用，辅助F40-4/4a的计算`（F40-4/4a的兜底辅助） |

### 41. 其他应付款

**汇总表（应付利息/应付股利/其他应付款项/合计）

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F41-1 | 余额 | `报表.其他应付款期末 = 汇总表.合计行.期末余额` |
| F41-2 | 余额 | `报表.其他应付款期初 = 汇总表.合计行.期初余额` |
| F41-9 | 其中项 | `汇总表: "应付利息" + "应付股利" + "其他应付款项" = 合计行`（每个数值列独立校验） |
| F41-3 | 二级明细 | `报表."其中：应付利息"行.期末 = 汇总表."应付利息"行.期末余额`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F41-3a | 二级明细 | `报表."其中：应付利息"行.期初 = 汇总表."应付利息"行.期初余额`（同上） |
| F41-4 | 二级明细 | `报表."其中：应付股利"行.期末 = 汇总表."应付股利"行.期末余额`（仅当报表中存在该二级明细行时校验，否则跳过） |
| F41-4a | 二级明细 | `报表."其中：应付股利"行.期初 = 汇总表."应付股利"行.期初余额`（同上） |

**① 应付利息**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F41-10 | 交叉 | `①应付利息表.合计行.期末 = 汇总表."应付利息"行.期末`（期初同理） |
| F41-5 | 其中项 | `①应付利息表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F41-5a | 其中项 | `①重要的已逾期未支付的利息表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；若存在则校验） |
| F41-5b | 完整性 | `①逾期利息表（排除合计行）: 若逾期金额 ≠ 0，则逾期原因列不应为空`（若存在则校验） |
| F41-5c | LLM审核 | `①逾期利息表（排除合计行，仅逾期金额 ≠ 0 且原因非空的行）: "逾期原因"列需调用LLM判断表述是否合理`（若存在则校验） |

**② 应付股利**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F41-11 | 交叉 | `②应付股利表.合计行.期末 = 汇总表."应付股利"行.期末`（期初同理） |
| F41-6 | 其中项 | `②应付股利表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**③ 其他应付款项**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F41-12 | 交叉 | `③其他应付款项按款项性质表.合计行.期末 = 汇总表."其他应付款项"行.期末`（期初同理） |
| F41-7 | 其中项 | `③其他应付款项按款项性质表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F41-8 | 其中项 | `③账龄超过1年的重要其他应付款项表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F41-8a | 完整性 | `③重要其他应付款项表（排除合计行）: 若期末余额 ≠ 0，则未偿还原因列不应为空` |
| F41-8b | LLM审核 | `③重要其他应付款项表（排除合计行，仅期末余额 ≠ 0 且原因非空的行）: "未偿还原因"列需调用LLM判断表述是否合理` |
| F41-8c | 交叉 | `③重要其他应付款项表.合计行.期末余额 ≤ ③按款项性质表.合计行.期末余额`（重要项目为子集，合理性校验） |

### 42. 持有待售负债

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F42-1 | 余额 | `报表.持有待售负债期末 = ①明细表.合计行.期末账面价值` |
| F42-2 | 余额 | `报表.持有待售负债期初 = ①明细表.合计行.期初账面价值` |
| F42-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 43. 一年内到期的非流动负债

**①②③ 一年内到期的非流动负债明细**

> 模板中一年内到期的非流动负债汇总表包含：一年内到期的长期借款/应付债券/长期应付款/
> 租赁负债/其他长期负债/预计负债/合计。
> 各明细行应与对应科目附注中的"减：一年内到期"行或"重分类至一年内到期"行交叉核对。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F43-1 | 余额 | `报表.一年内到期的非流动负债期末 = 汇总表.合计行.期末余额` |
| F43-5 | 余额 | `报表.一年内到期的非流动负债期初 = 汇总表.合计行.期初余额` |
| F43-6 | 其中项 | `一年内到期的非流动负债汇总表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F43-7 | 跨科目 | `①一年内到期的长期借款.期末 = 长期借款①分类表."减：一年内到期的长期借款"行.期末`（仅当长期借款①表有该减项行时校验，否则跳过） |
| F43-7a | 跨科目 | `①一年内到期的长期借款.期初 = 长期借款①分类表."减：一年内到期的长期借款"行.期初`（同上） |
| F43-8 | 跨科目 | `②一年内到期的应付债券.期末 = 应付债券①明细表."减：一年内到期的应付债券"行.期末`（仅当应付债券①表有该减项行时校验，否则跳过） |
| F43-8a | 跨科目 | `②一年内到期的应付债券.期初 = 应付债券①明细表."减：一年内到期的应付债券"行.期初`（同上） |
| F43-9 | 跨科目 | `③一年内到期的长期应付款.期末 = 长期应付款①明细表."减：一年内到期长期应付款项"行.期末`（仅当长期应付款①表有该减项行时校验，否则跳过） |
| F43-9a | 跨科目 | `③一年内到期的长期应付款.期初 = 长期应付款①明细表."减：一年内到期长期应付款项"行.期初`（同上） |
| F43-10 | 跨科目 | `汇总表."1年内到期的租赁负债"行.期末 = 租赁负债①表."重分类至一年内到期的非流动负债"行.期末`（仅当汇总表有该行且租赁负债①表有对应行时校验，否则跳过） |
| F43-10a | 跨科目 | `汇总表."1年内到期的租赁负债"行.期初 = 租赁负债①表."重分类至一年内到期的非流动负债"行.期初`（同上） |

> **一年内到期的应付债券子表：**
> 模板中（2）一年内到期的应付债券有两个子表：
> - 基本信息表（债券名称/面值/发行日期/债券期限/发行金额）→ 其中项校验
> - 增减变动表（期初+发行+计提利息±溢折价摊销-偿还=期末）→ 宽表校验

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F43-11 | 其中项 | `（2）一年内到期的应付债券基本信息表: sum(小计行以外的所有明细行) = 小计行`（面值/发行金额列独立校验） |
| F43-12 | 宽表 | `（2）一年内到期的应付债券增减变动表: 期初余额 + 本期发行 + 按面值计提利息 ± 溢折价摊销 - 本期偿还 = 期末余额`（每行独立） |
| F43-13 | 其中项 | `（2）一年内到期的应付债券增减变动表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 44. 其他流动负债

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F44-1 | 余额 | `报表.其他流动负债期末 = ①明细表.合计行.期末余额` |
| F44-2 | 余额 | `报表.其他流动负债期初 = ①明细表.合计行.期初余额` |
| F44-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |

### 45. 长期借款

> 模板中长期借款表有"小计/减：一年内到期的长期借款/合计"结构。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F45-1 | 余额 | `报表.长期借款期末 = ①分类表.合计行.期末余额` |
| F45-2 | 余额 | `报表.长期借款期初 = ①分类表.合计行.期初余额` |
| F45-3 | 其中项 | `①表: sum(各借款类别明细行) = 小计行`（每个数值列独立校验；不含"减：一年内到期"行） |
| F45-4 | 纵向 | `①表: 合计行 = 小计行 - "减：一年内到期的长期借款"行`（每个数值列独立校验；仅当表中有该减项行时校验） |

### 46. 应付债券

**① 应付债券明细表**

> 模板中应付债券①表有"小计/减：一年内到期的应付债券/合计"结构。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F46-1 | 余额 | `报表.应付债券期末 = ①明细表.合计行.期末余额` |
| F46-2 | 余额 | `报表.应付债券期初 = ①明细表.合计行.期初余额` |
| F46-4 | 其中项 | `①明细表: sum(各债券明细行) = 小计行`（每个数值列独立校验；不含"减：一年内到期"行） |
| F46-4a | 纵向 | `①明细表: 合计行 = 小计行 - "减：一年内到期的应付债券"行`（每个数值列独立校验；仅当表中有该减项行时校验） |

**② 应付债券的增减变动**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F46-3 | 宽表 | `②增减变动表: 期初余额 + 本期发行 + 按面值计提利息 ± 溢折价摊销 - 本期偿还 = 期末余额`（每行独立） |
| F46-5 | 其中项 | `②增减变动表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F46-5a | 完整性 | `②增减变动表（排除合计行）: 若期末余额 ≠ 0，则面值、发行日期、债券期限、发行金额列均不应为空` |
| F46-6 | 交叉 | `②增减变动表.期末余额合计 = ①明细表.小计行.期末余额`（②增减变动表含全部债券含一年内到期部分，对应①表小计行） |
| F46-6a | 交叉 | `②增减变动表.期初余额合计 = ①明细表.小计行.期初余额` |

### 47. 租赁负债

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F47-1 | 余额 | `报表.租赁负债期末 = ①明细表.租赁负债净额.期末余额` |
| F47-2 | 余额 | `报表.租赁负债期初 = ①明细表.租赁负债净额.期初余额` |
| F47-3 | 纵向 | `①表: 租赁付款额 - 未确认的融资费用 - 重分类至一年内到期 = 租赁负债净额`（期末/期初各独立校验） |
| F47-4 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；若表格有明细行） |

### 48. 长期应付款

> 模板中长期应付款科目有一个汇总表（长期应付款/专项应付款/合计），引擎需先校验汇总表。

**长期应付款汇总表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F48-0 | 余额 | `报表.长期应付款期末 = 汇总表.合计行.期末余额` |
| F48-0a | 余额 | `报表.长期应付款期初 = 汇总表.合计行.期初余额` |
| F48-0b | 其中项 | `汇总表: "长期应付款" + "专项应付款" = 合计行`（每个数值列独立校验） |

**① 长期应付款明细表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F48-1 | 交叉 | `①长期应付款明细表.合计行.期末 = 汇总表."长期应付款"行.期末` |
| F48-2 | 交叉 | `①长期应付款明细表.合计行.期初 = 汇总表."长期应付款"行.期初` |
| F48-6 | 其中项 | `①长期应付款明细表: sum(小计行以外的所有明细行) = 小计行`（每个数值列独立校验；不含"减：一年内到期长期应付款项"行） |
| F48-6a | 纵向 | `①长期应付款明细表: 合计行 = 小计行 - "减：一年内到期长期应付款项"行`（每个数值列独立校验；仅当表中有该减项行时校验） |

**② 专项应付款**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F48-3 | 交叉 | `②专项应付款表.合计行.期末 = 汇总表."专项应付款"行.期末`（期初同理） |
| F48-5 | 宽表 | `②专项应付款表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F48-7 | 其中项 | `②专项应付款表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 49. 长期应付职工薪酬

**① 长期应付职工薪酬总表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F49-1 | 余额 | `报表.长期应付职工薪酬期末 = ①总表.合计行.期末余额` |
| F49-2 | 余额 | `报表.长期应付职工薪酬期初 = ①总表.合计行.期初余额` |
| F49-3 | 宽表 | `①总表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F49-4 | 其中项 | `①总表: sum(小计行以外的所有明细行) = 小计行`（每个数值列独立校验；不含"减：一年内到期的长期应付职工薪酬"行；若表中无小计行则直接用合计行） |
| F49-4a | 纵向 | `①总表: 合计行 = 小计行 - "减：一年内到期的长期应付职工薪酬"行`（每个数值列独立校验；仅当表中有该减项行时校验，否则跳过） |

**② 设定受益计划变动情况**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F49-5 | 纵向 | `②设定受益计划义务现值表: "一、期初余额" + "二、计入当期损益" + "三、计入其他综合收益" + "四、其他变动" = "五、期末余额"`（本期/上期各独立校验） |
| F49-6 | 纵向 | `②计划资产表: "一、期初余额" + "二、计入当期损益" + "三、计入其他综合收益" + "四、其他变动" = "五、期末余额"`（本期/上期各独立校验；若存在则校验） |
| F49-7 | 纵向 | `②设定受益计划净负债（净资产）表: "一、期初余额" + "二、计入当期损益" + "三、计入其他综合收益" + "四、其他变动" = "五、期末余额"`（本期/上期各独立校验） |
| F49-8 | 交叉 | `②净负债表.期末 = 义务现值表.期末 - 计划资产表.期末`（若存在计划资产） |
| F49-9 | 其中项 | `②义务现值表: "二、计入当期损益" = sum(其下子项1~4)`（本期/上期各独立校验；子项包括当期服务成本、过去服务成本、结算利得、利息净额等，引擎自动识别"二"下的所有子项） |
| F49-9a | 其中项 | `②义务现值表: "三、计入其他综合收益" = sum(其下子项)`（本期/上期各独立校验；子项包括精算利得等） |
| F49-9b | 其中项 | `②义务现值表: "四、其他变动" = sum(其下子项)`（本期/上期各独立校验；子项包括结算时消除的负债、已支付的福利等） |
| F49-10 | 其中项 | `②计划资产表: 各主段下子项之和 = 该主段金额`（同F49-9逻辑；若存在则校验） |
| F49-10a | 其中项 | `②净负债表: 各主段下子项之和 = 该主段金额`（同F49-9逻辑） |

### 50. 预计负债

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F50-1 | 余额 | `报表.预计负债期末 = ①明细表.合计行.期末余额` |
| F50-2 | 余额 | `报表.预计负债期初 = ①明细表.合计行.期初余额` |
| F50-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 51. 递延收益

**① 递延收益明细表**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F51-1 | 余额 | `报表.递延收益期末 = ①明细表.合计行.期末余额` |
| F51-2 | 余额 | `报表.递延收益期初 = ①明细表.合计行.期初余额` |
| F51-3 | 宽表 | `①明细表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F51-6 | 其中项 | `①明细表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

**② 递延收益-政府补助情况**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F51-4 | 宽表 | `②政府补助表: 期初 + 本期新增补助金额 - 本期计入损益金额 - 本期返还的金额 ± 其他变动 = 期末`（每行独立） |
| F51-5 | 交叉 | `②政府补助表.期末合计 ≤ ①明细表.期末合计`（政府补助是递延收益的子集） |
| F51-7 | 其中项 | `②政府补助表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F51-7a | 完整性 | `②政府补助表（排除合计行）: 若期末余额或期初余额 ≠ 0，则"与资产相关/与收益相关"列不应为空` |
| F51-7b | 完整性 | `②政府补助表（排除合计行）: 若本期返还的金额 ≠ 0，则"本期返还的原因"列不应为空` |
| F51-7c | LLM审核 | `②政府补助表（排除合计行，仅期末余额 ≠ 0 且"与资产相关/与收益相关"非空的行）: 调用LLM判断该分类是否与补助项目名称及计入损益的列报项目匹配合理` |
| F51-7d | LLM审核 | `②政府补助表（排除合计行，仅本期返还金额 ≠ 0 且返还原因非空的行）: "本期返还的原因"列需调用LLM判断表述是否合理` |

### 52. 其他非流动负债

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F52-1 | 余额 | `报表.其他非流动负债期末 = ①明细表.合计行.期末余额` |
| F52-2 | 余额 | `报表.其他非流动负债期初 = ①明细表.合计行.期初余额` |
| F52-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 53. 实收资本（股本）

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F53-1 | 余额 | `报表.实收资本期末 = ①实收资本明细表.合计行.期末投资金额` |
| F53-2 | 余额 | `报表.实收资本期初 = ①实收资本明细表.合计行.期初投资金额` |
| F53-3 | 宽表 | `①明细表: 期初投资金额 + 本期增加 - 本期减少 = 期末投资金额`（每行独立；比例列为skip） |
| F53-4 | 其中项 | `①明细表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；比例列合计应为100%） |

### 54. 其他权益工具

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F54-1 | 余额 | `报表.其他权益工具期末 = ①明细表.合计行.期末账面价值` |
| F54-2 | 余额 | `报表.其他权益工具期初 = ①明细表.合计行.期初账面价值` |
| F54-3 | 宽表 | `①明细表: 期初账面价值 + 本期增加 - 本期减少 = 期末账面价值`（每行独立；数量列为skip） |
| F54-4 | 其中项 | `①明细表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 55. 资本公积

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F55-1 | 余额 | `报表.资本公积期末 = ①明细表.合计行.期末余额` |
| F55-2 | 余额 | `报表.资本公积期初 = ①明细表.合计行.期初余额` |
| F55-3 | 宽表 | `①明细表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F55-4 | 其中项 | `①明细表: sum(合计行以外的所有主项明细行) = 合计行`（每个数值列独立校验；含"其中"子项时，子项之和 ≤ 对应父项如"其他资本公积"，合计行不含"其中"子项避免重复加总） |
| F55-5 | LLM审核 | `①明细表下方若有文字说明逐项列示了资本公积增加或减少的原因及金额，调用LLM提取文字中各增加明细项金额之和，与表格"本期增加"列合计行核对；提取各减少明细项金额之和，与"本期减少"列合计行核对；判断是否一致` |

### 56. 专项储备

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F56-1 | 余额 | `报表.专项储备期末 = ①明细表.合计行.期末余额` |
| F56-2 | 余额 | `报表.专项储备期初 = ①明细表.合计行.期初余额` |
| F56-3 | 宽表 | `①明细表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| F56-4 | 其中项 | `①明细表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 57. 盈余公积

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F57-1 | 余额 | `报表.盈余公积期末 = ①明细表.合计行.期末余额` |
| F57-2 | 余额 | `报表.盈余公积期初 = ①明细表.合计行.期初余额` |
| F57-3 | 宽表 | `①明细表: 期初 + 本期增加(提取) - 本期减少(弥补亏损) = 期末`（每行独立） |
| F57-4 | 跨科目 | `①明细表."本期增加-提取"合计 = 未分配利润①明细表."提取盈余公积"行` |
| F57-5 | 其中项 | `①明细表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| F57-6 | 二级明细 | `报表."其中：法定公积金"期末 = ①明细表."法定盈余公积金"行.期末余额` |
| F57-6a | 二级明细 | `报表."其中：法定公积金"期初 = ①明细表."法定盈余公积金"行.期初余额` |
| F57-7 | 二级明细 | `报表."其中：任意公积金"期末 = ①明细表."任意盈余公积金"行.期末余额` |
| F57-7a | 二级明细 | `报表."其中：任意公积金"期初 = ①明细表."任意盈余公积金"行.期初余额` |
| F57-8 | 二级明细 | `报表."其中：储备基金"期末 = ①明细表."储备基金"行.期末余额` |
| F57-8a | 二级明细 | `报表."其中：储备基金"期初 = ①明细表."储备基金"行.期初余额` |
| F57-9 | 二级明细 | `报表."其中：企业发展基金"期末 = ①明细表."企业发展基金"行.期末余额` |
| F57-9a | 二级明细 | `报表."其中：企业发展基金"期初 = ①明细表."企业发展基金"行.期初余额` |
| F57-10 | 二级明细 | `报表."其中：利润归还投资"期末 = ①明细表."利润归还投资"行.期末余额`（仅当报表存在该行时校验） |
| F57-10a | 二级明细 | `报表."其中：利润归还投资"期初 = ①明细表."利润归还投资"行.期初余额`（仅当报表存在该行时校验） |

### 58. 未分配利润

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F58-1 | 余额 | `报表.未分配利润期末 = ①明细表.期末未分配利润` |
| F58-2 | 余额 | `报表.未分配利润期初 = ①明细表.期初未分配利润`（调整后） |
| F58-3 | 跨科目 | `①明细表."本期净利润"行 = 利润表."归属于母公司所有者的净利润"` |
| F58-4 | 跨科目 | `①明细表."提取盈余公积"行 = 盈余公积①明细表."本期增加-提取"合计` |
| F58-5 | 交叉 | `①明细表: 期初(调整后) + 本期净利润 - 提取盈余公积 - 分配股利 ± 其他调整 = 期末` |
| F58-6 | 其中项 | `①明细表: "本期增加额"行 = sum(其下各"其中"子项：本期净利润转入 + 盈余公积弥补亏损转入 + 资本公积弥补亏损转入 + 其他调整因素 + …)`（每个数值列独立校验） |
| F58-7 | 其中项 | `①明细表: "本期减少额"行 = sum(其下各"其中"子项：本期提取盈余公积数 + 本期提取一般风险准备 + 本期分配现金股利数 + 转增资本 + 其他减少 + …)`（每个数值列独立校验） |
| F58-8 | 纵向 | `①明细表: "本期期初余额" = "上年年末余额" + "期初调整金额"`（每个数值列独立校验） |
| F58-9 | 纵向 | `①明细表: "本期期末余额" = "本期期初余额" + "本期增加额" - "本期减少额"`（每个数值列独立校验） |
| F58-10 | 期间衔接 | `①明细表: 本期金额列."本期期初余额" = 上期金额列."本期期末余额"` |

---

## 二、利润表科目

### 1. 营业收入 / 营业成本

**① 营业收入、营业成本汇总表（主营业务/其他业务/合计）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F59-1 | 余额 | `报表.营业收入 = ①汇总表.收入合计行.本期金额` |
| F59-2 | 余额 | `报表.营业成本 = ①汇总表.成本合计行.本期金额` |
| F59-3 | 余额 | `报表.营业收入(上期) = ①汇总表.收入合计行.上期金额` |
| F59-4 | 余额 | `报表.营业成本(上期) = ①汇总表.成本合计行.上期金额` |
| F59-5 | 其中项 | `①表收入部分: sum(合计行以外的所有明细行) = 收入合计行`（每个数值列独立校验） |
| F59-6 | 其中项 | `①表成本部分: sum(合计行以外的所有明细行) = 成本合计行`（每个数值列独立校验） |

**②③④ 按行业/按地区/按转让时间划分**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F59-7 | 交叉 | `②按行业表.收入合计 = ①汇总表.收入合计`（本期/上期各独立校验） |
| F59-7a | 交叉 | `②按行业表.成本合计 = ①汇总表.成本合计`（本期/上期各独立校验） |
| F59-8 | 交叉 | `③按地区表.收入合计 = ①汇总表.收入合计`（本期/上期各独立校验） |
| F59-8a | 交叉 | `③按地区表.成本合计 = ①汇总表.成本合计`（本期/上期各独立校验） |
| F59-9 | 交叉 | `④按转让时间表.收入合计 = ①汇总表.收入合计`（本期各独立校验） |
| F59-10 | 其中项 | `②按行业表: sum(合计行以外的所有明细行) = 合计行`（收入/成本各列独立校验；"主营业务"和"其他业务"各有小计行，小计行之和=合计行） |
| F59-11 | 其中项 | `③按地区表: sum(小计行以外的所有明细行) = 小计行`（收入/成本各列独立校验） |
| F59-12 | 交叉 | `②按行业表."主营业务"小计行.收入 = ①汇总表."主营业务"行.收入`（本期/上期各独立校验） |
| F59-12a | 交叉 | `②按行业表."主营业务"小计行.成本 = ①汇总表."主营业务"行.成本`（本期/上期各独立校验） |
| F59-13 | 交叉 | `②按行业表."其他业务"小计行.收入 = ①汇总表."其他业务"行.收入`（本期/上期各独立校验） |
| F59-13a | 交叉 | `②按行业表."其他业务"小计行.成本 = ①汇总表."其他业务"行.成本`（本期/上期各独立校验） |
| F59-14 | 其中项 | `④按转让时间表: "主营业务"行各分部收入/成本之和 + "其他业务"行各分部收入/成本之和 = 合计行`（每列独立校验） |
| F59-15 | 二级明细 | `报表."其中：主营业务收入"本期 = ①汇总表."主营业务"行.收入.本期金额`（仅当报表存在该行时校验） |
| F59-15a | 二级明细 | `报表."其中：主营业务收入"上期 = ①汇总表."主营业务"行.收入.上期金额`（仅当报表存在该行时校验） |
| F59-16 | 二级明细 | `报表."其中：主营业务成本"本期 = ①汇总表."主营业务"行.成本.本期金额`（仅当报表存在该行时校验） |
| F59-16a | 二级明细 | `报表."其中：主营业务成本"上期 = ①汇总表."主营业务"行.成本.上期金额`（仅当报表存在该行时校验） |
| F59-17 | 其中项 | `④按转让时间表: "主营业务"行 = "其中：在某一时点确认" + "在某一时段确认" + "租赁收入"`（每个分部的收入/成本列独立校验；仅当存在"其中"子项时校验） |
| F59-18 | 其中项 | `④按转让时间表: "其他业务"行 = "其中：在某一时点确认" + "在某一时段确认" + "租赁收入"`（每个分部的收入/成本列独立校验；仅当存在"其中"子项时校验） |
| F59-19 | 交叉 | `③按地区表.主营业务收入小计 = ①汇总表."主营业务"行.收入`（本期/上期各独立校验；③表仅列示主营业务） |
| F59-19a | 交叉 | `③按地区表.主营业务成本小计 = ①汇总表."主营业务"行.成本`（本期/上期各独立校验） |
| F59-20 | 其中项 | `⑥剩余履约义务交易价格分摊表: sum(各年度行) = 合计行`（仅当存在该表时校验） |

> ⑤ 履约义务为纯文字描述，不参与数值校验。⑥ 剩余履约义务交易价格分摊表若存在则参与校验（F59-20）。

### 2. 税金及附加

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F60-1 | 余额 | `报表.税金及附加 = ①明细表.合计行.本期金额` |
| F60-2 | 余额 | `报表.税金及附加(上期) = ①明细表.合计行.上期金额` |
| F60-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 3. 销售费用

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F61-1 | 余额 | `报表.销售费用 = ①明细表.合计行.本期金额` |
| F61-2 | 余额 | `报表.销售费用(上期) = ①明细表.合计行.上期金额` |
| F61-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |

### 4. 管理费用

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F62-1 | 余额 | `报表.管理费用 = ①明细表.合计行.本期金额` |
| F62-2 | 余额 | `报表.管理费用(上期) = ①明细表.合计行.上期金额` |
| F62-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |

### 5. 研发费用

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F63-1 | 余额 | `报表.研发费用 = ①明细表.合计行.本期金额` |
| F63-2 | 余额 | `报表.研发费用(上期) = ①明细表.合计行.上期金额` |
| F63-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |

### 6. 财务费用

> 模板中财务费用表结构特殊：利息费用总额 - 利息资本化 = 利息费用净额，
> 合计行 = 利息费用净额 - 利息收入 + 承兑汇票贴息 + 汇兑损益 - 汇兑损益资本化 + 手续费及其他。
> 不是简单的其中项加总，引擎需按实际结构适配。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F64-1 | 余额 | `报表.财务费用 = ①明细表.合计行.本期金额` |
| F64-2 | 余额 | `报表.财务费用(上期) = ①明细表.合计行.上期金额` |
| F64-3 | 纵向 | `①表: 利息费用净额 = 利息费用总额 - 利息资本化`（本期/上期各独立校验） |
| F64-4 | 其中项 | `①表: 合计行 = 利息费用净额 - 利息收入 + 其他各项之和`（本期/上期各独立校验；引擎需按实际行名动态识别） |
| F64-5 | 二级明细 | `报表."其中：利息费用"本期 = ①明细表."利息费用净额"行.本期发生额`（仅当报表存在该行时校验；利息费用净额 = 利息费用总额 - 利息资本化） |
| F64-5a | 二级明细 | `报表."其中：利息费用"上期 = ①明细表."利息费用净额"行.上期发生额`（仅当报表存在该行时校验） |
| F64-6 | 二级明细 | `报表."利息收入"本期 = ①明细表."利息收入"行.本期发生额`（仅当报表存在该行时校验；注意报表中利息收入为正数，附注表中"减：利息收入"也为正数，取绝对值比较） |
| F64-6a | 二级明细 | `报表."利息收入"上期 = ①明细表."利息收入"行.上期发生额`（仅当报表存在该行时校验） |

### 7. 其他收益

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F65-1 | 余额 | `报表.其他收益 = ①明细表.合计行.本期金额` |
| F65-2 | 余额 | `报表.其他收益(上期) = ①明细表.合计行.上期金额` |
| F65-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（本期/上期各列独立校验；"其中：政府补助"行不参与加总，为合计行的子集） |
| F65-4 | 交叉 | `①表."其中：政府补助"行 ≤ ①表.合计行`（本期/上期各独立校验；政府补助是其他收益的子集） |

### 8. 投资收益

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F66-1 | 余额 | `报表.投资收益 = ①明细表.合计行.本期金额` |
| F66-2 | 余额 | `报表.投资收益(上期) = ①明细表.合计行.上期金额` |
| F66-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |
| F66-4 | 跨科目 | `①表."权益法核算的长期股权投资收益"行 = 长期股权投资②明细表."投资损益"列合计` |

### 9. 净敞口套期收益

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F67-1 | 余额 | `报表.净敞口套期收益 = ①明细表.合计行.本期金额` |
| F67-2 | 余额 | `报表.净敞口套期收益(上期) = ①明细表.合计行.上期金额` |
| F67-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 10. 公允价值变动收益

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F68-1 | 余额 | `报表.公允价值变动收益 = ①明细表.合计行.本期金额` |
| F68-2 | 余额 | `报表.公允价值变动收益(上期) = ①明细表.合计行.上期金额` |
| F68-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |

### 11. 信用减值损失

> 模板中信用减值损失表仅有"坏账损失/债权投资减值损失/其他债权投资减值损失/其他/合计"5行，
> 不按应收账款/应收票据/其他应收款/长期应收款分别列示。
> 跨科目校验需根据实际文档中的行名动态匹配，若仅有"坏账损失"一行则汇总比较。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F69-1 | 余额 | `报表.信用减值损失 = ①明细表.合计行.本期金额` |
| F69-2 | 余额 | `报表.信用减值损失(上期) = ①明细表.合计行.上期金额` |
| F69-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |
| F69-4 | 跨科目 | `①表."坏账损失"行（或"应收账款坏账损失"行，按实际行名匹配） ≈ 应收账款⑤坏账准备变动表.(本期计提 - 收回或转回) + 应收票据⑤坏账准备变动表.(本期计提 - 收回或转回) + 其他应收款坏账准备变动表.(本期计提 - 收回或转回) + 长期应收款④坏账准备变动表.(本期计提 - 收回或转回)`（若①表按科目分行列示则逐行匹配，若仅有"坏账损失"一行则汇总比较） |
| F69-5 | 跨科目 | `①表."债权投资减值损失"行 ≈ 债权投资③减值准备变动表.(本期计提 - 转回)`（转销/核销和其他变动不计入损益；若变动表仅有"本期增加/减少"列则调用LLM辅助判断） |
| F69-6 | 跨科目 | `①表."其他债权投资减值损失"行 ≈ 其他债权投资③减值准备变动表.(本期计提 - 转回)`（转销/核销和其他变动不计入损益；若变动表仅有"本期增加/减少"列则调用LLM辅助判断） |

### 12. 资产减值损失

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F70-1 | 余额 | `报表.资产减值损失 = ①明细表.合计行.本期金额` |
| F70-2 | 余额 | `报表.资产减值损失(上期) = ①明细表.合计行.上期金额` |
| F70-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |
| F70-4 | 跨科目 | `①表."存货跌价损失"行 ≈ 存货②跌价准备变动表.(本期计提 - 转回)`（转销/核销和其他变动不计入损益；若变动表仅有"本期增加/减少"列则调用LLM辅助判断） |
| F70-5 | 跨科目 | `①表."固定资产减值损失"行 ≈ 固定资产①情况表.减值准备段."本期增加"合计行中的"计提"部分`（若"本期增加"列下有"计提"子列则直接取值；若无子列则"本期增加"整列可能全为计提，但也可能含企业合并等非损益项，需调用LLM辅助判断；同理"本期减少"中仅"转回"部分计入损益，"处置"等不计入） |
| F70-6 | 跨科目 | `①表."在建工程减值损失"行 ≈ 在建工程③减值准备变动表.本期计提金额`（若变动表有"计提/转回"列则直接取值；若仅有"本期增加/减少"列则调用LLM辅助判断） |
| F70-7 | 跨科目 | `①表."无形资产减值损失"行 ≈ 无形资产①情况表.减值准备段."本期增加"合计行中的"计提"部分`（同固定资产逻辑：优先识别"计提"子列，无子列时调用LLM辅助判断） |
| F70-8 | 跨科目 | `①表."商誉减值损失"行 ≈ 商誉②减值准备表.本期计提金额`（若变动表有"计提/转回"列则直接取值；若仅有"本期增加/减少"列则调用LLM辅助判断） |
| F70-9 | 跨科目 | `①表."合同资产减值损失"行 ≈ 合同资产②减值准备变动表.(计提 - 转回)`（合同资产减值准备表有"计提/转回/转销"列，直接取"计提-转回"） |
| F70-10 | 跨科目 | `①表."生产性生物资产减值损失"行 ≈ 生产性生物资产减值准备变动.本期计提金额`（仅当①表有该行时校验；若变动表仅有"本期增加/减少"列则调用LLM辅助判断） |
| F70-11 | 跨科目 | `①表."使用权资产减值损失"行 ≈ 使用权资产①情况表.减值准备段."本期增加"合计行中的"计提"部分`（仅当①表有该行时校验；同固定资产逻辑：优先识别"计提"子列，无子列时调用LLM辅助判断） |
| F70-12 | 跨科目 | `①表."长期股权投资减值损失"行 ≈ 长期股权投资减值准备变动.本期计提金额`（仅当①表有该行时校验；需调用LLM辅助判断） |

### 13. 资产处置收益

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F71-1 | 余额 | `报表.资产处置收益 = ①明细表.合计行.本期金额` |
| F71-2 | 余额 | `报表.资产处置收益(上期) = ①明细表.合计行.上期金额` |
| F71-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |

### 14. 营业外收入

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F72-1 | 余额 | `报表.营业外收入 = ①明细表.合计行.本期金额` |
| F72-2 | 余额 | `报表.营业外收入(上期) = ①明细表.合计行.上期金额` |
| F72-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |
| F72-4 | 二级明细 | `报表."其中：政府补助"本期 = ①明细表."与企业日常活动无关的政府补助"行.本期发生额`（仅当报表存在该二级明细行时校验） |
| F72-4a | 二级明细 | `报表."其中：政府补助"上期 = ①明细表."与企业日常活动无关的政府补助"行.上期发生额`（仅当报表存在该二级明细行时校验） |

### 15. 营业外支出

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F73-1 | 余额 | `报表.营业外支出 = ①明细表.合计行.本期金额` |
| F73-2 | 余额 | `报表.营业外支出(上期) = ①明细表.合计行.上期金额` |
| F73-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行` |

### 16. 所得税费用

**① 所得税费用明细表（当期/递延）**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F74-1 | 余额 | `报表.所得税费用 = ①明细表.合计行.本期金额` |
| F74-2 | 余额 | `报表.所得税费用(上期) = ①明细表.合计行.上期金额` |
| F74-3 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（当期所得税费用 + 递延所得税调整 + 其他 = 合计；每个数值列独立校验） |

**② 会计利润与所得税费用调整过程**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F74-4 | 纵向 | `②调整过程表: "按适定/适用税率计算的所得税费用" ≈ "利润总额" × 适用税率`（适用税率通常为25%，若不等则提示异常；适用税率可从行名中提取或由引擎按25%默认计算） |
| F74-5 | 交叉 | `②调整过程表."合计"行.本期发生额 = ①所得税费用明细表.合计行.本期金额`（②表最终所得税费用应等于①表合计） |
| F74-6 | 纵向 | `②调整过程表: "按适定/适用税率计算的所得税费用" + sum(各调整项: 子公司适用不同税率的影响 + 调整以前期间所得税的影响 + 非应税收入的影响 + 不可抵扣的成本费用和损失的影响 + … + 其他) = 合计`（引擎按实际行名动态识别各调整项） |

### 17. 其他综合收益

> 模板中其他综合收益表结构较复杂：
> - "一、以后不能重分类进损益的其他综合收益"下有多个子项
> - "二、将重分类进损益的其他综合收益"下每个子项有"减：前期计入其他综合收益当期转入损益"和"小计"行
> - "三、其他综合收益合计" = 一 + 二
> - 列结构：税前金额/所得税/税后净额（本期/上期各3列）
> 引擎需动态识别实际出现的子项。

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F75-1 | 余额 | `报表.其他综合收益(税后净额) = ①明细表."三、其他综合收益合计"行.本期税后净额` |
| F75-2 | 余额 | `报表.其他综合收益(上期) = ①明细表."三、其他综合收益合计"行.上期税后净额` |
| F75-3 | 交叉 | `①表."三、其他综合收益合计" = "一、以后不能重分类进损益的其他综合收益"合计 + "二、将重分类进损益的其他综合收益"合计`（税前/所得税/税后各列独立校验） |
| F75-4 | 纵向 | `①表每行: 税后净额 = 税前金额 - 所得税`（本期/上期各独立校验） |
| F75-5 | 其中项 | `①表"一"类: sum(各子项) = "一"类合计`（税前/所得税/税后各列独立校验） |
| F75-6 | 其中项 | `①表"二"类中每个子项: 子项金额 - "减：前期转入损益" = 小计`（每个子项独立校验；税前/所得税/税后各列独立） |
| F75-7 | 其中项 | `①表"二"类: sum(各子项小计) = "二"类合计`（税前/所得税/税后各列独立校验） |

---

## 三、现金流量表科目

### 0. 重要的投资活动有关的现金

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F76-0a | 其中项 | `收到的重要的投资活动有关的现金表: sum(合计行以外的所有明细行) = 合计行` |
| F76-0b | 其中项 | `支付的重要的投资活动有关的现金表: sum(合计行以外的所有明细行) = 合计行` |

### 1. 收到其他与经营活动有关的现金

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F76-1 | 余额 | `报表.收到其他与经营活动有关的现金 = ①明细表.合计行` |
| F76-2 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 2. 支付其他与经营活动有关的现金

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F77-1 | 余额 | `报表.支付其他与经营活动有关的现金 = ①明细表.合计行` |
| F77-2 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 3. 收到其他与投资活动有关的现金

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F78-1 | 余额 | `报表.收到其他与投资活动有关的现金 = ①明细表.合计行` |
| F78-2 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 4. 支付其他与投资活动有关的现金

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F79-1 | 余额 | `报表.支付其他与投资活动有关的现金 = ①明细表.合计行` |
| F79-2 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 5. 收到其他与筹资活动有关的现金

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F80-1 | 余额 | `报表.收到其他与筹资活动有关的现金 = ①明细表.合计行` |
| F80-2 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 6. 支付其他与筹资活动有关的现金

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F81-1 | 余额 | `报表.支付其他与筹资活动有关的现金 = ①明细表.合计行` |
| F81-2 | 其中项 | `①表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

### 7. 筹资活动产生的各项负债的变动

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F82-1 | 宽表 | `筹资活动负债变动表: 期初余额 + 现金流量净额 + 计提的利息 ± 公允价值变动 ± 其他 = 期末余额`（每行独立） |
| F82-2 | 其中项 | `筹资活动负债变动表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |

---

## 四、补充资料

### 1. 将净利润调节为经营活动现金流量

**① 将净利润调节为经营活动现金流量**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F83-1 | 跨科目 | `①补充资料表."净利润"行 = 利润表."净利润"行` |
| F83-2 | 跨科目 | `①表."资产减值准备"行 ≈ 资产减值损失①表.合计 + 信用减值损失①表.合计`（符号取绝对值） |
| F83-3 | 跨科目 | `①表."固定资产折旧、油气资产折耗、生产性生物资产折旧"行 ≈ 固定资产①累计折旧段.本期增加(计提)合计 + 投资性房地产(成本法)②累计折旧和累计摊销段."购置或计提"列中属于房屋建筑物折旧的部分`（若投资性房地产采用成本法且有累计折旧段，需将其房屋建筑物折旧计入；油气资产折耗、生产性生物资产折旧同理按实际存在的科目累加；若无法精确拆分则调用LLM辅助判断） |
| F83-4 | 跨科目 | `①表."无形资产摊销"行 ≈ 无形资产①累计摊销段.本期增加(计提)合计 + 投资性房地产(成本法)②累计折旧和累计摊销段."购置或计提"列中属于土地使用权摊销的部分`（若投资性房地产采用成本法且有土地使用权摊销，需将其计入；若无法精确拆分则调用LLM辅助判断） |
| F83-5 | 跨科目 | `①表."长期待摊费用摊销"行 ≈ 长期待摊费用①明细表."本期摊销"合计` |
| F83-5a | 跨科目 | `①表."使用权资产折旧"行 ≈ 使用权资产①累计折旧段.本期增加(计提)合计`（仅当①表有该行时校验） |
| F83-6 | 跨科目 | `①表."处置固定资产、无形资产和其他长期资产的损失"行 ≈ 资产处置收益①表.合计（符号相反）` |
| F83-7 | 跨科目 | `①表."财务费用"行 ≈ 财务费用①表."利息支出"行 - "利息收入"行` |
| F83-8 | 跨科目 | `①表."投资损失"行 ≈ 投资收益①表.合计（符号相反）` |
| F83-9 | 跨科目 | `①表."公允价值变动损失"行 ≈ 公允价值变动收益①表.合计（符号相反）` |
| F83-10 | 交叉 | `①表."经营活动产生的现金流量净额" = 现金流量表."经营活动产生的现金流量净额"` |

### 2. 现金及现金等价物净变动情况

**③ 现金及现金等价物净变动情况**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F84-1 | 交叉 | `③表."现金及现金等价物净增加额" = 现金流量表."五、现金及现金等价物净增加额"` |
| F84-2 | 交叉 | `③表."期末现金及现金等价物余额" - "期初现金及现金等价物余额" = "现金及现金等价物净增加额"` |
| F84-3 | 交叉 | `③表."期末现金及现金等价物余额" = ③现金和现金等价物构成表."三、期末现金及现金等价物余额"` |
| F84-4 | 交叉 | `③表."现金的期末余额" = ③现金和现金等价物构成表."一、现金"期末余额` |
| F84-5 | 纵向 | `③表: 现金的期末余额 - 现金的期初余额 + 现金等价物的期末余额 - 现金等价物的期初余额 = 现金及现金等价物净增加额` |

**④ 现金和现金等价物的构成**

| 编号 | 公式类型 | 校验公式 |
|------|----------|----------|
| F84-6 | 其中项 | `④现金和现金等价物构成表: "一、现金"行 = sum(其下"其中"子项)`（期末/期初各独立校验；子项包括库存现金/可随时用于支付的银行存款/可随时用于支付的其他货币资金等） |
| F84-7 | 纵向 | `④表: "三、期末现金及现金等价物余额" = "一、现金" + "二、现金等价物"`（期末/期初各独立校验） |
| F84-8 | 跨科目 | `④表."三、期末现金及现金等价物余额".期末 = ③表."现金的期末余额" + ③表."现金等价物的期末余额"` |

---

## 五、跨科目交叉校验汇总

> 以下汇总所有跨科目交叉校验公式，方便规则引擎统一调度。
> 各公式在上文对应科目中已列出，此处为索引。

| 编号 | 校验名称 | 涉及科目 | 公式引用 |
|------|----------|----------|----------|
| X-1 | 权益法投资损益三方核对 | 长期股权投资 ↔ 投资收益 | F17-6, F17-7, F66-4 |
| X-2 | 信用减值损失 vs 各科目坏账准备 | 信用减值损失 ↔ 应收账款/其他应收款/应收票据/长期应收款/债权投资/其他债权投资 | F69-4 ~ F69-6, F5-7, F8-9 |
| X-3 | 资产减值损失 vs 各科目减值准备 | 资产减值损失 ↔ 存货/固定资产/在建工程/无形资产/商誉/合同资产/生产性生物资产/使用权资产/长期股权投资 | F70-4 ~ F70-12, F9-6 |
| X-4 | 在建工程转固 | 在建工程 ↔ 固定资产 | F22-7, F21-8 |
| X-5 | 开发支出转无形资产 | 开发支出 ↔ 无形资产 | F27-4, F26-7 |
| X-6 | 未分配利润勾稽 | 未分配利润 ↔ 利润表/盈余公积 | F58-3, F58-4, F58-5 |
| X-7 | 盈余公积提取 vs 未分配利润 | 盈余公积 ↔ 未分配利润 | F57-4, F58-4 |
| X-8 | 所得税费用调整 | 所得税费用 ↔ 利润表利润总额 | F74-4 |
| X-9 | 补充资料 vs 利润表/现金流量表 | 补充资料 ↔ 利润表/固定资产/无形资产/长期待摊费用/投资收益/公允价值变动/资产处置/财务费用/现金流量表 | F83-1 ~ F83-10, F84-1 ~ F84-5 |
| X-10 | 货币资金 vs 现金等价物 | 货币资金 ↔ 补充资料 | F1-5, F1-6 |
| X-11 | 一年内到期的非流动资产 vs 各长期资产科目 | 一年内到期的非流动资产 ↔ 长期应收款/债权投资/其他债权投资 | F12-3 ~ F12-5a |
| X-12 | 一年内到期的非流动负债 vs 各长期负债科目 | 一年内到期的非流动负债 ↔ 长期借款/应付债券/长期应付款/租赁负债 | F43-7 ~ F43-10a |

---

## 六、报表二级明细 vs 附注明细校验

| 编号 | 报表科目 | 报表二级明细行 | 附注对应 | 校验公式 |
|------|----------|---------------|----------|----------|
| D-1 | 其他应收款 | 其中：应收利息 | 其他应收款汇总表."应收利息"行 | F8-3, F8-3a |
| D-2 | 其他应收款 | 其中：应收股利 | 其他应收款汇总表."应收股利"行 | F8-4, F8-4a |
| D-3 | 其他应付款 | 其中：应付利息 | 其他应付款①应付利息表 | F41-3 |
| D-4 | 其他应付款 | 其中：应付股利 | 其他应付款②应付股利表 | F41-4 |
| D-5 | 固定资产 | 固定资产清理 | 固定资产⑥固定资产清理表 | F21-7 |
| D-5a | 固定资产 | 其中：固定资产原价 | 固定资产①情况表."一、账面原值合计"行 | F21-13, F21-13a |
| D-5b | 固定资产 | 其中：累计折旧 | 固定资产①情况表."二、累计折旧合计"行 | F21-14, F21-14a |
| D-5c | 固定资产 | 其中：固定资产减值准备 | 固定资产①情况表."四、减值准备合计"行 | F21-15, F21-15a |
| D-6 | 一年内到期的非流动负债 | 一年内到期的长期借款 | 一年内到期的非流动负债汇总表 | F43-7, F43-7a |
| D-7 | 一年内到期的非流动负债 | 一年内到期的应付债券 | 一年内到期的非流动负债汇总表 | F43-8, F43-8a |
| D-8 | 一年内到期的非流动负债 | 一年内到期的长期应付款 | 一年内到期的非流动负债汇总表 | F43-9, F43-9a |
| | | | _D-9、D-10 已废弃（原一年内到期的非流动负债租赁负债/其他，已合并至F43跨科目校验）_ | |
| D-11 | 应付职工薪酬 | 其中：应付工资 | 应付职工薪酬②短期薪酬明细表."工资、奖金、津贴和补贴"行 | F39-11, F39-11a |
| D-12 | 应付职工薪酬 | 其中：应付福利费 | 应付职工薪酬②短期薪酬明细表."职工福利费"行 | F39-12, F39-12a |
| D-13 | 应交税费 | 其中：应交税金 | 应交税费①明细表中"税"类行之和（排除"费"类） | F40-4, F40-4a |
| D-14 | 盈余公积 | 其中：法定公积金 | 盈余公积①明细表."法定盈余公积金"行 | F57-6, F57-6a |
| D-15 | 盈余公积 | 其中：任意公积金 | 盈余公积①明细表."任意盈余公积金"行 | F57-7, F57-7a |
| D-16 | 盈余公积 | 其中：储备基金 | 盈余公积①明细表."储备基金"行 | F57-8, F57-8a |
| D-17 | 盈余公积 | 其中：企业发展基金 | 盈余公积①明细表."企业发展基金"行 | F57-9, F57-9a |
| D-18 | 盈余公积 | 其中：利润归还投资 | 盈余公积①明细表."利润归还投资"行 | F57-10, F57-10a |
| D-19 | 营业收入 | 其中：主营业务收入 | 营业收入①汇总表."主营业务"行.收入 | F59-15, F59-15a |
| D-20 | 营业成本 | 其中：主营业务成本 | 营业收入①汇总表."主营业务"行.成本 | F59-16, F59-16a |
| D-21 | 财务费用 | 其中：利息费用 | 财务费用①明细表."利息费用净额"行 | F64-5, F64-5a |
| D-22 | 财务费用 | 利息收入 | 财务费用①明细表."利息收入"行 | F64-6, F64-6a |
| D-23 | 营业外收入 | 其中：政府补助 | 营业外收入①明细表."与企业日常活动无关的政府补助"行 | F72-4, F72-4a |
| D-24 | 存货 | 其中：原材料 | 存货①分类表."原材料"行 | F9-2a, F9-2c |
| D-25 | 存货 | 其中：库存商品 | 存货①分类表."库存商品（产成品）"行 | F9-2b, F9-2d |

---

## 七、所有者权益变动表 vs 附注

| 编号 | 权益变动表行/列 | 附注科目 | 校验公式 |
|------|----------------|----------|----------|
| E-1 | 实收资本列.期初 | 实收资本①明细表.期初合计 | `权益变动表."实收资本"列期初 = 实收资本①.期初合计` |
| E-2 | 实收资本列.期末 | 实收资本①明细表.期末合计 | `权益变动表."实收资本"列期末 = 实收资本①.期末合计` |
| E-3 | 资本公积列.期初 | 资本公积①明细表.期初合计 | `权益变动表."资本公积"列期初 = 资本公积①.期初合计` |
| E-4 | 资本公积列.期末 | 资本公积①明细表.期末合计 | `权益变动表."资本公积"列期末 = 资本公积①.期末合计` |
| E-5 | 专项储备列.期初 | 专项储备①明细表.期初合计 | `权益变动表."专项储备"列期初 = 专项储备①.期初合计` |
| E-6 | 专项储备列.期末 | 专项储备①明细表.期末合计 | `权益变动表."专项储备"列期末 = 专项储备①.期末合计` |
| E-7 | 盈余公积列.期初 | 盈余公积①明细表.期初合计 | `权益变动表."盈余公积"列期初 = 盈余公积①.期初合计` |
| E-8 | 盈余公积列.期末 | 盈余公积①明细表.期末合计 | `权益变动表."盈余公积"列期末 = 盈余公积①.期末合计` |
| E-9 | 未分配利润列.期初 | 未分配利润①明细表.期初(调整后) | `权益变动表."未分配利润"列期初 = 未分配利润①.期初(调整后)` |
| E-10 | 未分配利润列.期末 | 未分配利润①明细表.期末 | `权益变动表."未分配利润"列期末 = 未分配利润①.期末` |
| E-11 | 其他权益工具列.期初 | 其他权益工具①明细表.期初合计 | `权益变动表."其他权益工具"列期初 = 其他权益工具①.期初合计`（仅当报表有该科目时校验） |
| E-12 | 其他权益工具列.期末 | 其他权益工具①明细表.期末合计 | `权益变动表."其他权益工具"列期末 = 其他权益工具①.期末合计` |
| E-13 | 其他综合收益列.本期变动 | 其他综合收益①明细表.合计行.本期税后净额 | `权益变动表."其他综合收益"列本期变动 = 其他综合收益①.合计行.本期税后净额` |

---

## 八、母公司财务报表附注校验

> 国企版母公司附注披露以下科目，校验规则与合并附注一致，仅口径改为母公司报表"公司"列。

| 编号 | 报表科目 | 校验公式 |
|------|----------|----------|
| M-1 | 应收账款 | `母公司报表.应收账款期末 = 母公司附注.应收账款合计行.期末` |
| M-2 | 其他应收款 | `母公司报表.其他应收款期末 = 母公司附注.其他应收款合计行.期末` |
| M-3 | 长期股权投资 | `母公司报表.长期股权投资期末 = 母公司附注.长期股权投资合计行.期末` |
| M-3a | 长期股权投资 | `母公司附注.长期股权投资总表: 小计行 = 对子公司投资 + 对合营企业投资 + 对联营企业投资`（期初/期末各独立校验） |
| M-3b | 长期股权投资 | `母公司附注.长期股权投资总表: 合计行 = 小计行 - 减值准备行`（期初/期末各独立校验） |
| M-3c | 长期股权投资 | `母公司附注.长期股权投资总表: 期初 + 本期增加 - 本期减少 = 期末`（每行独立） |
| M-3d | 长期股权投资 | `母公司附注.对子公司投资明细表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验） |
| M-3e | 长期股权投资 | `母公司附注.对联营合营企业投资明细表: sum(合计行以外的所有明细行) = 合计行`（每个数值列独立校验；"一、合营企业"和"二、联营企业"为分组标题行，不参与加总） |
| M-3f | 长期股权投资 | `母公司附注.对联营合营企业投资明细表: 期初余额 + 追加投资 - 减少投资 + 投资损益 + 其他综合收益 + 其他权益变动 - 现金股利 - 计提减值 ± 其他 = 期末余额`（每行独立） |
| M-4 | 营业收入 | `母公司报表.营业收入 = 母公司附注.营业收入合计行.本期` |
| M-5 | 营业成本 | `母公司报表.营业成本 = 母公司附注.营业成本合计行.本期` |
| M-6 | 投资收益 | `母公司报表.投资收益 = 母公司附注.投资收益合计行.本期` |
| M-7 | 现金流量表补充资料 | 同合并附注补充资料校验规则（F83-1 ~ F84-2），口径改为母公司 |

---

## 附：校验优先级

| 优先级 | 校验类型 | 风险等级 |
|--------|----------|----------|
| P0 | 报表 vs 附注余额核对（F*-1, F*-2） | HIGH |
| P0 | 宽表横向公式（合计行） | HIGH |
| P1 | 同科目表间交叉校验 | HIGH |
| P1 | 跨科目交叉校验（X-1 ~ X-9） | MEDIUM-HIGH |
| P2 | 报表二级明细 vs 附注明细（D-1 ~ D-25） | MEDIUM |
| P2 | 所有者权益变动表 vs 附注（E-1 ~ E-13） | MEDIUM |
| P2 | 其中项校验 | LOW-MEDIUM |
| P3 | 宽表横向公式（明细行） | LOW |
| P3 | 母公司附注校验（M-1 ~ M-7） | LOW |
