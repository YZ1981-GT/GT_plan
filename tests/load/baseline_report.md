# 基线压测报告 — 100 用户

## 测试概要

| 项目 | 值 |
|------|-----|
| 测试日期 | [待实测] |
| 测试工具 | Locust 2.x |
| 目标主机 | http://localhost:9980 |
| 并发用户数 | 100 |
| 加压速率 | 20 users/s |
| 持续时间 | 60s（稳定负载） |
| 测试脚本 | `tests/load/locustfile.py` |

## 压测场景

| 场景 | 权重 | 端点 |
|------|------|------|
| 登录 | 1 | POST /api/auth/login |
| 查询试算表 | 3 | GET /api/projects/{pid}/trial-balance |
| 查询底稿列表 | 2 | GET /api/projects/{pid}/workpapers |
| 编辑保存底稿 | 1 | GET + PUT /api/projects/{pid}/workpapers/{wid} |
| 穿透查询 | 2 | GET /api/projects/{pid}/drilldown?account_code=1001 |

---

## 响应时间（毫秒）

| 指标 | 全局 | 登录 | 查询试算表 | 查询底稿列表 | 编辑保存底稿 | 穿透查询 |
|------|------|------|-----------|-------------|-------------|---------|
| P50 | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |
| P95 | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |
| P99 | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |
| 最大值 | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |
| 平均值 | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |

## 吞吐量与错误率

| 指标 | 值 |
|------|-----|
| 总请求数 | [待实测] |
| 总失败数 | [待实测] |
| 吞吐量 (RPS) | [待实测] |
| 错误率 (%) | [待实测] |

### 各场景 RPS 分布

| 场景 | RPS | 错误数 | 错误率 |
|------|-----|--------|--------|
| 登录 | [待实测] | [待实测] | [待实测] |
| 查询试算表 | [待实测] | [待实测] | [待实测] |
| 查询底稿列表 | [待实测] | [待实测] | [待实测] |
| 编辑保存底稿 (GET) | [待实测] | [待实测] | [待实测] |
| 编辑保存底稿 (PUT) | [待实测] | [待实测] | [待实测] |
| 穿透查询 | [待实测] | [待实测] | [待实测] |

---

## 资源使用率

| 指标 | 测试前 | 测试中峰值 | 测试后 |
|------|--------|-----------|--------|
| CPU 使用率 (%) | [待实测] | [待实测] | [待实测] |
| 内存使用率 (MB) | [待实测] | [待实测] | [待实测] |
| DB 连接数 (active) | [待实测] | [待实测] | [待实测] |
| DB 连接数 (idle) | [待实测] | [待实测] | [待实测] |
| Redis 连接数 | [待实测] | [待实测] | [待实测] |

### 资源监控命令参考

```sql
-- PostgreSQL 连接数
SELECT state, count(*) FROM pg_stat_activity GROUP BY state;

-- 慢查询 Top 10
SELECT query, calls, mean_exec_time, total_exec_time
FROM pg_stat_statements ORDER BY mean_exec_time DESC LIMIT 10;
```

```bash
# CPU / 内存（Windows）
Get-Process -Name python | Select-Object CPU, WorkingSet64

# Redis 连接数
redis-cli info clients
```

---

## 错误分析

| 错误类型 | 次数 | 占比 | 说明 |
|----------|------|------|------|
| [待实测] | [待实测] | [待实测] | [待实测] |

---

## 基线结论

### 达标判定

| 指标 | 目标 | 实测值 | 达标 |
|------|------|--------|------|
| P95 响应时间 | ≤ 2000ms | [待实测] | [待实测] |
| P99 响应时间 | ≤ 5000ms | [待实测] | [待实测] |
| 错误率 | < 0.1% | [待实测] | [待实测] |
| RPS (100 用户) | 基线记录 | [待实测] | — |

### 瓶颈初步识别

| 瓶颈点 | 现象 | 严重程度 | 优化建议 |
|--------|------|----------|---------|
| [待实测] | [待实测] | [待实测] | [待实测] |

### 后续行动

- [ ] 基于基线数据确定优化方向
- [ ] 执行 3.4 瓶颈优化（连接池/索引/缓存）
- [ ] 执行 3.5 目标压测（6000 用户）

---

## 执行方式

```bash
# 自动化执行（推荐）
python tests/load/run_baseline.py

# 手动执行
locust -f tests/load/locustfile.py --host=http://localhost:9980 --headless -u 100 -r 20 --run-time 60s
```

---

*报告生成时间: [待实测]*
*执行人: [待填写]*
