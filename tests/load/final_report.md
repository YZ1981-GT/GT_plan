# 最终压测报告 — 6000 用户目标验证

## 测试概要

| 项目 | 值 |
|------|-----|
| 测试日期 | [待实测] |
| 测试工具 | Locust 2.x + StepLoadShape |
| 目标主机 | http://localhost:9980 |
| 最大并发用户数 | 6000 |
| 梯度阶段 | 100 → 500 → 1000 → 3000 → 6000 |
| 总运行时长 | ~15 分钟（含加压 + 稳定负载） |
| 测试脚本 | `tests/load/locustfile.py` (StepLoadShape) |
| 优化版本 | 3.4 优化后（连接池/索引/Redis 缓存） |

## 压测场景

| 场景 | 权重 | 端点 |
|------|------|------|
| 登录 | 1 | POST /api/auth/login |
| 查询试算表 | 3 | GET /api/projects/{pid}/trial-balance |
| 查询底稿列表 | 2 | GET /api/projects/{pid}/workpapers |
| 编辑保存底稿 | 1 | GET + PUT /api/projects/{pid}/workpapers/{wid} |
| 穿透查询 | 2 | GET /api/projects/{pid}/drilldown?account_code=1001 |

---

## 各阶段结果

### Stage 1 — 100 用户（基线对照）

| 指标 | 基线值 | 优化后 | 变化 |
|------|--------|--------|------|
| P50 (ms) | [待实测] | [待实测] | [待实测] |
| P95 (ms) | [待实测] | [待实测] | [待实测] |
| P99 (ms) | [待实测] | [待实测] | [待实测] |
| RPS | [待实测] | [待实测] | [待实测] |
| 错误率 (%) | [待实测] | [待实测] | [待实测] |
| 达标 (P95 ≤ 2s) | [待实测] | [待实测] | — |

### Stage 2 — 500 用户

| 指标 | 值 |
|------|-----|
| P50 (ms) | [待实测] |
| P95 (ms) | [待实测] |
| P99 (ms) | [待实测] |
| RPS | [待实测] |
| 错误率 (%) | [待实测] |
| 达标 (P95 ≤ 2s) | [待实测] |

### Stage 3 — 1000 用户

| 指标 | 值 |
|------|-----|
| P50 (ms) | [待实测] |
| P95 (ms) | [待实测] |
| P99 (ms) | [待实测] |
| RPS | [待实测] |
| 错误率 (%) | [待实测] |
| 达标 (P95 ≤ 2s) | [待实测] |

### Stage 4 — 3000 用户

| 指标 | 值 |
|------|-----|
| P50 (ms) | [待实测] |
| P95 (ms) | [待实测] |
| P99 (ms) | [待实测] |
| RPS | [待实测] |
| 错误率 (%) | [待实测] |
| 达标 (P95 ≤ 2s) | [待实测] |

### Stage 5 — 6000 用户（目标验证）

| 指标 | 值 |
|------|-----|
| P50 (ms) | [待实测] |
| P95 (ms) | [待实测] |
| P99 (ms) | [待实测] |
| RPS | [待实测] |
| 错误率 (%) | [待实测] |
| 达标 (P95 ≤ 2s) | [待实测] |

---

## 各场景响应时间（6000 用户阶段）

| 场景 | P50 | P95 | P99 | 最大值 | 平均值 | RPS |
|------|-----|-----|-----|--------|--------|-----|
| 登录 | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |
| 查询试算表 | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |
| 查询底稿列表 | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |
| 编辑保存底稿 (GET) | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |
| 编辑保存底稿 (PUT) | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |
| 穿透查询 | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] | [待实测] |

---

## 吞吐量与错误率汇总

| 阶段 | 用户数 | 总请求 | 总失败 | RPS | 错误率 |
|------|--------|--------|--------|-----|--------|
| Stage 1 | 100 | [待实测] | [待实测] | [待实测] | [待实测] |
| Stage 2 | 500 | [待实测] | [待实测] | [待实测] | [待实测] |
| Stage 3 | 1000 | [待实测] | [待实测] | [待实测] | [待实测] |
| Stage 4 | 3000 | [待实测] | [待实测] | [待实测] | [待实测] |
| Stage 5 | 6000 | [待实测] | [待实测] | [待实测] | [待实测] |

---

## 与基线对比（优化影响分析）

### 优化措施清单（Task 3.4）

| 优化项 | 措施 | 预期效果 |
|--------|------|---------|
| DB 连接池 | pool_size=50, max_overflow=100 | 消除连接等待 |
| TB 查询索引 | tb_balance 复合索引 (project_id, account_code) | 查询加速 50%+ |
| Redis 查询缓存 | TB 数据 60s TTL | 减少 DB 压力 |
| prefill 结果缓存 | Redis key=wp_id+tb_version | 避免重复计算 |
| SSE 连接限制 | 每用户 1 个 SSE + 心跳 30s | 减少长连接数 |

### 优化前后对比（100 用户基线）

| 指标 | 优化前（基线） | 优化后 | 改善幅度 |
|------|---------------|--------|---------|
| P50 (ms) | [待实测] | [待实测] | [待实测] |
| P95 (ms) | [待实测] | [待实测] | [待实测] |
| P99 (ms) | [待实测] | [待实测] | [待实测] |
| RPS | [待实测] | [待实测] | [待实测] |
| 错误率 | [待实测] | [待实测] | [待实测] |
| DB 连接峰值 | [待实测] | [待实测] | [待实测] |

### 扩展性分析

| 用户数 | P95 (ms) | RPS | 错误率 | 线性扩展比 |
|--------|----------|-----|--------|-----------|
| 100 | [待实测] | [待实测] | [待实测] | 1.0x (基准) |
| 500 | [待实测] | [待实测] | [待实测] | [待实测] |
| 1000 | [待实测] | [待实测] | [待实测] | [待实测] |
| 3000 | [待实测] | [待实测] | [待实测] | [待实测] |
| 6000 | [待实测] | [待实测] | [待实测] | [待实测] |

> 线性扩展比 = (该阶段 RPS) / (100 用户 RPS × 用户倍数)。理想值 = 1.0，< 0.5 表示严重瓶颈。

---

## 资源使用率（6000 用户峰值）

| 指标 | 测试前 | Stage 3 (1000) | Stage 5 (6000) | 安全阈值 |
|------|--------|---------------|----------------|---------|
| CPU 使用率 (%) | [待实测] | [待实测] | [待实测] | < 80% |
| 内存使用率 (MB) | [待实测] | [待实测] | [待实测] | < 4096 |
| DB 连接数 (active) | [待实测] | [待实测] | [待实测] | < 150 |
| DB 连接数 (idle) | [待实测] | [待实测] | [待实测] | — |
| Redis 连接数 | [待实测] | [待实测] | [待实测] | < 200 |
| Redis 内存 (MB) | [待实测] | [待实测] | [待实测] | < 512 |

---

## 错误分析

| 错误类型 | 次数 | 首次出现阶段 | 说明 |
|----------|------|-------------|------|
| [待实测] | [待实测] | [待实测] | [待实测] |

---

## 最终达标判定

### 核心指标

| 指标 | 目标 | 实测值 (6000 用户) | 达标 |
|------|------|-------------------|------|
| **P95 响应时间** | **≤ 2000ms** | **[待实测]** | **[待实测]** |
| **错误率** | **< 0.1%** | **[待实测]** | **[待实测]** |
| P99 响应时间 | ≤ 5000ms | [待实测] | [待实测] |
| RPS (6000 用户) | ≥ 500 | [待实测] | [待实测] |

### 综合判定

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   最终结果:  [待实测]  PASS / FAIL                       │
│                                                         │
│   P95 ≤ 2s:     [待实测]  ✅ / ❌                       │
│   错误率 < 0.1%: [待实测]  ✅ / ❌                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 判定规则

- **PASS**: P95 ≤ 2000ms **且** 错误率 < 0.1%（两项同时满足）
- **FAIL**: 任一指标未达标
- 如 FAIL → 继续优化 → 重测（循环直到 PASS）

---

## 后续行动

- [ ] 如 PASS：归档报告，进入 UAT 验收
- [ ] 如 FAIL：分析瓶颈 → 针对性优化 → 重新执行 `python tests/load/run_final.py`
- [ ] 长期：接入 CI/CD 定期回归压测（防止性能退化）

---

## 执行方式

```bash
# 自动化执行（推荐）— 运行完整 StepLoadShape 梯度加压
python tests/load/run_final.py

# 指定参数
python tests/load/run_final.py --host http://localhost:9980 --output tests/load/final_report.md

# 手动执行（使用 StepLoadShape 自动梯度加压）
locust -f tests/load/locustfile.py --host=http://localhost:9980 --headless
```

---

*报告生成时间: [待实测]*
*执行人: [待填写]*
*优化版本: Task 3.4 完成后*
