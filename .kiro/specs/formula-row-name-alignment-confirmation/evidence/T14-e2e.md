# Task 14 — 真栈实测

判定 JSON：`evidence/T14-e2e-verdict.json`（`"verdict": "PASS"`）

## 环境

- Docker：audit-postgres / audit-redis 均 healthy（`rtk docker ps` 实测）
- **V160 迁移由 MigrationRunner 正常路径应用**：后端启动日志确认，`schema_version` 已登记 160（`SELECT ... WHERE version='160'` 返回一行）
- 启动时抓到并修复一处 ORM↔DB 漂移：`workpaper_row_name_mapping_target.created_at` DB 有但 ORM 未声明 → 已补 ORM 列

## 端到端数据链路实测（连真库 audit_platform，真实项目数据）

样本：项目 `a7fc75e5-b67f-436d-a126-42423018b6ce` / 2025 的 `tb_aux_balance`（科目 1221 客户维度，真实账套明细）。
底稿行名故意用模板固定名「预收账款——某个模板写死的行名」（与账套明细名不一致）。

判据链（Requirement 5.4 / Property 5）逐步 PASS：

| 步骤 | 判据 | 实测结果 |
|---|---|---|
| ① 确认前 | 不一致行名 → unmatched，无值 | `match_state=unmatched`, `amount=null`, 20 真实候选, `has_pending=true` |
| ② 建映射 | 弹窗建立映射 → 落库 | 映射到「重庆医药（集团）股份有限公司」并 commit |
| ③ 确认后 | user_confirmed，金额 = 账套真实值（与只读 SQL 快照逐字对齐） | `user_confirmed`, `amount=238921879.36` == SQL 快照 `238921879.36`, `has_pending=false` |
| ④ 二次刷新 | 从服务端重读（不依赖前端缓存），不弹窗且出数一致 | 新服务实例 `reloaded_from_server=true`, `user_confirmed`, `amount=238921879.36`（同③）, `has_pending=false` |

**该行金额从确认前 `null`（无值）→ 确认后 `238921879.36`（账套真实值）** —— 即 Requirement 5.4
「不一致行名经弹窗确认后金额从无值变账套真实值」的核心判据，用真库真数据验证通过。

归一验证：候选 `dimension_key=1221|客户|重庆医药(集团)股份`（剥「有限公司」后缀 + 全角括号折半角），
按 `norm-v1` 规则工作。金额 238921879.36 是该 aux_name 在 1221 子科目下多行归集值（`aggregate_aux_by_name` 语义）。

测试后清理：删除本次写入的 1 行映射（独立 wp_code `TMP-E2E-RNA`，不污染真实底稿）。临时脚本已删。

## row_key 与目标 identity 未错位

STEP 4 用**新 RowNameMappingService 实例**强制从服务端重读，`row_key='line_prepaid_1'` 命中，
其目标 identity（account_code/aux_type/dimension_key/dataset_id）与写入一致 → 未错位。

## 多对一告警在下游传播

多对一告警（`duplicate_reference`）由 `resolve_amounts` 产出（Property 3，PBT `test_property_shared_target_all_flagged` 覆盖），
前端弹窗 `multiToOneRows` 计算 + 确认前 `ElMessageBox` 口径二次确认 + 全局刷新汇总告警条三处消费，
告警随重算结果传播（纯函数层已证，随 RowAmount 携带到下游审定表/附注渲染）。

## 浏览器 UI 层（如实记录）

- 数据链路（端点逻辑 + 真库读写 + 归一 + 逐字对齐 + 二次不弹窗）已用真库真数据端到端 PASS。
- 纯浏览器 DOM 交互（点「刷新取数」按钮 → 弹窗 UI → 勾选 → 确认）受两项限制未做完整 Playwright：
  ① 平台无可用登录态（前置 F-SHELL spec 的 Task 17 亦因此未勾选）；
  ② 「刷新取数」按钮依赖具体底稿子组件暴露 `getRowNameAlignmentRows()`（逐底稿增强，样板底稿子组件接线尚未落到某一具体 SFC）。
  前端组件行为已由 vitest 6 例 + 隔离 2 例覆盖（弹窗打开/多对一/确认调端点带 base_version/取消零写入）。
- **结论**：spec 核心价值链（不一致 → 确认 → 真实值 → 复用不弹窗）已在真栈数据层证实；
  纯 DOM 端到端待平台登录态可用 + 样板底稿子组件接线后补做，此限制如实登记不假绿。
