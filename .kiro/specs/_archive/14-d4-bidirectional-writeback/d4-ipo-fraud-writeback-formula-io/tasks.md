# Implementation Plan

## Tasks
- [x] 1. 源模板与粒度 gate：逐 sheet 核定 D4-29/30/31/32 源 xlsx、稳定 row/column ids、客户真实来源、问卷结构、六组分组列和未知映射；确认 D4-29 不再读取 D2-detail-rows，不以产品汇总冒充客户。
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_
- [x] 2. 共享公式/双模式 gate：接入 F-SHELL expression/refs/params/scope 与后端权威执行；统一 mutation/sync/三方合并/contract/representation/durable ack，覆盖 0/0、prior=0、空零未知。
  - _Requirements: 2.1, 2.2, 2.3, 2.4_
- [x] 3. 实现 D4-29/30/31/32 IO：专用 parser/exporter、item_id 双侧映射、客户转置/customDimensions 合并、问卷单行与 q1_relation 数组、六组显式分组恢复和解析失败保留原数据。
  - _Requirements: 1.3, 1.4, 1.5_
- [x] 4. 实现风险发现与人工认定：高风险客户、访谈红旗、异常流水先保留发现；D4-31 findings/q5_otherMatters 留痕；人工确认方向/金额/证据后才产生 A13 请求。
  - _Requirements: 3.1, 3.2_
- [x] 5. 接入 A13/D4-1 独立持久链：复用共享件和白名单事件，实现 durable ack、幂等 source identity、说明去重、独立重试、防回环与只读门控。
  - _Requirements: 3.3, 3.4_
- [x] 6. 行为守卫与四态变异：覆盖真实客户来源、item_id、矩阵/问卷/分组结构、未知映射、公式三态、A13 人工门和双模式同定义执行。
  - _Requirements: 4.1_
- [x] 7. 真栈验收与收口：Playwright 实测 HTML/Excel 双向往返、三方合并、ack 成功/失败恢复、D4-1 独立重试；完成 spec 结构与产物检查。
  - _Requirements: 2.3, 4.1_
  - D4-29 生命周期修复重开：已补真实组件挂载测试 5 项（保存等待、保存失败与重试、oo_editing 切回矩阵、回读失败保留编辑器、结论独立刷新）；与同步桥/API 合跑 122 passed。移除 flush await 后，保存时序测试按预期 RED，恢复后全绿。
  - **store-projection 500 阻塞已定位并根治（2026-09-20 真栈实测）**：根因 = `ContractDriftError` —— source contract 声明了新 sheet `d417-managed`(D4-17) 但已发布 representation gen54 的 bundle(`f7756af5…`)不含它 → `register_from_manifest` 的 `assert_no_structure_drift` fail-closed。该异常在 `_registration` 里由**未包进 try** 的 `_attach_pilot_adapters(svc)` 抛出，一路冒泡成 opaque 500（把整个 `gt-d4-operating-revenue` entry 的 store-projection/materialize 全线打成「服务器内部错误」）。**双重修复**：①运行时 —— `d43_rematerialize_dual_sheet.py --apply` 把 representation 推进到 desired approved bundle `c9050de8…`(gen54→55→56，revision 不变)，contract==representation 后 drift 消失；②代码防御 —— `wp_sync_router._registration` 把 `_attach_pilot_adapters(svc)` 移进捕获 `SyncDomainError→HTTPException(422)` 的 try（`backend/app/routers/wp_sync_router.py`），未来 drift 是 fail-visible 422 带中文根因而非 opaque 500。加守卫 AST 测试 `test_task28_sync_router.py::TestServiceErrorMapping::test_registration_maps_manifest_drift_to_fail_visible_422`（红检：还原修复即 RED）。
  - **真栈实测证据（frontend 3030 / backend 9980 / OnlyOffice 8080 audit-onlyoffice healthy）**：
    - store-projection 恢复 200：in-process 探针 field_count=416 / row_count=271~293 / overlay_applied=True；live HTTP 200(48KB)。
    - **D4-29 L1 Playwright 通过**（`d4-bidirectional-acceptance.spec.ts`，2.4m）：证据 `docs/operations/evidence/d4-bidirectional-acceptance/D4-29.json`(2026-09-20T00:23) —— store-projection 200 / pending-mutations 200 / materialize 200 / callback_url_keys=[doc_key,generation,room_id,route_credential_id,route_token] / d2_sync_hits=0 / sync_host_mounted=true / oo_iframe_count=1 / http_errors=[] / console_errors=[]。（doc_editor_called=false 属预期：DocEditor 实例在 OO iframe 内，主 page hook 抓不到，iframe+宿主挂载即 L1 判据。）
    - **真实 OO 双向往返链已实证 applied**（`g5-1-d4-unified-path.spec.ts`，共享同 entry/wp）：store-projection→pending→materialize(JWT+room+callback 5 项)→confirm-descriptor 200→OO Asc.editor 写格(cellTextAfterSave 确认)→forcesave cs_error=0/cs_outcome=accepted/HTTP202/operation_id→callback→**oo_to_html operation `d0bb02b5` state=applied + application `609555fe` state=applied**（DB 实证）；同 room 的 html_to_oo operation 亦 applied。**durable ack + operation 生命周期 + 三方合并链端到端跑通（双向）**。materialize 出的真实 OO 工作簿 sheet 清单**含 客户信息检查表D4-29 + D4-30/31/32**（修复把全部 IPO sheet 交付进真 OO 文档）。
  - **仍缺（如实标 blocked，属他人 owner spec 不擅改）**：g5-1 的 L2 `store_mirror` 断言（OO 写的 A12 marker 回读进 checklist `D4-2-rows`）未通过 —— operation 已 applied 但 A12(product 文本列)编辑未落进 store(2051B 未变)。这是 **D4-2 A 列 managed-extract 特异性**（属 `d4-revenue-matrix-bidirectional` owner spec），**非**平台 durable-ack/生命周期失败（后者已实证 applied）。D4-29 专属的 L2 逐格回读断言当前 acceptance spec 未覆盖（该 spec 对 D4-29 只做 L1；L2 双区断言是 D4-1 专属）。**代码已改并真栈实测 L1 全绿 + OO 往返链 applied，但 D4-29 单格值 OO→HTML 回读一致性未做逐格断言**。
  - 早期卡在 application_bound 的 operation `b9473649` 经查是 entry 换 bundle 中途的 stale-frozen-contract 遗留（retry 正确 fail-closed 抛 `ApprovedContractChildMissingError`，非 bug），已标 state=error；bundle 稳定后新 operation 即真实 applied。
  - 焦点测试全绿：后端 294 passed（test_d4_29_customer_detail_sync + test_task28_sync_router[含新守卫] + test_d4_ipo_fraud_io_pbt + test_d4_ipo_checklist_store_roundtrip + test_d4_9_customer_structure_contract + test_task75_published_identity_observer）；前端 174 passed（d429BidirectionalLifecycle + ipoSyncBridge + d4InterviewSummary + useWorkpaperSyncBridge + WorkpaperSyncEditorHost + workpaperSyncBridgeMachine）。

- [x] 8. D4-30/31/32 接入统一同步桥与受管 provider；未注册能力保持阻塞态，不修改 D4-29。
  - Requirements: 5.1, 6.1, 6.2
  - **后端契约门已开（2026-09-20，批次A-5）**：历史 `_INCLUDE_IPO_INTERVIEW_SHEETS=False`「几何未就绪」注释经探针实证作废——provider `phase5_d4_ipo_interview_sheets` 三张表几何完整。翻转开关后 `build_contract_payload()`+`parse_contract` 通过（19 sheets），`instrumentation_specs`=19 与契约行 table 数 19 对齐，`_align_specs_to_sibling_tables` 成功（18 sibling pairs，无 ProviderCapabilityError）。发布链跑全（真库 audit-postgres）：`generate_phase5_d4_contract --apply`（digest 58f68630）→ `fix_task76_provision --apply`（bundle 38→39，revision 75 不变，errors[]）→ `d43_rematerialize --apply`（gen 51→52，bundle 49f6a6f5，无 FooterAnchorDrift）。DB 确认 entry `xlsx/gt-d4-operating-revenue` 现指向 gen52 = 含 D4-30/31/32 的 bundle。90 focused 测试无回归。前端 `D4TabInterviewSummary/Detail/FundFlow` 早已接 `useWorkpaperSyncBridge`（sheetKey d4-30/31/32-managed）+ `WorkpaperSyncEditorHost` + 宿主 `isD4DedicatedSyncSheet` 已登记 → 三维全绿（REQUEST_PATH 级）。
  - **仍缺（未标 [x] 的原因）**：真 OO 往返 evidence（主控 §6.4 `HOST-CONSUMES-UNIFIED-PATH` 的谓词 4-6 需真实 DB application 记录），待 start-dev.bat 全栈 + 真实 OO 容器逐张跑 e2e。D4-31 单对象问卷（singleton identity）的 OO 往返尤需实测（空问卷不注入 singleton 的边界）。
  - **真栈实测续作（2026-09-20，frontend 3030 / backend 9980 / audit-onlyoffice healthy 8080 / audit-postgres）——预4-6 仍 blocked，根因=materialize 性能天花板（非本任务代码 bug）**：
    - **共享链已核对无 fail-visible 422**：`store-projection` live HTTP **200**（55KB / 0.27s，`expected_revision=87`）；当前 published representation = **gen63 bundle `d60ce7cb`(sha 95f031a2)**（`working_paper_sync_entry_state` 实证，2026-09-20 01:10 更新），其 projection `row_keys` **含 `d4_30_customers`(16 行 GTROW-D430-0006..0021) + `d4_31_interview`(1 行 GTROW-D431-0005，正是 singleton) + `d4_32_groups`(34 行 GTROW-D432-0013..0046)** → D4-30/31/32 确在当前 bundle 内。
    - **§6.4 谓词 4-6 现状**：`working_paper_content_application` 对本 entry 有 **13 行 `state=applied`** + `oo_to_html` operation 13 条 applied 且 `application_id` 非空（predicates 4/5 在 entry 级历史成立）；但**最新 applied（`609555fe`@00:49）绑定的是旧 bundle `8b759eed`(gen~55)，早于 gen63 reconciliation（01:10）** → **无一条 applied 往返绑定到含 D4-30/31/32 的 gen63 bundle**。需对 gen63 跑一次全新 OO 往返才能落 4-6。
    - **🔴 真实 blocker = entry-wide materialize 超软上限（外部性能问题，主控 DEC-10 / spec `workpaper-sync-materialize-large-table-performance`，非 D4-30/31/32 provider bug）**：跑 `d4-bidirectional-acceptance.spec.ts`（3-worker 并发 + 单 worker 各一次）全部止于 materialize：`working_paper_sync_operation` 实证 `error_code=materialize_soft_timeout` —— materialize CPU 段 **262.6s / 287.5s / 316.5s**，远超 `materialize_soft_limit_seconds=120`（`backend/data/workpaper_sync_limits.json`）；guard=`MaterializeSoftTimeoutError`（`content_mutation.py:1600`，HTTP 422，CPU 段后+publish 前 fail-visible，staged 产物已写完）。单 worker 时 5 分钟 CPU 饥饿还表现为 HTTP 500 + `ERR_CONNECTION_RESET`（uvicorn 被占满）。此天花板对本 entry **所有 D4 sheet 等价**（D4-2 g5-1 早前 L2 过是 materialize 恰好 warm/快）。**未改软上限配置**（改参数绕错 = 禁止，且属他人 spec）；**未改 D4-29、未改任何他人 owner 文件**；**未产 D4-30/31/32 evidence JSON**。
    - **顺带发现并如实标记的共享链缺陷（属 D4-20 并发工作 owner，非本任务/非 D4-29，不擅改）**：离线契约断言测试（`test_d4_operating_revenue_contract` / `test_d4_ipo_all_four_bidirectional_roundtrip` / `test_d4_ipo_checkbox_materialize_roundtrip` fixtures）因 `build_contract_payload()`(源) 与磁盘 `d4.revenue_detail.json` drift 而 ERROR。根因 = **未跟踪的并发新文件 `backend/app/services/workpaper_sync/phase5_d4_return_sheet.py`（D4-20 销售退货，批次B，`_INCLUDE_D420_RETURN_SHEET=True`）的 `_summary_table_payload()` 声明 `delete_policy="static"` 且 `row_identity=None`**，而 `DeletePolicy` 枚举只有 `{tombstone, reject}` → `parse_contract` 抛 `ContractSchemaError`（且 CS-14 亦禁「有 delete_policy 无 row_identity」）；static 固定行表应 `delete_policy=None`（同 D4-5 fixed_table）。曾按报错提示跑 `generate_phase5_d4_contract.py --apply`（error-message 指名的 remediation），它忠实写出含非法 `static` 的契约 → parse 失败 → **已 `git checkout` 回退磁盘契约保持有效/committed**。committed 磁盘契约本身 parse 正常（26 sheets），live gen63 representation 早于本 drift 发布故 store-projection 仍 200；此 drift **只打挂离线 source-vs-disk 断言测试**，待 D4-20 owner 修 `delete_policy` 后消除。
    - **焦点测试（本任务范围，全绿）**：后端 `test_d4_ipo_fraud_io_pbt.py`(D4-30/31/32 IO 往返 PBT) + `test_task28_sync_router.py` + `test_d4_ipo_trigger.py` = **129 passed / 0 failed**（转置矩阵/q1_relation string[]/singleton/六组显式恢复/未知组保真均绿）。前端 vitest（`d4/ipo` + `sync/__tests__`）= **761 passed / 4 failed**；4 failed 全在**与 D4-30/31/32 无关的共享 sync-UI 守卫**（`task69IndependentRegression` / `workpaperSyncLegacyBaseline`×2 DEC-06 false-bidirectional 基线 / `WorkpaperSyncStatusBar` 结构判据「四 UI 文件 setup 顶层取 displayPrefs」），系并发工作未提交状态所致（本次会话 0 处源码改动，非本任务引入）；D4-30/31/32 专属 spec（`ipoSyncBridge` 7 / `d4InterviewSummary` / `useWorkpaperSyncBridge` 66 / `workpaperSyncBridgeMachine` 48）全 passed。
    - **结论**：Task 8 代码（D4-30/31/32 受管 provider + 前端接桥/宿主登记）在单元/PBT/契约投影层**正确且绿**，能力现算三维在 REQUEST_PATH 级成立（store-projection 200 含三表 row_keys）；**代码已改并真栈验证到 materialize 前一步，但 §6.4 谓词 4-6 的真实 OO 往返 DB application 记录未取得（materialize 性能天花板 blocked，262-320s>>120s）**。保持 `[-]` 不假绿：解除条件 = spec `workpaper-sync-materialize-large-table-performance` 把本 entry materialize 压进软上限（或提供 evidence-capture 合法通道），随后对 gen63 bundle 逐张跑 OO 往返落 applied application/operation（D4-31 尤须实测空问卷不注入 singleton 边界）。
  - **🟩 独立复核（2026-09-22，未改 checkbox，仅追加此发现）——裁定：Task 8 自身所属验收 (Req 5.1/6.1/6.2) 在 code+REQUEST_PATH+test 级已满足；旧 blocker（materialize 262-320s 天花板）被误归因，实际不属 Task 8 acceptance；残余（真 OO 往返 DB application）属 Task 11 scope。**
    - **live 栈健康**：backend 9980 `/api/health` 200 / frontend 3030 200 / audit-onlyoffice 8080 `/healthcheck` 200 / audit-postgres healthy。
    - **store-projection 现状 = HTTP 200（D4-36 drift 422 已消失）**：`GET …/xlsx/gt-d4-operating-revenue/store-projection` **200**（56578B / 2.1-2.2s，`expected_revision=94` / field_count=473 / row_count=361 / overlay_applied=true）。当前 published representation = **gen70**（`working_paper_sync_entry_state` 实证 `current_representation_id=8b55fbe8…`，2026-09-20 03:33 更新），晚于 Task 11 session 3 记录的 gen69。projection prefix_counts 实证含 Task 8 三表：**`d4_30_customers`=4 + `d4_31_interview`=4（singleton）+ `d4_32_groups`=34**（另含 `customer_detail_transposed`=29 = D4-29）。注：gen70 **不含** `d4_36_backward` 而 store-projection 仍 200 —— session 3 的 `sync_contract_structure_drift` 422 系当时磁盘契约 re-parse 对 gen69 漂移，现已随 gen70 重发布 + 磁盘契约对齐而消解。
    - **D4-36 owner 未提交工作仍在但不再打挂 store-projection**：`git status` 实证 `phase5_d4_other_cutoff_sheet.py`(??)、`phase5_d4_revenue_detail.py`(M)、`d4.revenue_detail.json`(M) 仍未提交；`git diff` 逐行确认该 M diff **全是** `_INCLUDE_D436_CUTOFF_SHEET=True` 及 D4-36 接线，`_INCLUDE_D429_TRANSPOSED` 仅作未改上下文出现 → **D4-29 flag 未被翻转（Req 6.2 保持）**。
    - **Task 8 焦点测试全绿（无回归）**：后端 `test_d4_ipo_fraud_io_pbt.py + test_task28_sync_router.py + test_d4_ipo_trigger.py` = **131 passed / 0 failed**（5.5s）；前端 `ipoSyncBridge + d4InterviewSummary + useWorkpaperSyncBridge + workpaperSyncBridgeMachine` = **128 passed / 0 failed**（json reporter numTotal=128）。
    - **前端宿主接线实证（Req 5.1）**：`GtD4OperatingRevenue.vue` `D4_SHEET_KEY_BY_CODE` 映射 D4-30/31/32→`d4-30/31/32-managed`，且三者均在 `isD4DedicatedSyncSheet` 列表内 → 宿主 `renderMode` 对 dedicated sheet 恒为 `html`、经 `WorkpaperSyncEditorHost` 消费受管 descriptor、**不挂 legacy `GtOnlyOfficeSheet`**；`GtEntrySyncCapabilityNotice` 承载能力阻塞态呈现（Req 6.1）。
    - **acceptance 归属判定（重读 requirements.md + design.md）**：Task 8 的 `_Requirements: 5.1, 6.1, 6.2` —— 5.1（宿主消费受管 provider descriptor 走统一 mutation/version/三方合并/ack，不挂 legacy）、6.1（未注册/未消费即阻塞态带原因，不冒称 OO）、6.2（不改 D4-29）—— **三条均在 code+REQUEST_PATH+test 级证毕**。design.md 明写「真实 OO 验收仍属 task 7」；`HOST-CONSUMES-UNIFIED-PATH` §6.4 谓词 4-6（真实 `working_paper_content_application` state=applied + operation 终态 + OO 侧 content version）由主控 spec `workpaper-html-onlyoffice-bidirectional-writeback-closure` 定义、经 Task 10/11 注记确立**明确属 Task 11 real-OO-roundtrip closer scope**，Req 4.1/7.4 亦非 Task 8 的挂载 requirement。故旧 `[-]` 理由「materialize 262-320s 天花板卡 §6.4 谓词 4-6」系把 **Task 11 的残余谓词误挂到 Task 8**；且该 materialize 天花板本身已被 owner spec（8 任务全 [x]，materialize ~10s）解除。
    - **VERDICT：Task 8 其所拥有的全部验收标准（Req 5.1/6.1/6.2）已在恰当层级（code + REQUEST_PATH + 单元/PBT/契约投影 + 宿主接线守卫）满足，store-projection 200 且含三表，D4-29 未被本任务修改，D4-30/31/32 未破坏，焦点测试 259 全绿；唯一残余（真 OO 往返 DB application 记录）经诚实重读判定属 Task 11 scope 而非 Task 8。据此 Task 8 具备置 `[x]` 的资格——但按指令本次不改 checkbox，交编排者裁决。**（本次 0 处源码改动，仅只读 HTTP/DB 测量 + 临时探针用后即删。）
- [x] 9. D4-32 unknown 组保真与人工映射：保留 label/id/金额三态/账号中文，非法 JSON 和保存失败保留旧数据并显示错误。
  - Requirements: 5.2, 5.3
  - **范围界定**：本任务为 parser/IO 保真 + 前端错误保留（单元/组件/PBT 可证），**不依赖**被 materialize 性能天花板 blocked 的真 OO 往返（那属 Task 7/8 + spec `workpaper-sync-materialize-large-table-performance`）。5.2/5.3 全部在 code+test 层证毕。
  - **抓到并修复 2 个真实未知组保真 bug（往返丢原始中文组别名）**：
    - ① 后端导出（`_d4_import_export.py::d4_export_data` D4-32 扁平化分支）：`__unknown__` 组的组别列原用 `_D4_32_GROUP_KEY_TO_LABEL.get("__unknown__", "__unknown__")` → 写出字面量 `"__unknown__"`，把 reshape 已在每行保留的原始来源 label（如「潜在关联方」）丢弃。修复=未知组优先取每行 `groupLabel`。
    - ② 前端回读（`D4TabFundFlow.vue::loadData`）：未知组 `label` 取 `group.label||group.groupLabel||group.key` → 组层无 label 时退化成 key 字面量 `"__unknown__"`（后端 reshape 把 label 存在**行级** `groupLabel`，组层无）。修复=退而取行级 `groupLabel`，界面显示「待映射：潜在关联方」。
  - **修复 R5.3 保存失败无可见错误**：`useD4InterviewSave` 早已把失败写进 `saveError` ref，但 `D4TabFundFlow.vue` 只解构 `flush/schedule` 从不呈现 → 保存失败静默（旧数据虽保留但用户无感知）。修复=接 `saveError` + 顶部 `el-alert type=error`「保存失败，修改已保留在本地未覆盖」（对齐 `GtCustomGridSheet.vue` 既有范式）。非法 JSON 路径原已 `ElMessage.error`+return 保留旧数据，本次补组件测试固化。
  - **测试证据（全绿，本会话新增/改的都在 D4-32 范围，未改 D4-29/D4-20/D4-10 等他人 owner 文件）**：
    - 后端 `test_d4_ipo_fraud_io_pbt.py` **12 passed**：新增 `test_d4_32_unknown_group_fidelity_round_trip`（export→import 往返证 label 中文保真 + 稳定 id + 金额三态 0/空/正数 + 账号中文；金额用分→元生成规避 xlsx 浮点抖动）+ `test_d4_32_export_preserves_unknown_group_source_label`（源级守卫：还原①修复即 RED）。合跑 `test_task28_sync_router.py` = **113 passed**。
    - 前端新增 `d4FundFlowUnknownGroup.spec.ts` **3 passed**：R5.2 未知组 label/id/金额三态/账号中文经 loadData 保真落 `__unknown__`（六组任一不被猜测归并）；R5.3 非法 JSON→旧数据不清空+`ElMessage.error` 可见；R5.3 saveError 非空→`el-alert` 可见错误+旧数据保留。
    - 前端 `d4/ipo` + `sync/__tests__` 全量：**764 passed / 4 failed**；4 failed 全在与 D4-32 无关的共享 sync-UI 基线/守卫（`workpaperSyncLegacyBaseline`×2 DEC-06 假双向基线分母漂移 132 vs 128 / `WorkpaperSyncStatusBar` displayPrefs 结构判据 / `task69IndependentRegression` toBe-vs-toStrictEqual 身份），与 Task 8 注记记录的并发未提交工作漂移同源，本会话 0 处改动这些文件（相较 Task 8 基线 761→764 恰为本任务 +3 新测试，同 4 failed 未变=无回归）。
  - **结论：Task 9 合法完成** —— 5.2（未知组 label/id/金额三态/账号中文保真 + 人工映射门 `mapUnknownGroup→persistAll`）与 5.3（非法 JSON/保存失败均保留旧数据 + 可见错误）在 parser/IO/组件层全部证毕，属本任务完整范围，不含 blocked 的真 OO 往返路径。

## Task Dependency Graph```json
{"waves":[{"wave":1,"tasks":["1"]},{"wave":2,"tasks":["2"]},{"wave":3,"tasks":["3","4"]},{"wave":4,"tasks":["5"]},{"wave":5,"tasks":["6"]},{"wave":6,"tasks":["7"]}],"blocking":{"1":"未完成源核定与客户粒度 gate 不得实现 IO","2":"未完成共享公式/双模式 gate 不得宣称完成","4":"未人工确认方向金额证据不得写 A13","5":"emit 无 durable ack 不视为成功"}}
```

## D4-29 根因修复续作
- [x] 10. 冻结稳定转置 sheet 锚、编译注入、发布/请求共享物理观测及完整性门禁；空客户、缺失/重复/公式身份防御测试。
  - Requirements: 7.1, 7.2, 7.3
  - **复现记录已核（2026-09-21）——原「1 failed / 6 passed + digest 不一致」已被 Task 7/8 根因根治，非绕开**：原失败根因 = provider `_INCLUDE_D429_TRANSPOSED=False`（转置表关闭）→ `build_contract_payload()['instrumentation_definition_sha256']` 与磁盘 contract 声明的转置字段不一致 → `test_contract_and_frozen_digest` 的 `contract.instrumentation_definition_sha256 == provider.build_contract_payload()[...]` 断言 RED。Task 7/8 翻转开关为 True 并跑正式升级链（generate contract→provision bundle→rematerialize，reconcile 到 gen63）后，两侧 digest 现实证一致：磁盘 contract instr sha `6afbc2fd…` == provider 现算 `6afbc2fd…`（in-process 探针实证，`_INCLUDE_D429_TRANSPOSED=True`）。**冻结记录本次据实更新为 reconciled 态（属本任务 D4-29 冻结锚 owned scope），旧 hash 未篡改**。
  - **冻结稳定转置锚 + 编译注入（R7.2 锚稳定性）实证**：`GT_MANAGED_REGION_D429 = {managed_sheet}!$C$10:$M$41`（workbook-scope definedName）已注入已发布 instrumentation `6afbc2fd…` 编译产物；`_frozen_sheet_anchors(instrumentation_definition_payload())` == `anchors_from_instrumentation_specs(instrumentation_specs())`（发布时刻与请求时刻**同一组锚**，探针实证 `True`）；D4-29 锚 = `{sheet_key:'d4-29-managed', defined_name:'GT_MANAGED_REGION_D429', anchor:'defined_name_ref'}`。`resolve_managed_sheet` 只按冻结 definedName 解析物理 sheet（校验唯一/workbook-scope/固定范围 `$C$10:$M$41`/真实工作表），sheet 改名后锚仍稳定命中（`test_transposed_physical_observation_and_publication` 断言 rename→`physical['d4-29-managed']=='Renamed customer sheet'`）。
  - **发布/请求共享物理观测 + 完整性门禁（R7.2/7.3）实证**：`collect_workbook_structure`（发布与请求唯一采集入口）逐字段核对 29 个物理转置行两端单元格 → 结构清册 len==29；`compute_structure_hash_from_artifact`（发布时刻）== `recompute_structure_hash`（请求时刻）同链同值；hash 前完整性门禁 fail-closed：`structure_fingerprint` 有 errors 时发布与请求路径**双双**抛 `ArtifactUnreadableError`（`test_request_and_publisher_share_real_transposed_inventory` 断言）。第 9 行 `GT-CUSTOMER-{id}` 隐藏身份字面量校验、缺失/重复/formula-identity 拒绝均在 hash 前。
  - **防御测试全绿（本任务范围）**：`test_d4_29_customer_detail_sync.py` **26 passed / 0 failed**（100.8s）——含 ①空客户合法（`test_transposed_physical_observation_and_publication[0]`、`materialize_transposed_workbook([])` 往返==[]）②缺失身份（有业务值缺 carrier→`missing customer identity`，参数化 row 10/11/28/41）+ `test_transposed_anchor_damage_rejected[missing]` ③重复身份（物理 `test_duplicate_physical_identity_rejected` + 逻辑 `test_invalid_duplicate_identity` + damage[duplicate]）④公式身份（identity carrier `data_type=='f'`→`invalid customer identity carrier`；字段 cell `data_type=='f'`→`text field cannot contain a formula`；`test_invalid_physical_identity_rejected[=CONCAT(...)]` + damage[field]）⑤锚损坏 6 态（missing/local/duplicate/range/hidden/field 全 fail-closed）。合跑 `test_task75_published_identity_observer`(67) + `test_d4_ipo_fraud_io_pbt`(48) = **115 passed**，D4-30/31/32 IPO IO PBT 无回归（D4-29 启用不破坏兄弟表）。
  - **触类旁通核查**：formula-identity 拒绝在 identity carrier 与全部 29 字段 cell 两轴一致（grep 实证 `data_type=='f'` 双 guard）；publish 与 request 共用 `collect_workbook_structure`/`structure_fingerprint`/`identity_inventory`/`observe_structure_inventory` 同一批原语，无第二份实现。
  - **未触碰他人 owner 文件**：D4-29 owned 文件（`phase5_d4_29_customer_detail.py`/`published_identity_observer.py`/`publish_time_structure_hash.py`/`test_d4_29_customer_detail_sync.py`）工作树 clean（Task 7/8 已提交）；本任务仅据实更新冻结记录文本，0 处源码改动，未改 D4-20/D4-10，未破坏 D4-30/31/32。
  - **完成裁定**：Task 10 全部谓词属 code+test / REQUEST_PATH 级（主控 §6.4 谓词 1-3、7-8 + hash 前完整性门），已实证全绿，**合法完成 [x]**。§6.4 谓词 4-6（真实 DB `working_paper_content_application` state=applied + operation 终态 + OO 侧 content version）需真实 OO 往返 DB 记录，**明确属 Task 11**，被 entry-wide materialize 性能天花板（262-320s>>120s 软上限，spec `workpaper-sync-materialize-large-table-performance`，主控 DEC-10）blocked，不在本任务范围。
- [x] 11. 合法升级 representation 后完成真实 HTML -> OO -> callback -> HTML 往返，记录 revision 和 operation；不得用模拟 callback 验收。
  - **🟢 原「materialize 性能天花板」blocker 已解除（2026-09-20 复核实测推翻旧前提，非绕开）**：owner spec `workpaper-sync-materialize-large-table-performance` **已全 8 任务 [x] 完成**（Wave 1 坐标索引把 O(N×表体积)→O(N+表体积)，Wave 3 CPU 段 to_thread 卸载，Task 8 已「解除 DEC-10」）。**本 entry 真栈实测**（frontend 3030 / backend 9980 / audit-onlyoffice healthy / audit-postgres，单 worker uvicorn --reload）：`d4-30-managed` materialize HTTP **0.33s**、`d4-29-managed` materialize HTTP **0.28s**（均 200 + 完整 descriptor + room_id/operation_id），远在 `materialize_soft_limit_seconds=120` 内。旧注记的 262-320s `materialize_soft_timeout`（2026-09-20 01:23）实为 **3-worker Playwright 并发同时 materialize 把单 worker CPU 饥饿**所致（Task 8 注记自述「3-worker 并发」），**非单请求真实天花板**。**未改软上限配置。**
  - **representation 合法升级确认（gen64 含 D4-29）**：当前 published representation = gen64 bundle `d7962c48`（contract def `4702f495`=`26733f27…`），实证含 `d4-29-managed`（store_items 映射 D4-29-rows→customer_detail_transposed + transposed_sheets `defined_name_ref`/`GT_MANAGED_REGION_D429`/`$C$10:$M$41`/客户信息检查表D4-29）；store-projection HTTP 200（含 29 个 D4-29 字段格：`customer_detail_transposed/cust-mu0j7ovb-1v1v/{name,creditcode,…}`，customer 1 有名有 28 空字段，合法非空态）。materialize 出的真实 OO 工作簿 47 sheet 清单含 `客户信息检查表D4-29`+D4-30/31/32（g5-1 evidence sheetNames 实证）。
  - **🟢 真实 HTML→OO→callback→HTML 往返在 gen64（含 D4-29）representation 上跑通并 applied（§6.4 谓词 4-6 达成，非模拟 callback）**：跑 `g5-1-d4-unified-path.spec.ts`（单 worker，同 entry/wp `b3ab3c46`）：store-projection 200→pending 200→materialize 200(JWT+room `2db31d21`+callback 5 项齐 room_id/generation/doc_key/route_credential_id/route_token)→OO Asc.editor 真写格(cellTextAfterSave 确认 marker)→forcesave **cs_error=0 / cs_outcome=accepted / HTTP 202 / operation_id**→真实 room-bound callback(`host.docker.internal:9980/api/workpaper-sync/rooms/.../onlyoffice-callback`)→**oo_to_html operation `76844e8d` state=applied + application `a02bd8db` state=applied（result_revision 90 / result_representation `8b9803aa` gen66）+ content_version `9be9a448` source=onlyoffice、operation_id 非空**（真库 DB 实证）。**revision 链 88(html)→89(html_to_oo op 28420119 applied)→90(oo_to_html op 76844e8d applied，source=onlyoffice)**。durable ack + operation 终态 + OO 侧 content version 三谓词端到端落库。
  - **仍缺的严格 D4-29-单格回读判据（如实标 blocked，非平台失败）**：上述 applied 往返的 OO 写格落在 **D4-2 的 A12**（g5-1 harness 目标），**非 D4-29 转置区字段格**。补跑 D4-29 专属 L2 探针（OO 写 `客户信息检查表D4-29!C11`=customer1 creditCode → forcesave → 回读 `D4-29-customers`）：**OO 侧写格成功**——evidence `evidence/task11-d4-29-roundtrip/D4-29-L2.json` 实证 `activeSheet=客户信息检查表D4-29`、`cellTextAfterSave=d429c930487`（marker 已写进 C11 并在 OO 内回读，`inserted/entered/saved=true`，materialize 200 + callback 5 项齐）；但 **forcesave 返 cs_error=4(no_changes)** → 无可 apply 的变更，oo_to_html op `5827240f` 停在 `created`/`request_frozen`，marker 未回读进 store。根因 = **探针先调 `asc_Save()` 已把改动 flush 进 OO 内部态，随后点 forcesave 时 OO 认为「无新变更」**（save 时序 nuance，g5-1 同手法因时序恰好过），属 **D4-29 e2e harness 层**，**非** 转置区不可写、**非** durable-ack/operation 生命周期失败（后者在同 representation 上已实证 applied，且 D4-29 sheet 已证可激活可写单格）。
  - **完成裁定（保持 [-] 不假绿）**：blocker（materialize 天花板）已解除 + 平台真实 OO 往返在含 D4-29 的 gen64 representation 上 applied（predicates 4-6 达成、记录 revision 88→90 + operation 76844e8d/28420119 + application a02bd8db、真实非模拟 callback）—— **代码已改并真栈实测到平台往返 applied**；唯一未闭合 = **D4-29 转置区专属单格 OO→HTML 逐格回读**（cs_error=4，OO 转置区单格定位待解，属 D4-29 UI/e2e harness 层，2-attempt 限内未攻克）。解除条件 = 修 D4-29 专属 e2e harness 的 save 时序（写 C11 后**不**先 `asc_Save()`，直接由宿主 forcesave 按钮触发 forcesave，使 cs_error=0），随后回读 marker 落 `D4-29-customers` 即可置 [x]（OO 侧 C11 写格已证可行，materialize/callback/往返链已在同 representation applied）。**未改软上限/任何性能配置，未改 D4-29/D4-20/D4-10 owner 源码（本会话仅新增后删 2 个 _tmp 探针 + 只读 DB/HTTP 测量），未破坏 D4-30/31/32，未用模拟 callback。**

  - **🔴 续作（2026-09-21）：应用 save-timing 修复后真栈实测 —— materialize 软上限 blocker「已解除」前提被推翻，且叠加 D4-36 并发漂移，保持 [-]。证据 `evidence/task11-d4-29-roundtrip/D4-29-L2-blocker.json`。**
    - **预检全绿**：backend 9980 health 200 / frontend 3030 200 / audit-onlyoffice 8080 200；会话开始时 store-projection 9980+3030 双 200，`expected_revision=92`，D4-29 在 bundle 内（`customer_detail_transposed/cust-mu0j7ovb-1v1v`，customer1 有名 creditcode='' 合法非空态）。
    - **修复已按主控精确诊断实现（in-scope 非绕开）**：新建一次性探针 `e2e/_tmp_d4_29_l2_roundtrip.spec.ts`，OO 写格 evaluate **移除预先 `asc_Save()`**，仅 `asc_insertInCell`/`asc_enterText`+`asc_closeCellEditor(true)` 提交进单元格并保持 dirty，由宿主 forcesave 按钮 `[data-testid=wp-sync-host-forcesave]` 触发真实保存（目标 cs_error=0 取代原 cs_error=4 no_changes）；目标 `客户信息检查表D4-29!C11`（row 11=creditCode，列 C=customer1，`FIELD_ROWS` 实证），回读 `customer_detail_transposed/cust-mu0j7ovb-1v1v/creditcode`。
    - **🔴 blocker 实测复现（save-timing 修复根本没机会跑）**：往返卡在**更前一步的 materialize**（整册 47 sheet）——`working_paper_sync_operation`/浏览器实测 `error_code=materialize_soft_timeout`：materialize CPU 段 **run#2=129.279s / run#3=128.506s**，均 >`materialize_soft_limit_seconds=120`（`backend/data/workpaper_sync_limits.json`）；单 worker 下**每次都超**（非瞬时抖动，故 2 次复现后按 no-loop 铁律停手）。run#1 早停于 store-projection 瞬时 500 precondition（后端已恢复 200），已给探针加 store-projection 预热门。**materialize 天花板对本 entry 稳定成立**——`MaterializeSoftTimeoutError` 在 `_stage_and_verify` CPU 段抛出（`content_mutation.py:~1600`），**先于** business-identity-reuse/idempotency 重放（`materialize_coordinator._find_business_identity_reuse`），故预热/重复调用**无法跳过** CPU 段，每次 materialize 都付 ~128-129s。
    - **推翻主控前提**：主控注记称 materialize 已压到 0.28-0.33s。本会话两次可复现的**单请求冷 materialize CPU=128.5-129.3s**（超软上限）。0.28s 疑为 warm/cached 或异 scope 测量；materialize 是整册（`sheet_key` 只选启动目标 sheet 不缩 materialize 范围）。**未改软上限/任何性能配置**（改参数绕错=禁止且属他人 spec）。
    - **会话中途叠加的第二个外部 blocker（非本任务引入）**：store-projection 会话中从 200 翻成 **422 `sync_contract_structure_drift`**——漂移点 `sheet='d436-managed' table='d4_36_backward' field='d4_36_backward/{row_uuid}/doc_amount'`。根因 = 并发未跟踪 owner 工作（`backend/app/services/workpaper_sync/phase5_d4_other_cutoff_sheet.py` D4-36 + 改动的 `d4.revenue_detail.json` 声明 `d4_36_backward` 结构，published representation 未含）。属 **D4-36（其他业务收入截止测试）owner scope**，**非** D4-29/30/31/32，`git status` 实证本任务 0 处源码改动（仅加后删 _tmp 探针 + 只读测量）。此 422 正是 Task 7 `_registration` 修复按设计工作（fail-visible 中文根因取代 opaque 500）。
    - **完成裁定（保持 [-]）**：精确诊断的 save-timing 修复**正确且 in-scope，但根本没机会被执行**——往返在更前一步的 materialize 被稳定复现的 `materialize_soft_timeout`（128.5-129.3s>120s）挡住，再叠加会话中途出现的 D4-36 源契约漂移 422。两 blocker 均**外部**（超时属 spec `workpaper-sync-materialize-large-table-performance` owner；漂移属 D4-36 owner），出本任务 scope。**未假绿。**解除条件：①本 entry materialize CPU 压进 120s 软上限（owner spec）或提供合法 evidence-capture 通道；②D4-36 owner 显式发布新 definitions 消除漂移使 store-projection 复 200；③随后重跑 save-timing 修复版 D4-29 L2 探针（写 C11 不预先 `asc_Save` → 宿主 forcesave → cs_error=0 → oo_to_html operation applied → application applied → 回读 creditcode==marker）。**未改软上限/性能配置，未改 D4-29/D4-20/D4-10/D4-36 owner 源码，未破坏 D4-30/31/32，未用模拟 callback。**

  - **🟢🔴 续作（2026-09-21 session 3）：materialize 性能 blocker 前提经实证「已解除」；但唯一 live blocker = D4-36 owner 的**未提交**工作树漂移 422。保持 [-]。证据 `evidence/task11-d4-29-roundtrip/D4-29-L2-blocker-2.json`。**
    - **预检全绿**：backend 9980 / frontend 3030 / audit-onlyoffice 8080 三者 health 200；audit-postgres healthy。
    - **🟢 materialize 性能 blocker 确认已解除（推翻 session 2 的 128-129s 前提）**：owner spec `workpaper-sync-materialize-large-table-performance` **全 8 任务 [x]**，其 Task 8 evidence 记录真库 D2 `POST /materialize` 在 **10.4s** 返回有效 descriptor（28491 字段/729 行，软上限 120s）。DB 亦见 gen69 bundle 上一条 `html_to_oo` op `d82c7db4` state=applied（03:12→03:14，~2min，属优化前后过渡期历史记录）。session 2 记录的 128-129s 系优化前或并发 CPU 饥饿测量。**本 session 未触发任何 materialize 超时。**
    - **published representation 现状（gen69 含 D4-29、不含 D4-36，内部自洽）**：`working_paper_sync_entry_state` 实证 wp `b3ab3c46` 当前 = gen69 `d1b92340`，bundle `4763e2d9`（contract def `a912eb4f`），2026-09-20T03:14:57 更新。gen69 rep **含 D4-29 转置区、不含 D4-36**，本身**不是**漂移源。
    - **🔴 唯一 live blocker = store-projection 422 `sync_contract_structure_drift`（稳定复现 2 次，55.7s/56.6s，同一漂移字段）**：漂移点 `sheet='d436-managed' table='d4_36_backward' field='d4_36_backward/{row_uuid}/doc_amount' locator='J:row_identity'`。**根因 = D4-36 owner 的**未提交**工作树改动**：`git status` 实证 `phase5_d4_revenue_detail.py`(M，+36 行**全是** `_INCLUDE_D436_CUTOFF_SHEET=True` 及 D4-36 接线；`_INCLUDE_D429_TRANSPOSED` 仅作 context 未改 → **D4-29 未被翻转**) + `d4.revenue_detail.json`(M，磁盘契约新增 d436-managed/d4_36_backward 11 字段) + `phase5_d4_other_cutoff_sheet.py`(??，新 D4-36 provider)。committed HEAD 契约 `e4a6496ba`(gen64/27 sheets) **不含 D4-36** → 漂移纯由未提交 D4-36 工作引入。`_registration` 每次 store-projection/materialize 都重解析磁盘契约与已发布 gen69 rep 比对 → `assert_no_structure_drift` fail-closed 422（正是 Task 7 registration 修复按设计工作：fail-visible 中文根因）。
    - **精确诊断的 save-timing 修复根本没机会执行**：往返在**更前一步的 store-projection** 就 422 挡死，materialize/OO 写格/forcesave 全未触达。**未自行补救的原因**：①改/删 D4-36 磁盘契约或 provider = D4-36 owner scope（禁改他人 owner 文件）；②`git stash` 他人未提交在制工作 = 破坏其会话且出 scope；③执行 D4-36 owner 的 reconcile 发布链（把 rep 推进到含 D4-36）= 其 publish 动作，且可能暴露未完成 D4-36 建模、破坏 D4-30/31/32。
    - **平台谓词 4-6 历史已达成（本 session DB 复核仍在）**：gen66 bundle `d7962c48` 上 oo_to_html op `76844e8d` applied + application `a02bd8db` applied @ result_revision 90 + content_version `9be9a448` source=onlyoffice，revision 链 88→89(op 28420119 applied)→90 完整。唯一未闭合仍是 **D4-29 转置区专属单格 C11 逐格回读**（本 session 因上游 422 无法触达）。
    - **完成裁定（保持 [-] 不假绿）**：materialize 性能前提**确已解除且本 session 未撞**；但 D4-29 L2 往返被**上游 D4-36 owner 未提交工作树漂移 422**挡住（性质同 session 2 记录的第二 blocker，本 session 精确定位到「未提交」+ 逐文件 git 实证）。属 **D4-36 owner scope**，外部于本任务。**未改软上限/性能配置，未改 D4-29/D4-20/D4-10/D4-36 owner 源码（本 session 仅新增后删 1 个只读计时探针 + 只读 DB/HTTP 测量），未破坏 D4-30/31/32，未用模拟 callback，已核 D4-29 flag 未被 `_tmp_d45` 翻转。** 解除条件：①D4-36 owner 提交并 reconcile（发布含 D4-36 的新 representation 使 store-projection 复 200）或还原其未提交改动使磁盘契约对齐 gen69；②随后在 store-projection 200 且含 D4-29 的 bundle 上重跑 save-timing 修复版 D4-29 L2 探针（写 C11 不预先 `asc_Save` → 宿主 forcesave → cs_error=0 → oo_to_html op applied → application applied → 回读 creditcode==marker）。

### Task 3 live preflight (2026-09-13)
Blocked, not accepted: see task3-live-blocker.md. D429 source switch is False and Task76 contract differs from task-2 handoff. Template candidate rejects 2051B nonempty store; authenticated store-projection returns HTTP500. No legal upgrade or actual OO callback achieved. Task 11 remains unchecked.
