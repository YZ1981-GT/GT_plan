---
inclusion: always
---

# 持久记忆

每次对话自动加载，**保持 ≤200 行**。明细下沉：完成事项 → `#dev-history`；架构决策 → `#architecture`；踩坑/编码约定 → `#conventions`；spec 状态 → `.kiro/specs/INDEX.md`。

## 用户偏好

- 语言中文；启动 `start-dev.bat`（后端 9980 + 前端 3030）
- **输出分步但连续做完**；**单次回复不要过长 —— 分段输出、每段一个可交付进展**（2026-08-05 用户明确要求）；**任务标记不能假绿**；**彻底解决不绕开**；**optional(*) 任务也要做完**
- **调查完必须主动给改进建议，别只堆实证就停下**（2026-08-05 用户催「明明都已分析好了呀」）
- **🔴 codegraph 优先于 grep**：146k 节点/312k 边/8673 文件；grep 仅用于非符号纯文本
- **触类旁通**；**改动前先 spec 三件套**（>500 行 / 3+ 组件 / 跨前后端）；**改动后必 Playwright 实测**
- **UI 全中文化**；**报表/附注金额默认「元」**；**中文场景全链路不能崩**
- **不要考虑轻量**：要针对性、联动性、美观性、实操性、易懂性；审计 UI 必须有逻辑追溯能力
- **四表取数新循环一律复用 `app/services/four_table/`**（`ReportLineAccountSpec` 声明报表行 + 兜底码 + `extra_standard_codes`；`select_leaves`/`aggregate_leaves` 做叶子聚合），禁止再抄一份科目定位/聚合逻辑
- **死代码立即删除**（不留 DEPRECATED/fallback 注释，否则每次复盘重复提议）
- **🔴 避免硬编码（2026-08-01 用户明确要求，全局适用）**：科目码/分类标签/章节号/表名/列 label/行集/列数/行数一律走**单一真源 + 守卫与真源双向锁死**。落法：①科目 → per-cycle `xAccountScope.ts`（`k2AccountScope.ts` 是范式），运行态取 render 下发的 `tb_source_codes`，常量只作兜底+展示 ②分类桶 → 一份声明式 dataclass（含 `source_ref` 指向源 xlsx 单元格），后端分类器/prefill 行标签/前端 seed 共同读，前端不抄第二份 ③章节号/表名字面量无法避免时（registry 生成器要求内联）**必须**与 `note_template_variant_matrix.json` / `note_template_*.json` 交叉锁死 ④列 label/行集真源 = 源 xlsx，守卫 openpyxl 直读三向比对 ⑤**按公司/单位横向展开的表禁写死列数**（`公司1..公司N`），改动态列 + 稳定 key `{slot}_{seq}`（H7 范式，**key 不能用 label** 会撞键）⑥**动态区骨架行数禁写死**（`blankRows(p,3|5|10)`），改 `max(seed 行数,1)`，预置空占位会被推成占位披露行 ⑦守卫读源码前必 `stripComments()` + 反向自检
- 功能收敛；git 单 commit；**push 前必先 fetch**；**协作走 PR 不直推 main**
- **会话结束前清掉自己的 `tmp_*` 诊断产物**（用户 2026-08-01 明确要求删；曾积到 68 个 / 18.7 MB，含 6.8 MB 的 vitest JSON 与带 token 的 `tmp_login.json`）。`.gitignore` **尚未收 `tmp_*`**（已向用户提议加、待答）
- **spec 归档按功能分类**（05-business-features / 04-infra 等），不按日期批次
- 目标并发 6000 人；底稿编码 = 致同 2025 修订版
- 5 角色轮转：审计助理 / 现场经理 / 业务合伙人 / 质量控制复核合伙人 / EQCR 技术复核人
- **v3.0 愿景（当前不做）**：项目级知识自动提取 + 跨年度续审继承

## 平台级 UI/数值铁律

> 完整机制与代码位置见 `#architecture` §平台级数值格式 / §表格 UI 规范。

- **金额格式单一真源** = `stores/displayPrefs.ts` 的 `fmtAmount()`（千分符 + 2 位小数 + 默认「元」+ localStorage 持久化）。只读金额一律走它。**🔴 它是 store 成员、不是模块级导出** —— 写 `import { fmtAmount } from '@/stores/displayPrefs'` 会让整页崩成「页面渲染出错：does not provide an export named 'fmtAmount'」（`get_diagnostics` 与 vitest 全绿，只有浏览器暴露）；正解 = setup 顶层 `const displayPrefs = inject(DisplayPrefs_Key, null) ?? useDisplayPrefsStore()` 再 `displayPrefs.fmtAmount(v)`（`useDisplayPrefsStore` 是 setup 作用域 composable，写进函数体静默失效）。
- **防折行全局** = `styles/global.css` 的 `.el-table td.is-right .cell{white-space:nowrap;font-variant-numeric:tabular-nums}`。
- **🔴 可编辑金额千分符只能用 `el-input`（2026-07-29 已定论，双证）**：①源码 —— EP **2.13.6** 的 `node_modules/element-plus/es/components/input-number/**` 全文无 `formatter`/`parser`，该 prop 不存在；②浏览器实测 —— `el-input-number :formatter` 下输 1234567.5 显示 `1234567.50`（**无千分符**），换 `el-input` 后显示 `1,234,567.50`。故平台现存 40+ 处 `el-input-number :formatter`（K1TabDisclosureListed·Soe / K1StageEclTable / E1TabCashCount / E1TabCreditReport）**全是空操作，千分符从未生效**；`wpAmountInput.ts` 的用法注释写错。→ 统一用 `components/workpaper/shared/WpAmountInput.vue`（失焦千分符 / 聚焦原始值便于编辑 / 粘贴带逗号可解析 / 非法输入回退不写 NaN）；存量替换待单独 spec 收口。`:precision="2"` 是真 prop 可留。**绝不套用**利率/汇率/比例/笔数/年度/月份。
- **🔴 `fmtAmount(0)` 默认返回「-」不是 bug**：`stores/displayPrefs.DEFAULTS.showZero = false` + `utils/formatters.fmtAmountUnit` 的 `if (n === 0 && !showZero) return '-'` → 「0 显示成 -」现在是**用户可切换的平台级偏好**（已收敛到单一真源），不再是组件私自写死。要让 0 恒显 `0.00` 得改 `showZero` 默认值 = 波及全部表格的平台级变更，别在单个循环里绕。
- **底稿表格统一**：13px 字号；AI+复核按钮右对齐在 section 标题同行；公式列虚线下划线 + tooltip 来源；审计说明/结论 `el-card` 包裹；编制提示 `details` 折叠底部。
- **`GtPageHeader` 操作按钮**放**默认插槽**用 `margin-left:auto` 推右（放 `#actions` 会被拉伸成整条）；列表页表格禁 `border stripe`，改无边框 + `el-card shadow="never"`。
- **列表页打磨范式**：状态列全中文彩色 tag（禁裸英文）；操作列 >4 项收「更多▾」；`highlight-current-row` 代替"选中"按钮；文件名带类型图标；状态面板禁空洞 `el-card` 改紧凑单行 bar。
- **🔴 Vue 模板属性禁用中文引号/特殊 Unicode**（`content="…"XX""` 的 U+201C/201D 触发 Vite 编译崩溃，`get_diagnostics` 查不出）。
- **导入导出统一** `el-dropdown「导入导出▾」`（导出模板/导出数据/导入数据）+ 复用 `useXImportExport`；多区块分 sheet。

## 底稿交互铁律

- **交互点选优先**（尤其 C 类控制测试）：判断/枚举字段一律下拉/单选/多选 tag/按钮点选；长文本才 autosize textarea + AI 辅助。
- **动态行新增**需命名的必须先 `ElMessageBox.prompt` 输入名称再创建。
- **多 section 底稿每个文本区都要 AI 辅助**（section 标题行右侧放 AI 按钮，不只底部有）。
- **宽表（>10 列）录入**改引导式弹窗（分组卡片 + 内嵌方法论 + 实时联动分析面板 + 点点点 select）；>15 列必须拆分（区段 Tab / 借贷双区块 / 固定列+滚动 / 左右视觉分组）+ ⚙列设置。
- **源模板红字内容**嵌入对应功能区上方作"方法论上下文"（琥珀色左边线+浅黄背景）；示例内嵌到编制界面可一键套用，不藏 Drawer。
- **必要处加 📎 附件上传 + OCR**（样本证据/凭证/审计证据/过程记录），复用 `/d4/contract-ocr` → 确认弹窗 → merge 填充。
- **结论/缺陷/偏差回写**：→ B50 EventBus / → A14 缺陷底稿 / → C21-1 汇总 / → A13 错报（`a13:push-misstatement`）。
- **叙述式底稿**（仅核对+结论）不加独立审计意见区；textarea 用 `:autosize="{minRows:5}"`。

## MCP 使用铁律

- **🔴 工具选型阶梯**：符号/调用链/影响面→`codegraph`；wp_code/componentType/spec 进度→`gt-plan`；库表实证→`postgres`；容器日志/健康→`docker`；PR/CI→`github`；框架 API→`context7`；用户可见行为→`playwright`；非符号纯文本→`grep`（末位）
- **🔴 gt-plan 优先于手翻 JSON**：查 wp_code、spec tasks、迁移 V 号先 `wp_lookup` / `spec_status` / `migration_status`
- **🔴 postgres 只读**：仅 `restricted` 模式；禁经 MCP 写库；写库验证用 pytest 或现有脚本
- **docker / github 默认只读**：破坏性操作须用户明确要求
- **禁止 MCP 叠床架屋**：同一问题只选一主工具；失败降级回退终端命令并说明；密钥仅 `.cursor/mcp.env`

## 底稿开发铁律

- **风险导向审计**：B50 风险 → D~N 程序表 → A13 评价错报，全链可追溯
- **componentType 选型**：结构化=`d-form-table` / 复杂 Excel=OnlyOffice / 文档=`word-template` / 程序表=`a-program-console` / 函证=`confirmation-*`（9 类）
- **三表 HTML 渲染**：底稿目录+审定表+附注全走 HTML，仅复杂公式/DCF/图表留 OnlyOffice
- **联动是核心价值**：`ref_index` chip + `auto_data_source` 实时取数；孤立底稿=无价值
- **🔴 函证模块跨循环共享**：D0 的 9 个 `confirmation-*` 组件跨循环复用（E0/F0/G0/H0/K0/L0），不为每循环独立开发
- **开发前必先逐 sheet 读源模板**（openpyxl 读 xlsx + BCD 类 md 交叉验证）
- **🔴 源模板 xlsx 只认 `backend/wp_templates/`（运行时权威）**：`wp_template_init_service` 生成底稿时从这里复制、`wp_template_finder` 以 `_index.json` 索引。`基础数据/致同通用审计程序及底稿模板…` 是**参考副本且已落后**（实测 G4/G5/G6 两处不一致：权威版多「项目N（可改名）」占位行 +「（预留，可填或在本区内插入行）」预留区 + 6 条「编制说明」；G4 国企三阶段用语统一为「减值准备」；G4/G6 上市**只有「期末第一阶段」末列是「理由」**其余为「划分依据」；G5 删了「应收保证金/应收关联方款项」两行。G8~G12 两处字节一致）。2026-07-30 曾据参考副本重建 G4/G5/G6 后返工。**读模板前先比对两处 size**，并注意 `~$` 锁文件要跳过（用户开着 WPS 时可能还有未保存改动）
- **🔴 增强打磨禁止自造披露内容**：附注/披露表增强必先看源模板，禁按"常识"造表（D5 曾自造"金融资产风险敞口"）。**存量已发现自造组件：`G6TabDisclosureListed.vue`**（7 个虚构小节「一、其他债权投资成本/二、利息调整/…/七、其他披露事项」+ `generateRows()` 批量生成 137 行 `成本项目N`，列头亦自拟；源表实为 14 张表）→ **不得接附注同步**（会污染附注），须按源模板重写；G6 国企侧结构正确（仅行名是可改名占位，可接）
- **模板预填优先于 AI 生成**：有固定骨架的章节用 CHAPTER_TEMPLATE + 变量替换，AI 仅用于复杂章节
- **🔴 底稿→附注同步不得压扁列结构**：底稿 UI 已按源模板渲染宽表时，`{X}NoteSectionMap.ts` 必须推同构列头（D2 曾把 6 列分类披露压成 2 列，附注/Word 双双缩水）；双期表拆两张、续表键补「（续：上年年末余额）」
- **`sub_table_data` 唯一规范形态 = `{key: list[dict]}`（业务键行）**：投影结果（`{rows,_column_groups}` / 位置化 `values`）被回写时由 `note_sub_table_projector.normalize_sub_table_data` 读写两端逆投影，禁止再写 skip 分支丢整表
- **底稿改版重命名子表**须随载荷上报 `sub_table_data._removed_table_keys`（后端删旧键，跳过本次推送键），否则附注永久残留空表
- **附注行真源 = `rows[].label` + `rows[].values`（`_cell_meta`/`_cell_modes` 按 value 列索引键，标签列不算数据列）**；离线导入 xlsx 中间结构才是 `cells`（`cells[0]` 是标签列，对齐时须 -1）。`note_offline_import_service` 曾两侧都读 `cells` → 对真实附注 diff 恒「本地全空+全 ADD」、OVERWRITE 还把 `cells` 写回库（渲染器读不出），已修
- **D~N 专属组件 8 步**：registry+yaml → composable 分层（`useXFormulaEngine` 纯函数）→ 主入口 sheetName v-if 分发 → 后端 3-4 py → 注册四件套 → 联动（TB 回写+EventBus+GtIndexChip）→ UI 铁律 → 功能方向（联动/美观/溯源/易操作/导入导出/AI/双三模式）
- **OO sheet-name 必须与源 xlsx tab 名完全一致**；多 sheet workbook OO 隐藏非目标 tab
- **通用 schema 复用** `{wp_code}-generic.yaml` + pattern matching
- **🔴 列式转置结构**（投资项目作列头+检查项作行，如 G4-9）前端必须转为行式交互视图

## 审定表预填充铁律

- **X-1 审定表未审数从 `tb_balance` 明细子科目预填**（render 策略 `_build_adjudication_prefill`）：`get_active_filter` 查 `{code}%` → **优先叶子科目防双算** → 按 code 段分类 → 负债 `abs()` → snake_case 对齐前端；**仅无持久化时预填**（编辑后不覆盖）。`trial_balance` 只有一级总额+期末，故分类行必须从 `tb_balance` 取。
- **审定表双期结构**：源模板若有「期初数/期末数各（未审/账项调整/重分类/审定）」分组表头即双期，单期 roll-forward 是误用；有一年内到期则加「减一年内到期→最终审定数」。
- **四表取数 Tier A vs Tier B**：固定类别审定表（每类=一个科目，如 F2/H/I/L/M/N）适合 Tier A 可编辑 `TB()` 公式；动态分类审定表（D 循环信用风险/账龄）只能 Tier B 预填。**宁缺勿造**：无法干净映射的分类行不 seed。

## 环境配置

- Python 3.12 / Docker / PG 16 / Redis；后端 9980 / 前端 3030 / vLLM 8100；DB `audit_platform`
- vLLM：Qwen3.5-27B-NVFP4，APC+fp8 kv-cache
- Docker：postgres(5432)/redis(6379)/metabase(3000)/pgbouncer(6432)/OCR(8200)/onlyoffice(8080)
- DB_DISABLE_SSL=True；连接池 150 / PG max_connections=200
- 前端唯一路径：`audit-platform/frontend/`
- **codegraph v0.9.9**；**MCP 7 个**（codegraph/playwright/postgres/github/gt-plan/docker/context7，配置 `.cursor/mcp.json`）；**rtk 0.42.1**；**OnlyOffice 9.4.0**
- 部署 v2.0：瘦客户端（Electron）+ 内网全栈
- **DSH = Deep Horness（集成 AI Agent 引擎/聊天面板，spec `dsh-agent-panel-integration` Task 1~31，Phase A/B/C 门禁）**：后端 `app/services/ai_chat/{dsh_engine(40KB per-run 子进程+有界 worker 池+取消传播+跨用户隔离),dsh_discovery(SDK discovery+版本 pin),dsh_masking(工具结果脱敏，审计只记 bytes)}.py` + `backend/tests/dsh_agent_panel/`(17 文件)；前端统一面板 `DocAiChatPanel.vue`（6 宿主：底稿/报表/附注/知识库/全局/独立窗口共用同一 `AiHostRequest` 形状，Req 3.x）+ `WorkpaperEditor` 的 `openShellDsh`/`dshAssistBridge` + SSE 断点续传 + adopt 只认服务端签发 message ID；MCP = `tools/audit-data-mcp/server.py`（stdio，数据经平台授权 REST）。**启动**：`start-dev.bat` 有 `DSH_DIR=D:\DeepHorness` / `DSH_PORT=3080` / `VITE_DSH_URL`，launcher 不再起 DSH Web UI（Task 30，改后端 DshEngine per-run 子进程）。**🔴 运行时当前不可用**：SDK vendor 目录 `D:\DeepHorness` 本机**不存在** → AI 面板回退 native engine（代码齐全但要先部署 Deep Horness SDK 到该目录）。DSH 失败**绝不静默回落 native**（Property 25，返 `engine_unavailable`）。

## PG schema

- `trial_balance` = standard_account_code / unadjusted_amount / aje_adjustment / audited_amount（v2 正数口径，只有一级总额+期末）
- `tb_balance` = account_code / opening_balance / closing_balance / debit_amount / credit_amount / closing_direction（多级子科目；**无符号绝对值 + 方向列**，recalc 必按方向带符号求和）
- `working_paper` 无 wp_code（在 `wp_index`，需 JOIN）
- recalc 铁律：只汇总**叶子**科目；未映射叶子按**最长前缀**继承祖先映射；损益取发生额
- `tb_ledger` 人员列仅 `preparer`（无 poster/reviewer）；`counterpart_account` 填充率低（~9%）不可靠
- `tb_aux_balance` 按维度**冗余存储**（校验须 GROUP BY aux_type，见 `#architecture`）
- 契约测试：schema_contract（表级）+ column_contract（列级）+ componentType 契约 vitest
- **迁移** = `backend/migrations/V*.sql`（MigrationRunner，非 alembic），新加必须 `IF NOT EXISTS` 幂等

## 任务状态

> 完整 spec 进度、编排器历轮判定、各循环（G7/I/K/L/M/N/E/F 等）深度调查结论、各类复盘明细 → 全部下沉 `#dev-history` 末尾「从 memory.md 下沉的任务状态与复盘明细」。此处只留每次对话必用的活状态。

- **当前在做**：`k-cycle-extraction-formula-and-disclosure-closure`（最新实测 5/25，Wave1 全交付 + Wave2 Task4/5，未 commit）。
- **`d4-price-analysis-writeback-linkage` 进行中 7/10（2026-09-13，未 commit）**：D4-10/11 价格分析双向回写+联动+死代码修复。✅ Task 1~7 全交付：`useD4Adjudication.ts` crossSheetMainRows/crossSheetOtherRows computed 派生+合并去重；删 `useD4CrossSheet.ts` syncProductRowToAdjudication/syncOtherItemRowToAdjudication 死函数；宿主 provide d4CrossSheet；D4-10/11 inject+「从上游导入」merge+chip bug `wp:D4-10→wp:D4-2`；eventBus `d4:price-abnormal`+crossWpEventBridge 桥接+`useD4PriceWriteback.ts`(新建)接收端幂等回标 D4-2-rows；D4-10/11 异常 watch emit+结论 `disclosure:note-text-updated`；守卫 26 绿（4 spec 文件）。`[-]` Task 8 Playwright 待全栈；Task 9/10 C0-C4 治理待做。核心决策：DEC-A 前端 computed 联动（tb_ledger 数量列填充率 0%，不造 resolver）。
- **`d4-ipo-fraud-writeback-formula-io` 已 7/7 全交付（2026-09-13，未 commit）**：D4-29/30/31/32 IPO 舞弊应对四表。产物：`d4_ipo_descriptor.py` + `_d4_import_export.py`（4 parser/exporter + item_id 修正 + 列头补齐）+ `useD4IpoDiscovery.ts` + 守卫 44 绿（`test_d4_ipo_io.py` 27 + `useD4IpoDiscovery.spec.ts` 17）+ Playwright 四 Tab 渲染+导出模板验收通过。核心修复=item_id 错配（D4-29→customers 等）+ 通用 parser 中文 key + D4-30/31 列头未声明。
- **`d4-ipo-checklist-dual-mode-writeback-and-formula` 进行中 14/22（2026-09-13 新开，未 commit）**：D4-25/26/27/28 IPO 检查四表一次收口三件事。✅ Task 1/3/4/7/8/9/10/11/12/13/14/15/17/18/20/22 全交付（14 主任务绿）。产物：`ipoChecklistSchema.ts`（列规格+14条公式预设+投影纯函数）+ `ipoChecklistFormulaEngine.ts`（表内计算引擎，SUM 多参+四则+分母0→null+手填覆盖）+ 四组件改写（D4TabDealer/Overseas/UndisclosedRp/CustomerChecklist.vue 列规格驱动+两级表头嵌套分组+checkbox/select 点选+派生列只读）+ `_d4_revenue.py` 追加 4 resolver（`d4_25_dealer_sales`/`d4_26_overseas_sales`/`d4_27_related_party_sales`/`d4_28_customer_balances`）+ 守卫 28 绿（后端 15 pytest + 前端 13 vitest）+ CI job `d4-ipo-checklist-column-formula-guard`。`[-]` blocked：Task 2/5/6（Governance B2 门控需 ContentMutationService 适配）、Task 16（变异脚本待写）、Task 19（Playwright 待全栈环境）、Task 21（git add 待确认）。
- **`d4-inspection-writeback-formula-io` 进行中 6/7（本会话 run-all-tasks 完整推进，未 commit）**：D4-13/14/15/16 检查表 IO+公式+发现链+守卫。**本会话从头推 7 任务**：Task1 descriptor 57绿+3变异RED / Task2 F-SHELL 预设13条+verdict适配器 76绿+2变异RED / Task3 D4-13双item+D4-15三层嵌套+D4-16英文key IO 34绿+3变异RED / Task4-6 auto-completed（wave propagation）。Task7 `[-]` 有意驻留（双模式阻塞：36 D4 sheet 共用 `d4:save-items` last-write-wins + Playwright 需 OnlyOffice 环境）。**合计 178 passed 0 failures**。产物=`d4_inspection_descriptor.py`+`d4_inspection_formula_verdict.py`+`_d4_import_export.py`(M)+`useD4InspectionDiscovery.ts`+5测试文件，全 `??` 待 git add。Spec 三件套 0 error 0 warning。
- **`d4-9-customer-structure-bidirectional-writeback` 进行中 11/24（原 15 任务 + 新增 Task 16-24 统一前端桥；Task5/9/13/14 仍 `[-]` UNVERIFIABLE；合计 115 passed+1 xfail+mutation RED 5/5；未 commit，分支 `work/2026-05-30-wp-specs`）**。🔴 **2026-09-13 新增 Task 16-24（Unified Frontend Bridge）**：用户决定将 D4-1 Task 2.1b（前端桥接入）统一归入 d4-9 spec 推进。策略=只对 D4-1/D4-9 加 `syncMode='bridge'` 旁路（`flushSave` 走 `commitHtmlProjection`），其余 34 sheet 的 `d4:save-items` 原链不动。D4-1 复用 `xlsx/gt-d4-operating-revenue`（overlay 升级 bidirectional），D4-9 走独立 curated entry（已存在），两不同 entry_id 规避 RG-4。全链前置=Task 16.1 overlay 升级（RG-18 门）+ 16.2 digest 复核 + 16.3 manifest 重生成。
  - ✅ **Task7 内核多区扩展已交付并验证**（本会话委派子代理 + 独立复验）：双向引擎支持**同 sheet 多受管区**，additive、单区 358 工作簿字节/行为不变。改动点：`ExcelIdentityBinding` 加 `region_key`（默认空=单区不变）；新增 `excel_extract.binding_key(rt, key, region_key=)`（`f"{KEY}__{region_key}"` 优先、缺失回退裸键）；`managed_tables_of` region_key 非空时同 sheet 第二张动态表不 fail-closed（属别区）、静态表只挂第一区（totals 不双写）；`excel_materialize.assert_footer_anchor_stable`/`assert_shifted_footer_gates`/`_plan_row_shift` 全线 region-scoped，marker 搜索用 `region_last_data_row` + 新 `_find_marker_row_after` 消歧（两区 footer 同 marker「合计」落 23/37）；🔴 **injector `_GT_SYNC` 后缀从 `entry_id` 改 `template_id`**（d4-9 两区共享 entry_id，用 entry_id 会互相覆盖，第一区元数据被第二区盖掉）。`phase5_d4_customer_structure.region_bindings()` 产两 region binding（D49C/D49P）。守卫 `test_d4_9_task7_roundtrip.py` 2 passed（真 materialize→extract 两趟：本期+4总额、上期逐字段往返 + D/F 占比公式保护），全 D4-9 后端 **54 passed**（+1 pre-existing stale-gate error 与本改动无关，stash 复现确认）。变异 3 处 RED。真实模板 `D4收入底稿.xlsx` 有 **17 外链工作簿** ⇒ `ooxml_security` 拒绝，materialize 测试用几何一致的**无外链最小 workbook**（仍解析真契约）。
  - ✅ **Task8/12（前端 legacy 移除 + 守卫）**：🔴 **design 前提过时**——D4-9 在线编辑**早已**由宿主 `GtD4OperatingRevenue.vue` 统一 `useD4EntryDualMode`+`GtOnlyOfficeSheet`（entry=`xlsx/gt-d4-operating-revenue`）承担，不需要「第二个 useWorkpaperSyncBridge/独立 host」。真正 legacy 双入口 = `D4TabCustomerStructure.vue` **自带**的第二个 `editorMode`/`GtOnlyOfficeSheet`（宿主+子组件各起一个 OO 会话）。已移除该组件内 mode-bar+双模式+OO 分支+`.mode-bar` 样式，只留结构化视图。守卫 `d4CustomerStructureLegacyEntry.spec.ts` 5 passed（剥 HTML/JS 注释 + 自证 helper 有效）。前端合计 11 绿。
  - 🔴 **纠错（2026 本会话实证，此前 memory 说「生成器无 curated 机制」已过时）**：`generate_workpaper_sync_manifest.py` **已实现完整 curated_entries 设施**（`_build_curated_entries`/`_CURATED_MIGRATION_STATES`/`_REQUIRED_CURATED_FIELDS`/`_assert_profile_in_expected`，docstring 点名 GtD4OperatingRevenue.vue 为动机场景，属并发 `workpaper-sync-curated-entry-facility` spec 交付）；`workpaper_sync_entry_overlay.json` **已声明** reviewed curated entry `xlsx/gt-d4-customer-structure`（capability=bidirectional/adapter_id=d4.customer_structure/host=GtD4OperatingRevenue.vue/sheet_literals=[重要客户结构分析D4-9]/review_status=curated_reviewed）。**Task 6 代码交付已完成且全绿**：给 `phase5_d4_customer_structure.py` 加 `assert_manifest_capability_enabled`/`manifest_capability_enabled`/`attach_adapters`（镜像 D4-2，fail-closed 顺序），守卫 `test_d4_9_task6_manifest_capability_attach.py` 真跑 `build_manifest`(真 overlay + pinned discovery fixture) 10 passed + 变异 3 锚点 RED，D4-9 套件 46 passed。🔴 **Task 6 仍 2 项 UNVERIFIABLE（未强绿、未 flip 复选框）**：①磁盘 `--apply` 重生成被**既有** `approved_source_digest` 漂移堵死（reviewed `d9fddb64…` vs 活源 `a18a531d…`，276→274 mounts，因**并发会话正在编辑 D4/D1-D7 宿主 .vue**）—— bump digest 需 mount-diff 复核 in-flux 拓扑，不安全故未动 ②`attach_adapters` 的 DB/OO runtime 尾部（representation/bundle/observation）需 DB+已发布 bundle+真实 OO，本机 `D:\DeepHorness` 缺，属 Task5/7 阻塞链。∴ Task13/14/15 全依赖此链（Task13 明确要真实 OO），本会话不可跑绿。解阻需：(a) mount-diff 复核并重批 digest 让 manifest 干净重生成 (b) 提供真实 OO/DeepHorness runtime。**用户已裁决走 Option B（per-sheet entries），但实证 curated 设施已就绪，Task6 落地实为附加式 curated entry（骑真实宿主，不改 discovery 分组键），与 B 目标一致且侵入更小。**
  - 🔴🔴 **（历史记录，部分已被上条纠错覆盖）剩余核心阻塞 Task5/6（manifest curated entry）**：D4-9 与 D4-2 同宿主，生成器一 host 一 entry + registry entry_id 唯一 ⇒ `d4.customer_structure` adapter 无法注册（无独立 entry）。**新架构认识**：registry 按 `(wp_code, sheet_key)` 解析 + D4-9 在线编辑本就走 D4 entry ⇒ 更干净的终态可能是**D4 entry 挂多个 sheet 域 adapter**（而非独立 entry）；D4-9 adapter/contract/引擎已建好、两种终态都就绪。生成器 `--apply` 需 Node discoverer + `approved_source_digest` 门（本分支已漂移）＋会重生成 186-entry manifest，blast radius 大，**未动**。另本分支 committed manifest **D4-2 自身仍 single_onlyoffice/adapter=null**（D4 adapter 注册链在本分支可能整体休眠，Task5 落地前需先确认 D4 后端能否走到注册路径）。
  - 🔴🔴 **Task5/6/7/8 架构阻塞（design §5 与实现冲突，待用户裁决 B/C）**：design 要 D4-9「新建独立 entry `xlsx/gt-d4-customer-structure`」，但 ① `generate_workpaper_sync_manifest.build_manifest()` 按 `(host文件,component)` 分组建 entry（`_entry_id(doc_type, source_file)`，一 host 一 entry），D4-9 与 D4-2 **同宿主** `GtD4OperatingRevenue.vue` + 同 component `GtOnlyOfficeSheet` ⇒ 塌成同一个 entry，且 D4-9 的 sheet 是**运行时表达式** `ooSheetName`（AST 静态取不到）；② registry `register()` **entry_id 唯一绝对**（`if entry_id in self._by_entry_id: raise`，RG-4 Property 3）。**故 Option A（两 adapter 共用 D4-2 entry_id + sheet 域 matcher）已实证不可行**——本会话重读 registry 源码确认 entry_id 唯一是硬约束，与 sheet_keys 无关。**剩 B（改生成器+Node discovery 出 per-sheet 子 entry，但 sheet 名动态 AST 取不到）/ C（给生成器加 curated_entries 附加段产合成 entry，推荐、附加式不扰 186 个 discovered entry）**。生成器**无任何 synthetic/curated 机制**（严格 discovery-derived）。改动 `generate_workpaper_sync_manifest.py` = 共享生成器（属已归档 closure spec）+ 会重生成 186-entry manifest，有 blast radius，**待用户明确同意再动**。另本分支 committed manifest D4-2 自身仍 `single_onlyoffice`/adapter=null（bidirectional overlay→manifest 链在本分支降级未跑）。
  - 🔴🔴 **阻塞②（materialize 引擎单区，2026-09-13 新发现，比 entry_id 阻塞更根本）**：`excel_materialize`/`excel_extract.resolve_managed_region`/`read_runtime_binding_pairs` 读 `_GT_SYNC` 的**单个未加后缀** `GT_MANAGED_TABLE`/`GT_FOOTER_ROW`/`GT_ROW_UUID_COLUMN`，`ExcelIdentityBinding` 单个 `table_name`/`uuid_column`/`table_key` ⇒ **整个双向引擎假设「一 entry/sheet 一受管区」**。Task1 多区注入器产出**加后缀** `_GT_SYNC` 键，引擎消费不了。D4-9 同 sheet 双动态行区是全栈首例 ⇒ Task5/7/8 完整落地需**三处共享内核**扩展（生成器 curated_entries + materialize/extract 多区迭代 + binding 多区），都在降级分支+属已归档 closure spec，blast radius 大。**结论：D4-9 独立逻辑（contract/投影/合并/保护集/血缘/IO/变异守卫，57 绿）已全交付；剩余双向接线是跨三内核的大改，需专门决策而非「继续」可完成。**
  - Task11（后端守卫+变异）已交付：`mutate_d4_9_customer_structure_guards.py` 5 锚点（删 formula_mask/静态标量改 row 域/合并两 table/去 rowId/总额来源改错）`--run all` = RED 5/5、还原全绿。踩坑三条：①`parents[3]` 才是 repo 根（diagnose 下 `parents[2]` 双拼 `backend/backend`）②pytest 中文输出 GBK 控制台 `text=True` 静默吞 None ⇒ 0 FAILED 误判 GREEN，改 `PYTHONIOENCODING=utf-8`+`decode(replace)` ③契约漂移让 module fixture 抛异常 ⇒ 整模块报 **ERROR 非 FAILED**，判据须 `-rAE` 收两者。🔴 **决定性实证（2026-09-13 读源码）**：`EntryMatcher.overlaps()` 对「共享 wp_code 但 `sheet_keys` 不相交」**返回 `()`（不冲突）**、`matches()` 按 sheet_key 过滤 —— 即 registry 层**已支持**同 wp_code 两 adapter 只要各带不相交 sheet_keys（RG-3 不触发）。故唯一真改点在**生成器分组键**：`build_manifest` 用 `_group_source_facts` 按 `source_file` 分组 + `_entry_id(doc_type, source_file)` 一 host 一 id ⇒ 若要 B（独立第二 entry）须把分组/命名键从 `(source_file)` 改成 `(source_file, sheet_key)`。**A 路径无需改生成器**（单 entry 双 adapter 违 RG-4，故 A 实为「单 adapter 覆盖 D4-2+D4-9 双 sheet」或需 registry 允许 entry→多 adapter）。综上 **B 是结构正解但动共享生成器，A 最小侵入但需重读 design §5 语义**——两条都要用户拍板，非可自动推进。🔴 **D4-1 同宿主同理**：D4-1/D4-2/D4-9 都挂 `GtD4OperatingRevenue.vue`，d4-1 的 Task2.1b/5.2 撞的是**同一个** manifest per-host 阻塞，与 d4-9 Task5/6 是同一裁决，必须合并推进。**另：D4-1 TB 发布侧已由并发会话在 `useD4Adjudication.publishAdjudicated()` 落地人工发布+阈值确认+幂等（见本 d4-dual-mode 行），与我 Task4.2 后端 `decide_d4_1_tb_publish` 纯决策互补（前端已接、后端判据可作守卫/二次校验），不重复。** 🔴🔴 **Path B 可行性再实证（2026-09-13，用户选 B 后深挖）：纯改生成器分组键行不通**。`GtD4OperatingRevenue.vue` 只有**一个** `GtOnlyOfficeSheet` mount，绑 `:sheet-name="ooSheetName"`（单一动态表达式服务全部 D4 sheet），且 entry-notice 硬编码 `entry-id="xlsx/gt-d4-operating-revenue"`。discoverer(`discover-workpaper-sync-mounts.mjs`) 的 `mountId` 已含 `sheetExpression`，但此 host 只有一条 mount 一个表达式 ⇒ 无论按 `(file)` 还是 `(file,sheetExpression)` 分组都只得**一组一 entry**。∴ Path B 是**前端宿主 + 生成器耦合改**（非纯生成器）：须先让宿主为 D4-9（及 D4-1）声明**独立的 per-sheet sync-host mount**（各自 `WorkpaperSyncEditorHost`/`useWorkpaperSyncBridge`，distinct entryId binding）—— 这正是 d4-9 **Task 8** 的活；再让生成器按新的 per-sheet mount 分组产出第二 entry。故 Path B 必须落在 d4-9 owner（Task8 前端宿主重构 + Task5/6 生成器/manifest），d4-1 无法独立推进。**且是跨前后端多组件改 ⇒ 必须先立 spec 三件套**（改动前 spec 铁律）。🔴🔴🔴 **Path A 也被 RG-4 与 manifest 现状堵死（2026-09-13 用户改选 A 后读源码+manifest 实证）**：① registry `register()` 有 **RG-4**：`if entry_id in self._by_entry_id: raise 「一个独立 entry 只能解析到唯一 adapter」` ⇒ **不能**在 `xlsx/gt-d4-operating-revenue` 上再注册第二个 adapter；Path A 只能是「**一个** adapter 的 matcher 覆盖 D4-2+D4-1 双 sheet，内部按 sheet_key 分派」。② 但**决定性**：committed manifest 里该 entry `capability="single_onlyoffice"` + `migration_state="legacy_fake_bidirectional"` + `legacy_reasons=[template_only_open, no_durable_forcesave_ack, missing_adapter]`、无 adapter_id、唯一 mount 是 legacy `GtOnlyOfficeSheet`（`:sheet-name="ooSheetName"` 动态）。register() 的 RG-18：对非 bidirectional entry 注册 bidirectional adapter → `FakeBidirectionalError`。∴ **A/B 共同的前置 = 先把该 entry 经 reviewed overlay 裁决升级为 `bidirectional` capability + 重生成 manifest**（overlay/manifest 是共享文件、d4-9 owned，且升级后须 OO 运行时浏览器实测 409/双向，本机 `D:\DeepHorness` 缺）。**结论：D4-1 自己的双向回写在本会话无法交付**——它结构性地被三件事门控：(1) 共享 entry capability 升级(reviewed overlay+manifest regen，d4-9 owned/shared) (2) RG-4 禁第二 adapter(Path A 必须单 adapter 双 sheet) (3) 前端宿主须声明真 sync-host mount(现仅 legacy OO)。这三件都跨 d4-1 spec 边界 + 碰共享/并发文件 + 需本机没有的 OO 运行时。已交付的 10/12 后端纯决策/IO（`d4_formula_cell_verdict`/`d4_1_tb_publish`/`_d4_import_export` D4-1/公式预设/projection provider/descriptor）是能独立落地的全部；双向 UI 链（2.1b/5.2）确属 d4-9 统一路径，无捷径。
  - Task10（IO 修）已交付：`_d4_import_export.py` D4-9 表头改 `期间/序号/客户名称/销售金额/销售金额占比/销售数量/销售数量占比/上期排名`（此前只有 4 列走 `_parse_generic_row` 必错）；`_export_d4_9_rows`（本期/上期两分区+销售总额行+占比计算值不可回导）+ `_parse_d4_9_row`（期间列判归属/识别总额行/忽略派生占比）+ `_assemble_d4_9_payload`（→`{current,prior}` 补 rowId 与前端 `d4CustomerRowIdentity` 同源）。守卫 `test_d4_9_task10_import_export.py` 11 绿。
  - Task9 核心已交付（`phase5_d4_customer_structure.py` §7）：Req5.1/5.2 由平台级 `WpFormulaService`（wp_formula upsert + ACNR full_resolve + 中文错误）+ `_mark_user_formula_cell` 承担，D4-9 直接继承不重复；D4-9 特有 = `runtime_protected_cells`（contract formula_mask ∪ footer 合计 cell ∪ 运行时用户公式 cell）+ `is_cell_protected`（归一化 $/大小写）+ `formula_lineage()`（占比→C/$C$24·合计→SUM·总额←D4-7!D26/L26）。守卫 8 绿。✅ **Task5 `publish_definitions` 亦交付（design §3 deliverable，8 例守卫用 FAKE 内存 publisher `.sha256=canonical_digest` 与生产同函数，PASS 6+MUTATION 2 错 digest→单向引用断裂）；至此 D4-9 bridge 的 design §3 代码交付物全齐（instrumentation_specs/三 payload/assert_entry_selectable/publish_definitions/attach_adapters/投影合并/region_bindings），只差 attach 的 DB/OO runtime 驱动。** ✅ **Task5 `assert_entry_selectable` 已交付（新会话，8 例守卫，独立复验全 D4-9 套件 96 passed/1 xfail）**：`phase5_d4_customer_structure.py` 补 design §3 列明的 fail-closed 选型门（镜像 D4-2 但绑本模块无空格模板路径），对内存现建 manifest 跑真门（PASS 2 + FAIL-CLOSED 6）。🔴 **顺带修真缺陷**：`WP_CODES` 原 `{'D4O'}`（D4-2 幻影码复制残留）与 reviewed overlay 权威 `["D4-9"]` 不符 ⇒ 门永久 fail-closed；按真源优先级改 `{'D4-9'}`，契约 digest `dc3337a3→3f251a70` 重生成磁盘 json，变异 5 锚点无漂移。教训：镜像 sibling 常量前必核对 overlay/manifest 权威值，勿盲抄。✅ **bridge↔引擎缺口已闭合（2026 新会话）**：新增 `test_d4_9_task9_merge_protection.py`（21 例，独立复验 29 passed）把 D4-9 真契约喂进**真实 merge 引擎** `merge.ContractIndex(contract).resolve(key).protection_policy`（非 bridge standalone 函数），行为级证明 D/F 占比列本期+上期两区判 `read_only_formula`(`.is_protected=True`) · 业务列 B/C/E/G 判 `editable` · totals 标量判 `editable` · 引擎 `_FieldTemplate.formula_mask` 与契约逐字一致；变异 2 条（D 列 formula→editable+移出 mask 变 editable / 业务列 C 塞进 mask 变 `read_only_masked_cell`）证非重言式。无生产缺陷（引擎既有分类器已正确处理 D4-9）。🔴 **仍剩**：per-cell **运行时**用户公式注入（`wp_formula` DB → 请求期注入 merge 保护集）依赖 live adapter（需 DB bundle+真实 OO），不接以免死代码。保护集**注入 materialize/extract** 待 Task7。：D4-9 单 sheet 双动态行区（本期 R13-22 + 上期 R27-36）+ 4 表级标量（C24/E24/C38/E38）接统一双向路径。🔴 **design §2.1.1 前提有误**——`instrument_workbook_bytes_multi`/`_inject_managed_sheet` **从不存在**（grep 0），唯一注入器是单区 `instrument_workbook_bytes` 且硬编码单 table part `tableGtRowId.xml`/rel `rIdGTTBL1`+「重复注入即拒」门 ⇒ Task1 是**新增加法式多区注入器**而非改一行 `<tableParts>`：`_attach_table_part(...,rel_id=)` 已有块时追加 `<tablePart>`+count++（单区 358 工作簿字节不变，双证）、新增 `instrument_workbook_bytes_multi`+`InstrumentedWorkbookMulti`（同 sheet 多区共享 `_GT_SYNC`+行级键 `__{entry_id}` 后缀+per-region `tableGtRowId{N}.xml`/`rIdGTTBL{N}` 合并进一个 `<tableParts count="2">`）。真实模板实测 openpyxl 同认 `GT_D49C_ROWS`(A13:W22)+`GT_D49P_ROWS`(A27:X36)。Task2 前端 rowId 迁移抽单一真源 `d4CustomerRowIdentity.ts`。Task3 新建 `phase5_d4_customer_structure.py` 3-table contract（current/prior 动态行 + `customer_totals` 静态标量 row_scoped=False/static_row 24·38/无 formula_mask）+ 生成器 + 磁盘锁 `d4.customer_structure.json`（parse_contract 真跑过）。Task4 `{current,prior,totals}` 嵌套投影/合并（build_combined_store_projection 三 table 合并·totals row_key=None·merge_projection_into_store 按 table_key 分流·protected 占比不回写·区域内 rowId 唯一同名跨区允许）。守卫 **33 绿**（后端 27 + 前端 6，全行为级）。🔴 **真实模板名 = `D4收入底稿.xlsx`（无空格！D4-2/3 reference 模块写的 `D4 收入底稿.xlsx` 带空格是错的）**，sha256 `ecac5d56…`；**C24/C38 总额是已存在的跨表物理公式** `='[9]毛利率分析表D4-7'!D26/L26`（非空白标量，强化 Req5.5 需保护）。**剩 Task5-15**（registry attach 需 Task6 manifest entry `xlsx/gt-d4-customer-structure`——大型共享生成文件属已归档 closure spec，是本会话停点 / materialize·extract / 前端宿主换 unified host + 删 legacy GtOnlyOfficeSheet / 用户公式 wp_formula+保护区+血缘 / IO 修 / 变异 / Playwright）。
- 🔴 **`work/2026-05-30-wp-specs` 分支后端曾起不来，本会话已修**：`wp_user_formulas_v2.py` import 的 `app.services.workpaper_capability`（`CapabilityPrincipal`/`build_capability_snapshot`/`assert_snapshot_action_allowed`）从未提交（`git log --all` 0 命中）→ `import app.main` ModuleNotFoundError。本会话新建 fail-closed 实现（接口由两处 call site 完全确定，`??` 未 commit）解阻。另：`onlyoffice_excel_instrumentation_gate.json` committed 基线 digest 与磁盘 carrier_contract/fingerprint_module 漂移（refresh 脚本 `refresh_excel_instrumentation_gate.py` 亦未提交）⇒ `gate.load()` stale、Task17 全套 11F/54E（与本会话无关）；D4-9 模块 `excel_carrier_gate()` 用 tmp 重建基线兜底（仅兜 digest 漂移，不改 probe_verdict）。
- **`d4-dual-mode-formula-governance` 已 7/7 全交付（2026-09-13，产物已 `git add` 未 commit）**：这是 36 底稿 D4 治理总纲，本质是**冻结契约 + owner 矩阵 + 行为守卫**，非从零建（多数能力已由兄弟 owner spec 建好，design Req1.2 禁重复立项）。产物 = `backend/data/d4_{owner_matrix,sync_contract_frozen,formula_contract_frozen,linkage_platform_contract_frozen,c4_acceptance_registry}.json`（5 份真源）+ `backend/scripts/check/check_d4_{owner_matrix,sync_contract,formula_contract,linkage_platform_contract,c4_acceptance}.py`（5 守卫，**全反 fiction**：引用的类/函数/测试/模块必须命中源码，非仅 JSON parse）+ `backend/tests/d4_governance/`（3 文件 64 例，含变异反向自检）+ CI job `d4-dual-mode-formula-governance`。🔴 **一条真缺口 + 一处自我更正**：①（真缺口，**发布侧已收口 2026-09-13**）D4-1 TB 回写原走 fire-and-forget CustomEvent（`d4:writeback-trial-balance`）无确认/幂等。已把 `useD4Adjudication.publishAdjudicated()` 升级为**人工发布动作**：返回 verdict(`published`/`needs_confirm`/`skipped_idempotent`/`no_project`)，差异超阈值(`PUBLISH_DIFF_THRESHOLD=1`)→needs_confirm(组件 `onPublishAdjudicated` 弹 ElMessageBox 二次确认)、发布快照签名幂等去重(防重复点击写两次)、仅按钮显式触发不随模式切换(Req4.2/5.1)；测试 `d4AdjudicationPublish.spec.ts`(5 例含反向自检)+66 D4 前端例全绿。**唯一剩项 = HTML↔Excel OO 编辑器 roundtrip 经 `useWorkpaperSyncBridge`**（真需 OO/DeepHorness 运行时，本机缺，归 owner spec Task2.1b，须与 d4-9 统一双向路径合并推进，与全部 36 D4 sheet 共用 `d4:save-items` 保存链单切风险高）。每 cycle 各有自己的 `publishAdjudicated`（useK5/H9/... 独立实现），改 D4 的不波及他 cycle。②**（自我更正 2026-09-13）初版把 `user_formula_v2.DEFAULT_V2_STORE`(in-memory) 误当 D4-1 公式定义 SSOT 登记成缺口——错了**。D4-1 公式定义 SSOT 真身 = `backend/app/services/formula_management/{effective_formula,preset_library}.py`：`resolve_effective_formula` 五元键 `wp_id∷stable_sheet_key∷row_key∷field_key∷custom_suffix` + 6 态(custom/preset/preset_missing/corrupt/stale/blocked) + 白名单(禁 eval/外链/属性穿透)，**已持久化**（seed `backend/data/formula_presets/formula_presets_seed.json` + custom `formula_custom_presets.json`，`preset_library` 三源读时收敛），custom 优先回落 preset、升级保留 custom、删 custom 自动回落——正是用户要的「预设+二次编辑单一真源」，且 **owner spec Task3.1 已交付**（12 条 D4-1 预设入 seed，39 守卫测试）。`user_formula_v2` 是**独立的公式工具栏批量改路径**（in-memory，非定义 SSOT），契约已拆成 `definition_ssot` + `user_formula_toolbar_v2` 两节，`ssot_status.d4_1_definition_ssot_persisted=true`。教训：**登记缺口前必先确认「谁才是这功能的真源」**——同名/近义模块（user_formula_v2 vs formula_management）易张冠李戴，sub-agent 初查也漏了 effective_formula。踩坑：`validate_formula` 的未知函数正则 `[A-Z_]+\(` **只认大写** ⇒ 小写 `eval(` 不被判未知函数（no-eval 真实机制 = 引擎只派发大写注册函数 + 自带危险 token 黑名单 `'eval('/'exec('/'subprocess'` 等，非靠拒绝 eval 字面量）；`fs_write` 顶层键名带 `$` 前缀（如 `$schema_note`）会触发 Supervised 模式的 Remote JSON Schema 写保护而失败，改普通键名 `note`。
- **`d4-cutoff-return-writeback-formula-io` 进行中 4/7（Task1/2/3/6 交付，未 commit，产物 `??` 待 add）**：D4-17/18/19/20 IO 原本全坏——四表无专用 parser 全落 `_parse_generic_row` 写中文 key、前端英文 key 读不出（导入写孤儿、导出导空）。修法=新建单一真源 `backend/app/routers/wp_render_strategies/_d4_cutoff_return_io_spec.py`（列头↔英文 key↔派生↔方向，源锚点 `wp_templates/D/D4-13至D4-20…检查.xlsx`）驱动 `_d4_import_export` 的 `_parse_cutoff_return_row`/`_export_cutoff_return_row`/`_recompute_derived`。**D4-20 主 sheet 是死配置**（六区无单一存储）→ 从 `_SUPPORTED_SHEETS` 删 + `_validate_sheet` 显式中文拒绝；**item_id 错配**修正 `D4-20-current→D4-20-current-returns`/`-post→-post-returns`（前端存这俩键）。派生 `isCutoff/discountRate/shouldProvide/diff` 忽略文件值由公式重算。截止 √/× 收敛单一真源 = `useD4FormulaEngine.isCutoffOk`(前端)↔`_cutoff_is_ok`(后端)同定义，两 Tab 本地 `checkCutoff` 改委派；`cutoffDate` 落库 `D4-1{7,8}-cutoff-date` 让 HTML/IO 重算共用参数（Req2.3 非跨期不恒相反）。四 Tab 导入后 `reloadWorkpaperData()` 重载。守卫 `backend/tests/test_d4_cutoff_return_io.py`（21 绿，`_cutoff_is_ok` 取反变异 2 例 RED 已验）+ `useD4FormulaEngine.spec.ts` isCutoffOk 5 例。**剩 Task4/5/7**（发现→A13 人工确认门复用 `a13:push-misstatement` EventBus + 二次确认 + `a13PushedAt` 幂等，K8/K9 cutoff 是范式 / durable ack 幂等去重防回环 / Playwright 双模式真栈）。挂靠总纲 `d4-dual-mode-formula-governance`（同族 8 个 D4 spec，兄弟 `d4-inspection-writeback-formula-io` 也 0/7 无现成可抄）。
- **`d4-1-adjudication-bidirectional-writeback-and-formula-io` 进行中 10/12（2026-09-13 本会话续，未 commit）**：D4-1 营业收入审定表 HTML/OO 双向回写 + F-SHELL 公式 + 导入导出。**本会话续做 Task 3.2/4.1/4.2/5.1/5.3 全交付（均纯后端决策/IO 不碰共享保存链），2.1b/5.2 有意驻留（tasks.md 写明阻塞），2.1 父任务待 2.1b）**。✅ **Task 4.1**（子代理交付后本会话复验）：`_d4_import_export.py` D4-1 十列（加「科目码」）+ `_build_d4_1_import_items`（`D4-1-rows` 清单 + per-field 键去重、不写旧 blob）+ `_load_d4_1_rows_for_export`（新键优先回退旧 blob）+ `_d4_1_account_code_status` 三态 + 从 `_SPECIAL_ITEM_IDS` 移除 D4-1；`test_d4_1_import_export.py` 23 绿。✅ **Task 3.2**：新建 `formula_management/d4_formula_cell_verdict.py`——`FormulaCellVerdict` 是 HTML/OO **共用唯一裁决**，`verdict_from_evaluation` 把 `effective_formula` 态 + 底层求值器 `(value,errors)` 裁决为统一结果，errors 非空/依赖失败/corrupt/blocked/stale 一律 `value=None,applied=False`**绝不转零**（丢弃求值器的 `Decimal('0')` 兜底），仅 `ok` 写值；`build_source_tooltip` 单一口径中文 tooltip；`test_d4_1_formula_cell_verdict.py` 21 绿 + 失败转零变异 RED。🔴 **底层 `wp_formula_eval_service.evaluate_wp_formula_expression` 失败时 `return Decimal('0'), errors` 内部转零** —— 直接投影它会把「求值失败」伪装成「结果=0」；render 路径 `tier_a_seed.seed_tier_a_reconciliation` 已 `if errs: skip 不落 0`，但任何新消费者必须走 verdict 层裁决、不能直接 apply 它的裸返回值。✅ **Task 4.2**：新建 `d4_extraction/d4_1_tb_publish.py::decide_d4_1_tb_publish` 纯决策 SSOT——`trigger=='mode_switch'` 恒 `blocked_mode_switch`（优先级最高，Req1.5）/ 只读 `blocked_readonly` / `|快照审定合计−TB核对值|>0.005` 未二次确认 → `needs_second_confirm`（取消不写）/ 通过 `publish` 发布**快照拆分金额 6001/6051 不偷换 TB** / 非法入参 `invalid` fail-closed；真实回写仍走 `PUT /trial-balance/writeback`；`test_d4_1_tb_publish.py` 15 绿 + mode_switch 短路变异 RED。✅ **Task 5.1**：**135 例**后端行为守卫按七维分布各带变异 RED（此前虚标 205 系混入其他 spec 计数，2026-09-13 实跑修正）。🔴 **2.1b 标记伪绿已修**（tasks.md 原标 `[x]` 但正文写「有意驻留」，已改 `[-]`）。✅ **Task 5.3**：三件套对齐 Kiro Spec Format（requirements 补 `# Requirements Document`/`## Introduction`/`## Glossary`/`## Requirements` + 每 Requirement `**User Story:**`+`#### Acceptance Criteria`；design 补 7 标准 section + Property 加标题；tasks 补 `# Implementation Plan:`/`## Overview`/`## Tasks`/`## Notes`），`get_diagnostics` 三件套 0 error。**本会话实测全栈在线**（后端 9980 / 前端 3030 / OnlyOffice healthy）但 D4-1 双向 UI 未接（2.1b 驻留）故 5.2 端到端无法驱动。**未 commit 产物（`??`/M）**：`d4_formula_cell_verdict.py`/`d4_1_tb_publish.py`/`_d4_import_export.py`(M) + 3 测试 + 三件套(M)+evidence。**—— 以下为本会话早前 Wave 交付：✅ Task 3.1**（F-SHELL 公式治理，纯后端 seed 不碰共享保存链）：复用并发会话建的 `app/services/formula_management/effective_formula.py`（五元键 `wp_id∷sheet∷row∷field∷custom`+6 态+custom 优先/删回落 preset+白名单 `TB/TB_SUM/SUM_TB/WP/ADJ/PREV/AUX/SUM/cell/ABS/MIN/MAX/ROUND` 禁 eval/外链），预设读 `preset_library`（三源 custom∪seed∪prefill/check/wide_table 按 `(page_key,target_cell)` 去重）；在 `backend/data/formula_presets/formula_presets_seed.json` 补 **12 条** D4-1 预设（连原「审定数」共 13 条，`page_key:workpaper:D4`，target_cell 用语义名带 `D4-1-` 前缀防跨循环撞键：上期审定数/主营·其他小计/营业收入合计/TB核对(6001·6051·合计,`TB('6001','发生额')` 损益取发生额)/差异数/未审·审定变动率/D4-2·D4-3 明细 WP 引用），expression 全过白名单、全解析 `state=preset`；守卫 `backend/tests/formula_management/test_d4_1_formula_presets.py` **39 绿**（逐类覆盖+唯一性+resolve+变异 RED）；全量预设库 **4239 条 0 撞键**。**⏳ 下一步 Task 4.1**（导入导出键对齐：`_d4_import_export.py` 的 D4-1 用旧 `D4-1-adj-rows` blob 键与前端 per-field 分裂，需对齐+缺 accountCode/scope 拒绝或人工+派生列只重算，纯后端不碰共享保存链）。✅ **Task 2.1a**（后端 projection provider，独立新增不碰并发前端）：`backend/app/services/workpaper_sync/phase5_d4_operating_revenue.py` 仿 d4-9 范式建 **单 sheet 3 table**（`main_rows` R8:R11 + `other_rows` R14:R17 + `tb_check` 表级标量 R20，`STORE_ITEM_ID="D4-1-adj-store"` 嵌套 `{main:{rows},other:{rows},tbCheck:{tb6001,tb6051}}`）；审定列 E/I 进 `formula_mask` protected 不回写；`build_operating_revenue_store_projection`/`merge_projection_into_store` 纯函数（fail-closed 行身份：缺/重复 rowId 抛 StorePayloadError）；磁盘锁 `data/workpaper_sync_contracts/d4.operating_revenue.json`（canonical_digest `e562361e…`，`TEMPLATE_SHA256=fa69c92d…`）+ 生成器；`test_phase5_d4_operating_revenue.py` **14 绿**（变异删 is_protected skip → protected 测试精确 RED 已验）。**关键调研结论（复用清单）**：`useD4EntryDualMode` 名不副实（只是 HTML/OO 视图切换+健康探测，不接 ContentMutationService）；真正统一回写 = `ContentMutationService.stage_html_projection`+`commit_html_projection`（single_html lane，经 `wp_html_save.py` 端点，CAS `bump_content_revision`+单事务+`RevisionConflictError`→409 fail-closed）+ 前端桥 `useWorkpaperSyncBridge`（`flushHtml` 交回 `{expectedRevision,projection}`，桥不拼 endpoint）+ `read_store_projection` provider 链（store-backed entry 服务端现算）；entry 复用现有幻影 `xlsx/gt-d4-operating-revenue`。**⏳ Task 2.1b 待做**（前端桥接入：`useD4Adjudication` 的 `debounceSave`/`d4:save-items` last-write-wins 直写 → 经 `useWorkpaperSyncBridge`→`commit_html_projection`，store 键迁 `D4-1-adj-store`；改 `useD4Adjudication.ts`/`GtD4OperatingRevenue.vue`——并发在编辑需确认停手）。**🔴 2.1b 架构评估（2026-09-13）：D4-1 保存链与全部 36 个 D4 sheet 共用 `d4:save-items`→`useD4FormData.saveBatch`→`PUT /checklist-responses`（无版本校验 last-write-wins），单独把 D4-1 切到 sync bridge 需在宿主层按 sheet 隔离、大改这条公共链风险极高；且 `useWorkpaperSyncBridge` 走双向 OO↔HTML entry 路径（`pending-mutations`/`materialize`）需 manifest 注册+Excel instrument+OO 健康，而 D4-1 现是纯 HTML sheet（走 `checklist-responses` PUT 非 `wp_html_save.py`）；冲突 409 fail-closed 须浏览器实测但本地 OO 环境不具备（`D:\DeepHorness` 缺）。→ 2.1b 应与 d4-9 的统一双向路径工作合并推进（d4-9 剩 Task5-15 含「前端宿主换 unified host+删 legacy GtOnlyOfficeSheet」正动同一条链），不单独切一条会冲突的路径。本会话转做 Wave3/4 中不碰共享保存链、可独立交付的后端部分。**—— 以下为早前 Wave 交付：✅ Wave1 全交付（Task1.1 openpyxl 核定权威模板 `D4-1至D4-4…审定表明细表…xlsx`：**真实 tab 名 `营业收入审定表D4-1`**（非裸 `D4-1`）、6 输入列 B/C/D(本期未审/账项/重分类)+F/G/H(上期)、审定 E/I 是 `=SUM(本期/上期三列)` 派生公式列、48 公式格、主营段 R7 标题+R8:R11 四行可扩+R12 小计 / 其他段 R13+R14:R17+R18 小计 / R19 合计 / R20 试算平衡表数 / R21 差异 `=E19-E20`；产物 `evidence/d4_1_{descriptor.md,template_facts.json}` + 后端单一真源 `app/services/d4_extraction/d4_1_descriptor.py`；Task1.2 契约测试 `backend/tests/d4_extraction/test_d4_1_descriptor.py` 13 绿 openpyxl 三向比对+反向自检）。✅ Wave2 Task2.2（render 缺码改可见拒绝/人工：新增纯函数 `classify_d4_source_status(scope)` 三态 ok/manual/blocked + `project_context.tb_source_status` 下发 + `test_d4_1_source_status.py` 11 绿含反向自检；`tests/d4_extraction/` 93 绿、`import app.main` OK）。🔴 **Task2.3 停手**：查工作树发现**并发会话正在改一大批 D4 共享文件**（`useD4Adjudication.ts`/`dynamicAdjudicationRows.ts`/`useD4FormulaEngine.ts`/`useD4ImportExport.ts`/`_d4_import_export.py`/`_d4_operating_revenue.py`/`account_scope.py` + 多个 d4-* spec tasks.md），且 `DynamicAdjRow` 的 `sectionKey` **已被并发会话补上**（我上一版读到没有、这一版有）——Task2.3 缺口可能已被并发解决。**同族 8 个 d4-* spec 多会话并行是常态，d4-1 的 Wave2/3/4 要改的正是这些被并发编辑的共享文件 → 不与并发并行改同一文件**（memory 铁律）。已完成的 Wave1 descriptor + Task2.2 状态判定是**独立新增文件**（`d4_1_descriptor.py`/两个 test/evidence），未与并发冲突。待用户协调后再推进。
- **`g7-column-alignment-and-extraction-closure` 已 24/24 全交付并归档**（2026-08-15 → `_archive/08-disclosure-notes/`，commit `83ccf630`，分支 `work/2026-08-12-g7-column-alignment-closure`）：56 偏差点→0；补第三边（源xlsx↔seed↔运行时）+ **新增第四边「渲染层」**（两级表头 0/38→20 张）；契约 11→38 张；2 个 CI job；变异 **15 锚点**全 RED；前端 887 passed / 后端 G7 主域 2297 passed。收口复盘追加修 **fail-open**（两个披露 Tab 的 `syncToDisclosureNotes()` 真同步与收尾共用一个 `try`，失败被成功文案盖掉 ⇒ 拆两段 + 失败 `return`，文案下沉 `g7DisclosureSyncFeedback.ts`）。**16 个新文件已入库，此前登记的「10 个正式产物 `??` 未跟踪」已解除。**
- **`e-cycle-extraction-formula-and-disclosure-completion` 已 24/24 全交付并归档**（2026-08-15 复盘后 → `_archive/08-disclosure-notes/`，归档 commit `267111b6`，主交付 commit `2ee7929e`）：账户级取数走 `tb_aux_balance` 的 `aux_type='银行账户'` ⇒ E1-3 出 22 行逐户（此前叶子口径恒 1 行汇总）；浏览器实测抓出两处真实缺陷（① `resolveRestrictedRows` 按数组下标重算 id 覆盖稳定序号 ⇒ 删后再增复用已删序号、历史 reason 串到新类别 —— **已修**；② E1-3 multi 版 variant 切换抹零 —— **登记未修**，见下）；变异 **29 条全 RED**；后端 368 / 前端 568 passed；CI 两个新 job。**复盘沉淀两条通用做法**：变异脚本 `--check-anchors` 是「已归档 spec 是否还可复现」的最便宜判据（只读、秒级、证明 29 处结构未漂移，不必重跑全量变异去改带并发在途改动的文件）· 工作树 dirty 文件**必须逐个 diff 归因**（`E1TabDisclosure.vue` 名字是 E 循环的却是并发 K 循环改的）。
- 🔴 **E 循环遗留缺陷（未修，非该 spec 引入，建议单独立任务）**：E1-3 先点`仅人民币`再点`人民币及外币` ⇒ 22 行金额全变 `-` + 一条错的「审定合计 0.00 ≠ TB」横幅。根因 = 宿主 `seedFromFourTable()` 只在 `onMounted` 跑一次且 `seedRowsKey` 有 persist-first 短路，种子形态一会话内只按当时 variant 定型；`recalcRow(row,'multi')` 又把六个金额全由「原币 × fxRate」派生，rmb 形态种子无 `openingFc` ⇒ 恒 0。29 条变异 + 568 例守卫全没拦住，因为**没有一条把「种子的 variant」与「消费它的 Tab 的 variant」串起来断言** = 假绿第①源第三种变体。改法要动种子键按 variant 分离 + 新守卫。可与「E1-10 交叉核对把科目码当账号比对」并入存量口径回填 spec。
- **Active spec = 6 个带 tasks.md + 2 个空壳**（🔴 数字一律以 `.kiro/specs/` 复选框实扫为准，行首锚定正则 `^\s*-\s\[([ x~-])\]\s+\d+\.`，别信 memory 旧数）。2026-08-15 实扫：`i-cycle-…closure` **23/24** · `k-cycle-…closure` **19/25** · `l-cycle-…completion` 3/26 · `procedure-trim-report-line-account-resolution` 0/16 · `workpaper-import-export-lifecycle-closure` 24/25 · `x3-adjustment-entry-import-export` **33/53**（`e-cycle-…completion` 24/24 已归档，见上）。空壳 2 个（`procedure-delegation-visibility-isolation` / `visibility-isolation-go-live-hardening`，git 里 `??` 未跟踪，2026-08-12 曾误记为「已清理入归档区」）⇒ **报 Active 数必须区分「目录数」与「带 tasks.md 的 spec 数」**。Archived **553**。
- 🔴 **4 个在办 spec 目录在 git 里全是 `??` 未跟踪**（2026-08-15 归档 e-cycle 时 `git status` 实录：`i-cycle` / `k-cycle` / `l-cycle` / `x3`，连同 2 个空壳）⇒ **丢工作树即全部蒸发**，且 INDEX/memory 登记的进度数无法从 HEAD 复核。已归档 spec 不受影响（`_archive/**` 已跟踪）。建议各推进方尽快把自己的 spec 目录入库。
- **`d4-adjustment-and-analysis-gap-closure` 本轮交付 Task 3（C2formula backbone）**（2026-09-13，未 commit，3 后端产物+2 测试全 `??`）：🔴 根因—前端 formula shell 挂载拉 `GET /api/workpapers/{wpId}/capability-snapshot` 但**后端从没这个端点** ⇒ 整个公式/AI/复核壳层 fail-closed 到 `snapshot_uninitialized`，公式管理器/右轨/双向回写触发器全打不开。补 `app/services/workpaper_capability_matrix.py`（9 键 role→capability 真源 fail-closed + owner-epoch 竞态门 + 拒绝文案不泄敏，角色归一化 admin/partner·manager·qc·eqcr→reviewer/auditor→assignee/readonly→viewer）+ `app/routers/wp_capability_snapshot.py`（`capability-snapshot`/`capability-assert`，已注册「数据」组），**组合**并发会话新建的 `workpaper_capability.py`（`??`）而非改它。有效公式单一真源 = `app/services/formula_management/effective_formula.py`（key=`wp_id∷stable_sheet_key∷row_key∷field_key∷custom`；6 态 custom/preset/preset_missing/corrupt/stale/blocked；custom 优先·删 custom 回落 preset·预设升级不覆盖 custom；schema 白名单禁 eval/外链只放行 TB/WP/ADJ/PREV/AUX/SUM/cell/ROUND）。守卫 13+22 全绿、前端 shellTask12/workpaperCapabilitySnapshot 13 绿。双向回写入口级 `useD4EntryDualMode` 已覆盖 D4-4/8/12（深度 unified-path 归 `workpaper-html-onlyoffice-bidirectional-writeback-closure`）；导入导出 D4-4/8/12 已在 `_d4_import_export.py::_SUPPORTED_SHEETS`。**Task 1/3/4 已交付，Task 5 部分**（Task2 `[-]`=入口级已覆盖+深度归他 spec；Task5 `[-]`=待 Playwright 全栈实测/CI/入库）：Task1 owner 去重实证—三表结构均归归档 spec（D4-4/8=`d4-operating-revenue`+`workpaper-adjustment-centralization`；D4-12=`d4-12-contract-inspection`），本 spec 只 own backbone 层。Task4 公式不发布 TB/A13 守卫 `test_d4_gap_formula_no_tb_a13_publish.py`（源码禁词+行为 audit kind 双判据，**变异 RED 已验**）。Task5 端点集成守卫 `test_wp_capability_snapshot_endpoint.py`（ASGITransport 真路由，代替浏览器后端半）。**6 个 `??` 待 add**：`workpaper_capability_matrix.py`/`wp_capability_snapshot.py`/`effective_formula.py` + 3 测试（`test_workpaper_capability_matrix.py`/`test_effective_formula.py`/`test_d4_gap_formula_no_tb_a13_publish.py`/`test_wp_capability_snapshot_endpoint.py`）。✅ **Playwright 实测通过**（2026-09-13，`python -m uvicorn app.main:app --port 9980` 起后端 + 前端 3030）：打开 D4 底稿（wp `c581bffb…`/proj `5e193c68…`）→ `capability-snapshot?ownerEpoch=1` **200 ×2**，data 九键 decision 全 allowed（admin），「公式管理/批量AI复核」按钮渲染启用（未 fail-closed）。**wp_index 无 FK 到 working_paper**（真实库列=id/project_id/wp_code/wp_name/audit_cycle/…，无 wp_id）；join 走 `working_paper.wp_index_id = wp_index.id`（`working_paper` 有 wp_index_id 列）。会话内 session 工厂 = `app.core.database.async_session`（非 `app.database`）。✅ **全部入库 commit `69f1ced3e`**（分支 `work/2026-05-30-wp-specs`，11 文件 1384 insertions）：含 CI job `d4-gap-formula-capability-backbone`（governance-checks.yml，43 例）+ 依赖的 `workpaper_capability.py`。🔴 **并发会话另建了互补 C4 契约层**（`backend/data/d4_owner_matrix.json`/`d4_*_contract_frozen.json` + `backend/scripts/check/check_d4_*.py` + `backend/tests/d4_governance/`，其 frozen 契约断言 `resolver_fn=="resolve_effective_formula"` + 6 态逐字一致 = 两会话独立收敛到同一设计）——**未纳入我的 commit**（他人在途，`git add` 时用 `git reset HEAD` 摘出，只提自己 11 文件）。教训：`rtk git add <显式清单>` 时若并发会话已把文件 staged 进 index，仍会一起进 commit → **commit 前必 `git diff --cached --name-only` 核对，多出的用 `git reset HEAD` 摘除**。d4-adjustment-and-analysis-gap-closure Task 1~5 本会话侧全 `[x]`（Task2 入口级+深度归他 spec）。✅ **已推远端**：`work/2026-05-30-wp-specs` 本地比其远端**超前 145 提交**（几乎全是并发会话在途，只 1 个是我的）→ 直接 push 会推一堆他人未推提交，不安全。我的 commit 父提交 `0bedc5e1a` 已在远端（`origin/work/2026-09-13-adjustment-created-emit-parity` 等）→ 建**只含我 1 提交**的专用分支 `work/2026-09-13-d4-gap-formula-capability-backbone`（`git branch <新> <commit>` + `git push -u`）推上去（干净单提交 delta）。教训：长命名 work 分支超前远端几百提交是常态，push 前必 `git rev-list --left-right --count origin/<b>...HEAD` 看真实 delta，超前数十以上就别推原分支、改建单提交隔离分支。🔴 **`gh` CLI 未登录**（`gh auth login` 需交互），PR 只能走 web 链接 `github.com/YZ1981-GT/GT_plan/pull/new/work/2026-09-13-d4-gap-formula-capability-backbone` 或用户先 auth 后我 `gh pr create`（base 选 `work/2026-09-13-adjustment-created-emit-parity` diff 才只 1 提交）。🔴 `formula_management` 既有套件 **21 例失败为既有环境失败**（`no such table: working_paper` DB fixture + `inventory.json` 232≠249 漂移，并发会话加 `note_formula_derivation` 预设没重生成），与本轮无关（只加文件没碰 `preset_library.py`/预设数据，`git status` 已证）。
- **🔴 run-all-tasks 编排器累计逾二十次均判「不接管」**（最近 2026-08-09）。判据三条，任一不满足即不启动子代理：①本会话是否真发起过批量执行（用户说「继续」≠「run all tasks」，编排器只恢复被中断的批量、不替用户发起）②带 `[-]`/`[~]` 的任务，其 tasks.md 正文是否写明了阻塞原因（写明=有意驻留，如 `h-cycle` Task18「浏览器实测待做」；未写明才可能是被中断）③该 spec 的 tasks.md mtime 是否秒/分钟级（=并发会话正在写，勿碰）。
- **git/迁移**：当前分支 `work/2026-08-12-g7-column-alignment-closure`（并发会话另在 `work/2026-05-30-wp-specs` / `work/2026-08-12-e-cycle-extraction-closure`）；最高迁移 **V146**（2026-08-15 实扫 `backend/migrations/V*.sql` 得 146 个文件、最大号 146，`R1xx__` 是配对回滚脚本非同号冲突）；新迁移须重启后端才应用（MigrationRunner 只在启动时跑）；**push 前必先 fetch 看远端真实 base**（memory 里的旧 commit hash 会过期）；协作走 PR 不直推 main。
- **并发多会话现状**：工作树长期不干净、根目录 `tmp_*`/`backend/scripts/diagnose/_wip_*` 多为他人在用（按 mtime + 内容关键词判归属，勿按文件名里的任务号误删）；同一文件禁与并发会话并行编辑；判 spec 是否真完成一律**工作树 + HEAD 双查**（防「从未提交」与「被回退」两类假红）。
- **🔴🔴 最新几条 work 分支后端起不来（2026-09-13 同步实证）**：分支时间线 `origin/work/2026-09-11-report-config-preset-materialize`(09-11) → `2026-09-12-d-cycle-bidirectional-canaries`(09-12) → `2026-09-12-four-table-extraction-entry-completion` / `formula-row-name-alignment`(09-13) → `2026-09-13-d4-4-single-html-adjudication` → `2026-09-13-adjustment-created-emit-parity`（最新，含前者）。**从 `d-cycle-bidirectional-canaries` 起**引入提交 `acb763d0f`「公式管理壳层与 F-SHELL 里程碑」，其 `wp_user_formulas_v2.py` import `app.services.workpaper_capability`（`CapabilityPrincipal`/`assert_snapshot_action_allowed`/`build_capability_snapshot`）—— 该模块**从未提交到 git**（`git log --all` 0 命中，符号全后端不存在）→ `import app.main` 抛 `ModuleNotFoundError`，整个后端起不来。**最新的可运行分支 = `origin/work/2026-09-11-report-config-preset-materialize`**（不含坏提交）。判据：`git merge-base --is-ancestor acb763d0f <branch>` exit 0 = 含坏提交。`main`/`master` 已废弃（2026-06-01/05-25，落后主线 899 提交），团队主线是 work 分支流。**同步后先跑 `python -c "import app.main"` 冒烟，起不来先 `git log --all --oneline -- <缺失文件>` 判断是「本分支太旧缺文件」还是「上游 WIP 真断裂」**。**2026-09-13 二次拉取到 `0bedc5e1a`（`feat(formula): publish F-SHELL milestone artifacts` + D4 dual-mode 治理 spec）仍未补 `workpaper_capability.py`**（`git log --all` 依旧 0 命中）—— 提交名带 "F-SHELL" 有迷惑性但发布的是 `workpaper_sync_program_milestones.json` 等判据文件、不是缺失模块，后端仍起不来，待上游后续补齐。
- **🔴 「spec 全绿」≠「产物已入库」**：多个 spec 的正式产物（守卫测试、诊断脚本、判据 JSON、新建实现文件）长期 `??` 未跟踪 —— G7 收口时实测本 spec **10 个正式产物全未跟踪**，同目录另有他人 20 个 `??` 测试文件。后果两条：①挂进 `governance-checks.yml` 的 job 在干净 checkout 下**必挂**（文件不存在）②工作树一丢全部蒸发。**每个 spec 收口必查 `git status --porcelain -- <产物清单>`**，见到 `??` 即登记待 add。
- **`.gitignore` 已于 2026-08-15 补收 `tmp_*` 与 `_wip_*`**（此前只收带前导下划线的 `_tmp_*`，G7 收口前根目录积了 **487** 个 `tmp_*`）。补之前用 `git check-ignore -v` 验证过 0 个正式产物被挡（`tmpHeadBaseline.spec.ts` / `_wipG7RuntimeProbe.spec.ts` 因无下划线分隔不受影响）。清理时仍按前缀 + mtime 判归属（今日仍有产出 = 并发会话在用，勿碰）。
- **pre-push 的「6 维核查」不达标≠push 有问题**：`.git-hooks/pre-push` 在 `GIT_MODE=single` 下只警告（multi 才阻断）。维度 1（工作树 clean）与 5（untracked 0）在本仓库**长期不可能达标**（并发多会话在途 + 数百 `tmp_*`/`_wip_*`）；判自己的 push 完不完整只看**维度 2/3/4**（本地 HEAD == 远程 / ahead 0 / behind 0）。核查脚本 = `backend/scripts/check/check_git_sync_state.py`。

## 踩坑铁律（完整 500+ 条见 `#conventions`）

> 每写一段代码/守卫前先过一遍下列高频坑；具体某模块的坑在 `#conventions` 对应小节。

- **假绿三源**：①additive 注入即死代码（新加的取数/字段无消费方）②grep 式守卫只查字符串存在（改成 `if False:`/删调用/改名残留仍绿）③守卫把错值当基线锁死（把真源改对反而打红）。→ 判据一律用「行为/结构/真实执行」而非「字符存在」；**每写完守卫必做变异检验**（改一字看是否变红），没打红=守卫有缺陷不是代码没问题。
- **🔴 第①源的最隐蔽形态 = 守卫层级不够，渲染层死代码结构性不可见**：G7 实测「模型声明 `column.group`（三向守卫拿它跟源 xlsx 合并单元格逐张对齐、39 例全绿）而任何 `.vue` 零引用」⇒ 两级表头 **0/38 张从未渲染**，审计师看到 5 组一模一样的 `期末数|期初数` 分不清属于哪家公司。守卫只比「源↔seed↔模型」三层数据，**从不看 DOM**。→ 判「某声明是否真生效」必须落到**浏览器 DOM** 或**模板形态判据**（遍历+外层门控+内层嵌套三要素缺一即红），并把 DOM 结论沉成可测投影当第四边。
- **多 spec 混合文件（`governance-checks.yml` / 共享模板 JSON）的「只加不动」验收必须用归因型判据**（变动是否落在**我的字节区间**内），不能用全局等值型（「其他 job 一个都没变」）—— 并发会话同时改同一文件是常态，全局等值必假红（实测拍快照 144 job → 我 append → 复核 147）。`fs_append` 与并发编辑不互相覆盖。
- **真实库写入/复原三坑**：①timestamptz 回写必须在 **Python 侧**转 `datetime`（备份用 `::text` 导出后回写会被 asyncpg 拒；**SQL 层 `CAST(:x AS timestamptz)` 无效**，驱动发送前就按目标类型编码）②`engine.begin()` 内**一处失败全部回滚**（复原脚本第 3 步抛错把前两步撤回）③**被 `^C` 中断的运行可能已提交部分变更** ⇒ 判成败一律查数据不看 exit code。
- **别跑全量 `backend/tests`**（根目录 **1522** 个测试文件，前台跑数分钟无输出会被当卡死）⇒ 按**引用关系反查辐射面**：扫测试文件里对本次改动物的实际引用（G7 得 77 个文件，2分49秒跑完）。
- **「源侧物理结构」字段不能直接当期望值**：facts 的 `is_two_level` 描述源 xlsx 有几行表头（含父行为空的占位合并），须剔除已登记的 `single_slot_exemption` 才是「应渲染两级」的期望（24 → 20）。同理任何 `_exemption`/`_adjudicated` 登记表都要参与期望值推导。
- **变异检验四态**：RED（打红且正是预期那条测试）/ GREEN（守卫缺陷）/ ANCHOR-MISS（脚本缺陷：锚点未命中或命中 >1，含 `\n` 跨行锚点在 CRLF 必 MISS）/ WRONG-TEST（打红了但不是预期项=污染残留或锚点错行）。只看退出码会把后三态误判成 RED。
- **fail-open 掩盖接线错误（最贵一类）**：`except Exception` 把「函数名/列名拼错、单参调用 async、传错客户端形态」全吞成 WARNING → 表现为「本项目无此数据/静默取空」，而 Volar/vitest/get_diagnostics/HEAD-swap 四层全绿 → 取值层守卫必须**真跑一次并把异常记 ERROR 态**，反向自检要「故意写错必失败」。
- **Vue 传不存在的 prop / 绑不存在的字段** = 静默失效（渲染空串、门控恒开、录入不落库），四层全查不出，只有浏览器挂载才暴露。判「某能力接没接」要落到**唯一消费方 + 有渲染宿主**，不能只 grep 符号名。
- **截函数体禁固定字符窗口、禁 `strip_comments` 剥 SQL**：一律花括号/圆括号配对 + **先跳参数列表**（TS 返回类型注解 `): Promise<{...}>`、Python 多行签名都会骗到「第一个 `{`」）；`strip_comments` 会把 `sa.text("""...SQL...""")` 一起剥掉。
- **磁盘真相与编码**：`read_file` 对并发/自己刚改的文件返回**陈旧版本** → 判磁盘一律 `python -c "open(p,encoding='utf-8').read()"`；PS `>`/`Out-File` 会把 python 的 UTF-8 中文腌成乱码 → 脚本内 `Path.write_text(encoding='utf-8')` 或 `cmd /c "... > f 2>&1"`；含 emoji/中文的破坏性脚本先 `$env:PYTHONIOENCODING='utf-8'`，判成败查数据不看 exit code（`--apply` 常被 Ctrl+C 中断但写入已提交）。
- **pytest 一律从仓库根跑**（从 `backend/` 跑用相对路径的测试会 `FileNotFoundError` 假红 17 例）；连库守卫用**一次 `asyncio.run` 取全部快照**（每测试各自 async 会污染共享连接池，第二个起 `NoneType has no attribute send`）；`-k "a or b"` 经 shell 会被拆成多个位置参数 → 用 `subprocess.run([...])` 不经 shell + 加「passed<N 即中止」自检。
- **改共享数据文件前 grep 其他 active spec 是否也要改它**（`note_template_*.json`/`prefill_formula_mapping.json`/`report_config` 等是回退高发文件）；并发会话会互相回退，**幂等脚本 `--check` + 契约测试**是唯一可靠恢复手段，测试红了先重跑对应 `scripts/fix/fix_*.py`。
- **三件套校验（`get_diagnostics` 对 `.kiro/specs/**/*.md` 生效）**：`### Property N` 只认整数、`**Validates: Requirements X.Y**` 只认 `X.Y`、tasks 的 `## Task Dependency Graph` 必含 waves JSON。除机器校验外还要人工核：**未被引用的 AC**（悬挂 0 也可能整条 Requirement 零实现）+ **design 承诺的新函数/新取值是否真在生产代码 grep 得到**。
- **改 .md 禁用 `index()` 算边界**（会一次删掉整段，Markdown 无结构校验查不出）：一律 `str_replace` 传完整旧文本；非要脚本切片则边界用行首锚点 + assert + 改完做结构核验（`### `/`## ` 计数、字符数、尾部锚点）。**Kiro local history**（`%APPDATA%\Kiro\User\History\<hash>\entries.json`）可恢复误删、也可取证「文件被谁何时覆盖」。

## 关键引用

- spec 状态 → `.kiro/specs/INDEX.md`
- 领域术语 → `glossary.md`（inclusion:always）
- 底稿内容结构权威来源 → `基础数据/致同通用审计程序及底稿模板（2025年修订）/BCD类底稿md/`
- 附注章节号权威源 → `backend/data/note_template_variant_matrix.json`
- 附注 section↔wp 映射真源 → `backend/data/note_workpaper_sync_registry.json`
- 报表科目映射真源 → `report_config` DB 表（非 `formula_presets_seed.json`）
