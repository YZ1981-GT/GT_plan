---
inclusion: always
---

# 持久记忆

每次对话自动加载。详见 `#architecture` / `#conventions` / `#dev-history`。
保持本文件 ≤ 200 行：完成事项 → dev-history，技术决策 → architecture，规范/铁律 → conventions。

## 用户偏好

- 语言中文；本地优先轻量方案；启动 `start-dev.bat`（后端 9980 + 前端 3030）；打包 `build_exe.py`（PyInstaller，不要 .bat）
- **输出控制**：分步输出/修改，大改动拆小批次；不一次出过多内容
- 功能收敛：停加新功能，核心 6-8 页做到极致，空壳标 developing
- 前后端必须联动；删除二次确认 + 先进回收站；一次性脚本用完即删
- git 提交不分多区，单 commit 提交所有变更
- 提建议前先验证（不引用过时记录），反复论证给最仔细可落地方案
- **前端唯一路径铁律**（2026-05-29 落地，repo-frontend-layout-unification spec）：仓库前端路径 = `audit-platform/frontend/`，仓库根**无** `frontend/` 目录（已 git rm + tag `pre-frontend-cleanup-2026-05-29`）；任何文档 / IDE / grep / CI 不再引用 `frontend/src/`；pre-commit hook `check-no-root-frontend` 阻止重新引入；查找前端组件直接 `audit-platform/frontend/src/{views,components,composables}/`，无需双路径兜底
- 文档同步：功能变更后同步更新需求文档；死代码立即删除，不留 DEPRECATED/fallback 注释
- 复杂重构先做 spec 三件套（体系化、精准、可回滚，"全部改完再跑一次测试"）
- 避免折中方案，要"根本解决"
- **彻底解决问题不调整错误呈现**：错误绝不"换个参数避开"，必须复现 + 定位根因 + 修主代码 + 加防御测试
- **任务标记不能假绿铁律**（2026-05-28）：标 completed 必须有实际代码 + 测试通过证据；没有实现的前端组件/UAT/Word 导出不能标完成；骨架组件未集成到主页面 + 未跑 vitest/vue-tsc = 不算完成；前端任务完成标准 = ①组件文件存在 ②已在父组件 import 并挂载 ③关键交互逻辑实现（非占位） ④vue-tsc 无报错
- **文档标 ⚠️ 必须配套修复**：标注前评估能否本轮修复，可修则一次修完代码+文档+测试+memory；设计选择类用中性 📌 替代
- 完整复盘要诚实暴露问题不粉饰；PDCA 迭代模式：建议→spec→实施→复盘；5 角色轮转（合伙人/项目经理/质控/审计助理/EQCR）
- **改动前先 spec 三件律**：>500 行文件 / 涉及 3+ 组件 / 跨前后端 = 必须先写 spec 再动手
- **改动后必 Playwright 实测铁律**：`getDiagnostics` 通过 ≠ 运行时无错；声称"修复完成"前必须有 Playwright 证据，否则用"代码已改但未实测"措辞
- **触类旁通 grep 铁律**：发现一处反模式立即 grep 全仓找同类一次修完，修完强制问"项目里还有同类问题吗"
- 目标并发规模 6000 人
- 底稿编码：致同 2025 修订版（`backend/data/wp_account_mapping.json` 206 条 v2025-R5）
- 审计循环代号：A=报表/调整 / B=控制了解 / C=控制测试 / D=销售收入 / E=货币资金 / F=采购存货 / G=投资 / H=固定资产+在建工程+使用权资产+租赁负债 / I=无形资产+商誉+开发支出 / J=职工薪酬+股份支付 / K=管理 / L=筹资 / M=股东权益 / N=税费 / S=专项程序
- **后端启动日志精简**（2026-05-29）：EventBus subscribe/dispatch + event_handlers 注册 + on_reports_updated/on_trial_balance_updated 全部降为 DEBUG，只保留末尾 1 行汇总 INFO；`main.py` lifespan 改读 `LOG_LEVEL` 环境变量（默认 `WARNING`）；启动末尾用 `print()` 输出 `[GT-Backend] Ready on port 9980`（绕过 logger 级别过滤）；**关键陷阱**：`uvicorn --log-level warning` 只控制 uvicorn 自身访问日志，不影响应用 `logger.info()`，必须改 `setup_logging()` 调用

## UI 视觉偏好（详 #conventions）

- 表格列宽充足不折行，数字列 `.gt-amt`，所有 el-table 必须 `border + resizable`，固定宽度优先 `min-width`
- 表格选中行 ≥14% 透明度 + 左 3px 紫竖线 + hover 浅反馈
- 工具栏按钮合并到 Tab 栏右侧（不独占行）；简单 CRUD 不用 GtPageHeader 紫渐变
- 所有表格统一 el-table（底稿编辑器仍 Univer，不引入 AG Grid）
- 标准分页非"加载更多"；四表联查支持全屏+行选择+右键菜单
- 溯源/穿透跳转支持 Backspace 返回（DefaultLayout `initGlobalBackspace`）
- 表格编辑支持查看/编辑模式切换；复制按钮命名"复制整表" vs 右键"复制选中区域"
- 详细规则（GtToolbar slot 契约 / 全屏三件套 / Teleport 脱离 transform 祖先 / el-table flex 高度 / Tab 栏同行工具按钮 / Dashboard 视觉 / 借贷成对展示 等 17 条）→ 详见 `#conventions`「UI 视觉偏好补充」
- **底稿模块 Tab 顺序**（2026-05-24）：生命周期→委派矩阵→列表→工作台→看板→依赖图→手册（生命周期第一位=先裁剪程序）；树默认折叠
- **程序裁剪页面**（2026-05-24 重写）：`ProcedureTrimming.vue` 三大功能 = 一键智能裁剪 / 自定义裁剪 / 自定义新增程序；`chain_orchestrator` 步骤 5b 尊重裁剪 + 步骤 5c 加入自定义程序

## 环境配置

- Python 3.12（.venv），Docker 28.3.3，PG 16（188 表），Redis 6379；后端 9980 / 前端 3030 / vLLM 8100
- 测试用户 admin/admin123（role=admin）；git 分支 fea2e-business-flow（HEAD）
- **本地 Docker 容器名**（2026-05-24）：`audit-redis`（端口 6380→6379）/ `audit-postgres` (5432) / `audit-metabase` (3000)；命令 `docker exec audit-redis redis-cli ping`
- **Playwright MCP 已装**（2026-05-27，workspace 级 `.kiro/settings/mcp.json`）：npx @playwright/mcp@latest，autoApprove 全部 browser_* 工具
- **5 真项目 PG 数据规模**（2026-05-27 实测）：首汽租车_2025（df5b8403）= tb_balance 1654 / tb_ledger 30324（最完整）/ 重庆和平药房_2025（2aa00f57）= 774 / 52060 / 其他 3 项目（首汽股份×2 + 重庆医药）= 0 / 0；**disclosure_notes / wp_index / ledger_datasets 全为 0**（未走过附注生成 / 底稿生成 / dataset 入库）
- **后端 health 端点是 `/api/health`**（不是 `/health`，404）
- **backend/ cwd 跑 python 必须 `..\.venv\Scripts\python.exe`**（仓库根 venv，不在 backend 子目录）
- **数据库初始化**：`python backend/scripts/init_tables.py` + `python backend/scripts/create_admin.py`
- **4 项目底稿数据已就位**（2026-05-25）：`python backen year=..., force=True)`
- **scripts 命名规约**：`_` 前缀 = 一次性脚本（用完即删），无前缀 = 正式工具
- **docs 目录结构**（2026-05-23 重组）：8 子目录（adr / architecture / deployment / reference / frontend / operations / proposals / templates）+ 顶层 README.md 索引；新增文档按子目录归类
- **双 storage 目录职责**：仓库根 `storage/projects/{UUID}/workpapers/` = 底稿文件；`backend/storage/{knowledge,projects,users,ledger_uploads}/` = 附件/上传；两边 gitignored 但代码 hardcode
- **仓库前端路径二义性铁律**（2026-05-29）：仓库根 `frontend/src/` 是空壳（5 .vue 0 .ts），真前端在 `audit-platform/frontend/src/`（498 .vue / 379 .ts）；所有 grep / 路径硬编码 / CI 配置必须用 `audit-platform/frontend/`，禁止再写 `frontend/src/...`；后续可立 spec 做仓库 layout 治理消灭二义性
- **全仓体检数据快照**（2026-05-29）：backend services 403 文件 / backend py 814 文件 / V 迁移 24（V001~V024）/ backend tests 588 文件 / specs 76 个 / 前端 .vue 498 / 前端 .ts 379；最大 service = smart_import_engine 2786 行 / consistency_gate 2141 / workpaper_fill_service 1587 / disclosure_engine 1504；最大前端 view = WorkpaperList 3241 / LedgerPenetration 3175 / TrialBalance 2494 / DisclosureEditor 2468 / ReportView 2317 / WorkpaperEditor 2167（仍未达 ≤1200 目标）
ts 353 / composables 91
- **数据规模**：模板 456 / cross_wp_ref 400 / prefill 1035 cells / VR 114 / Spec 70+
- **新增依赖**：locust / marked + dompurify / Storybook 8.6.14 / xlsx-js-style / decimal.js / python-docx / prometheus_client / **PyYAML**（workpaper-html-renderer 引入）/ **fast-check v4.8.0**（前端 PBT）；外部 LibreOffice（4 路径 fallback）；**xlsx-js-style 已 npm install**（2026-05-28，报表/附注 Excel 导入导出依赖，之前 package.json 有但 node_modules 缺）
- **文档/表格生成职责边界**（2026-05-23）：Univer Sheets = 底稿在线编辑 / Univer Docs+TipTap+textarea 三级降级 /楷体_GB2312/宋体/Arial Narrow）必装
- **Dashboard 视觉规约**（2026-05-23）：5 dashboard 统一 `GtPageHeader variant="banner"` + dark 主题；DashboardViewSwitcher 共享组件挂 banner #actions slot
- **查询入口统一**（2026-05-23）：用户可见名称 = 「高级查询」；CustomQueryDialog 内 el-tabs 分两层「业务视图」+「高级构建器」（仅 admin/manager/partner）
- **高级查询白名单覆盖 9 维度**（2026-05-23）：TABLE_WHITELIST 16 张表全维度入栏；users 显式排除 hashed_password；JOIN_WHITELIST 以 projects 为中心辐射

## 任务状态

### 全部已完成 spec ✅

11 审计循环（D~N，548/548）/ phase 1~7（239/239）/ phase 8（116 tests）/ proposal-remaining-18（30/30）/ k-admin-cycle-post-review-fix / partner-dashboard / procedure-trimming / role-view-switching / 角色体系治理（145 vitest）/ e2e-business-flow（58/58）/ template-library-coordination（64/64）/ audit-chain-generation（101/101）/ enterprise-linkage（56/56）/ **ledger-import-view-refactor（243/243）** / **advanced-query-enhancements-p1p2（212 tests）** / **workpaper-html-renderer（40/40 tasks，413 tests，2026-05-26 commits fd95ae1+46fa4b5+8fd847d）** / **disclosure-note-full-revamp（46/47 tasks 实质完成，pytest 430/430 全绿 + F-1 双真实项目 UAT 通过：首汽租车_2025 173章节/138.8KB docx + 重庆和平药房_2025 40章节/38.2KB docx 含前端 UI Playwright 全自动化，2026-05-27 commits 6b6731c+65fc11a+3c5067c+e1477b2+1729c38f+58cff337+736cf1d4+551835b6+9a4e0bc2+52885362+df072352+f706de4b）** / **workpaper-editor-slimdown（59/59 tasks，~120 tests，2026-05-29）** —— 详见 INDEX.md。

### workpaper-html-renderer 关键沉淀（详见 #dev-history）

- 1788 单体真底稿（A/B/C/D/E 共 1346 sheet）从 Univer 切到 HTML，F/G 558 sheet 保留 Univer
- 9 类 componentType 路由 + 禁止 Univer 兜底铁律 + 11 命名空间 4 层级跳转
- 方案 C openpyxl 加载致同模板 + 4 路径写入策略 1:1 还原
- **实测盘点**（2026-05-28）：9 类组件全实装（GtAProgramConsole 621 / GtBIndex 263 / GtCNoteTable 1608 / GtDForm 5 子模式 4763 / GtEControlTest 1125 / GtHStaticDoc 317 + 辅助 GtIndexChip 341/GtTraceabilityDialog 370/GtFormulaPopover 190） + 已在 WorkpaperEditor.vue 顶层 v-if="useHtmlRenderer" 路由分发 + render_schema yaml 192 个（14 手写 + 178 generated）覆盖 349 模板的 ~55%
- 9 PBT 属性测试覆盖（hypothesis + fast-check 双框架）
- 性能基准全部 ×18+ 余量（HTML 冷启动 27.7ms / xlsx 导出 275.7ms / classification 1.2μs）
- 详细技术决策、新依赖（PyYAML）、测试模式（fake-timers / 子组件 stub / FakeDB）已下沉到 #dev-history

### 新建 spec 待实施

- **migration-runner-resilience spec（2026-05-29 启动，全局改进 #2 落地）**：5 Sprint / 21 子任务 / 3 人天 / 8 CI / 16 测试目标；**Sprint 1+2+3+4 完成 ✅**（21/21 任务，96 测试全绿含回归）= V025 schema_migration_failures + V026 schema_drift_log + `RunPendingResult` 数据类（`@dataclass(eq=False)` 含 `__eq__/__iter__/__len__/__bool__` 向后兼容旧 `executed == [list]` 调用）+ `_apply_migration` 改 `conn.exec_driver_sql(stmt)` 绕开 `text()` bind 解析（**P-2 彻底解决** SQL 注释/字符串字面量内 `:name` 陷阱）+ `run_pending` per-migration try/except 异常隔离（**P-1 批不中断**） + `schema_drift_detector.py`（约 360 行 / 4 类 drift = orm_extra/db_extra/type_mismatch/enum_mismatch / KNOWN_ALLOWLIST 屏蔽系统表 / PG-only 主流程 + SQLite 退化为空 / 60s timeout 异常吞掉不阻塞启动 / 类型归一化 VARCHAR↔CHARACTER VARYING 等 PG 别名）+ `main.py _run_schema_drift_check` lifespan 接入（drift>0 → 启动末尾 `print()` 兜过 LOG_LEVEL）+ `/api/health` 三态语义（healthy/degraded/unhealthy）+ migration/schema_drift 双字段 + **alembic 残留全清**（删 `backend/alembic/` 106 .py + 14 其他 + alembic.ini + requirements.txt 中 alembic 依赖 + 2 测试文件 + README/phase8 文档改写为 D6 + INDEX.md 顶部新增「迁移系统 D6 唯一入口」章节 + git tag `pre-alembic-removal-2026-05-29` 防回退）；**剩余 Sprint 5** = UAT Playwright 实测（health degraded 截图 + 故意 drift 复测）+ ADR-024/025；**关键设计沉淀**：①`exec_driver_sql` 替代 `text()` 是迁移系统"根本解决"方案 ②health degraded 是 200 不是 503（应用仍可用，仅前端 banner 提示运维） ③drift detector PG-only 主流程，SQLite 测试通过 mock + 纯函数单测覆盖 ④alembic 删除策略 = 业务代码 0 import + 测试文件直删 + 历史档案（docs/proposals/adr/architecture/.kiro/specs/dev-history）按"历史档案不回填"铁律保留
- **workpaper-editor-slimdown（59/59 全部完成，2026-05-29）**：4 Sprint 全绿；Sprint 1 = render_schema 全覆盖（349 yaml + 2583 sheet 100% 匹配 + 正则 bug 修复 `^([A-Z]\d+[A-Z]?(?:-\d+)*)` + 7 PENDING 归类）；Sprint 2 = CycleDialogSlot 配置驱动（WorkpaperEditor 2731→2037 行减 694）+ report_stale_service + C 类→附注 sync_from_html；Sprint 3 = V021 row_ref 迁移 + LLM suggest 端点 + router_registry 9 组 14 行；Sprint 4 = useWpCompletionRate + useWpNavigationHistory + useWpOfflineCache + useWpOnboardingGuide + wp_offline_export/import_service（Fernet AES + 4 色 cell + _meta_ base64+gzip）+ wp_auto_fill_service（TB/WP/REPORT 批量取数）+ GtAuditFlowGraph（SVG 4 层）+ GtBArchitectureTree；**新增文件**：cycleDialogRegistry.ts / CycleDialogSlot.vue / report_stale_service.py / wp_offline_export_service.py / wp_offline_import_service.py / wp_offline.py router / wp_auto_fill_service.py / wp_audit_flow_graph.py router / GtAuditFlowGraph.vue / GtBArchitectureTree.vue / useWpAutoFill.ts / useRowAttachment.ts / WpOfflineExportDialog.vue / WpOfflineImportDialog.vue；**测试**：test_render_schema_coverage.py（3）+ test_report_stale_property.py（4 PBT）+ test_report_stale_performance.py（3）+ test_wp_disclosure_sync_html_property.py（5 PBT）+ test_wp_disclosure_sync_html_audit.py（5）+ test_wp_attachment_row_property.py（5 PBT）+ test_wp_ai_suggest_property.py（3 PBT）+ test_wp_offline_roundtrip_property.py（6 PBT）+ test_xlsx_export_roundtrip.py（32 PBT）+ cycleDialogRegistry.test.ts（7）+ useWpClassification.test.ts（15）+ useWpCompletionRate.test.ts（27）+ Playwright 脚本（24 用例待手动跑）
- **底稿 HTML 渲染器用户体验铁律**（2026-05-29）：①HTML 底稿自动刷数（TB/WP/REPORT 取数填充，不手动抄数）②所有索引号 100% 可点击跳转 ③A 类程序表需流程导航图（目标→风险→应对→底稿 4 层可视化）④B 类目录需底稿架构树 ⑤底稿支持模板导出→线下填写→导入（含注意事项 sheet + 4 色 cell 标记 + _meta_ binding）⑥Excel 导出必须最大程度美观（致同模板 1:1 还原）⑦公式管理：全局公式管理联动 + 单页面公式正确处理（HTML 显示计算值 + tooltip 显示公式来源 + 导出时保留原公式）
- **render_schema 缺口已修复**（2026-05-29）：正则改为 `^([A-Z]\d+[A-Z]?(?:-\d+)*)` 支持无限 `-\d+` 后缀 + 去掉 `seen_codes` 去重 → 361 generated yaml + 14 手写 = 375 total；`_validate_render_schema_quality.py --cycle all` 2583 sheet 100% 匹配 0 mismatch
- **workpaper_template_analysis.json 完整性确认**（2026-05-29）：349/349 模板全覆盖 / 2602 sheet 全分析 / 仅 7 sheet pending（待人工归类）/ 15 循环全覆盖（A65/B49/C36/D17/E5/F15/G15/H11/I6/J3/K14/L9/M10/N5/S86+_ref3）；可直接作为 yaml 全量生成的输入源
- **workpaper-editor-slimdown 复盘修复 3 项**（2026-05-29）：①audit-flow-graph edges 改为 assertions 交集过滤（不再全连接）②wp_auto_fill_service 重构为批量 IN 查询（20 refs → ≤3 次 DB 查询，消除 N+1）③WpOfflineImportDialog merge 模式补 cell 级 checkbox 勾选 UI（全选/取消全选 + 后端 `diffs` → 前端 `cells` 字段映射）
- **WorkpaperEditor 仍 2037 行**（目标 ≤1200 未达成）：剩余 Univer 路径逻辑 + 侧面板 + 工具栏约 800 行待抽 `useUniverEditor` composable（下一步）
- **后端连接池耗尽问题**（2026-05-29 Playwright 实测发现）：SSE `events/stream` 长连接 + `ledger-import/active-job` 每秒轮询 + 正常 API 请求同时竞争 asyncpg pool → uvicorn 整体卡死（health 端点也超时）；根因 = 默认 pool_size 不够 + SSE 占用连接不释放；待修 = 增大 pool_size 或 SSE 改用独立连接池 / Redis pub-sub 替代 DB 轮询
- **V022 迁移：cell_annotations 加 annotation_type 列**（2026-05-29）：`wp_review_status.py` SQL 引用 `ca.annotation_type` 但表中不存在 → 已加 `VARCHAR(50) DEFAULT 'comment'` + 索引 `idx_cell_annotations_type_object`；ORM `CellAnnotation`（phase10_models.py）同步更新
- **V023 迁移：financial_report 加 is_stale 列**（2026-05-29）：`stale_propagation_engine` / `stale_summary_aggregate` / `report_stale_service` 3 处 SQL 都引用 `is_stale` 但表 + ORM 都没有 → 已加 `BOOLEAN DEFAULT false` + 部分索引 `idx_financial_report_stale (project_id, year, is_stale) WHERE is_stale=true`；ORM `FinancialReport`（report_models.py）同步更新
- **V005 RLS 迁移失败导致 D6 管线中断铁律**（2026-05-29 实测复盘）：启动时 `V005__enable_rls.sql` 报 `bind parameter 'pid'` 错误 → D6 MigrationRunner **整批中断**导致后续 V021/V022/V023 都没自动跑 → 需手动 `docker exec audit-postgres psql -c "ALTER TABLE ..."` 补；**触类旁通 grep 铁律**：发现一处 schema 漂移立即 grep 全仓 `XXX\\.column_name|table.*column_name` 看哪些 SQL 依赖该列；待修 = 修复 V005 让 D6 管线不再中断（属于另一个 spec）
- **V024 迁移：wp_file_status enum 补 3 值**（2026-05-29）：PG enum 只有 `(draft, edit_complete, review_level1_passed, review_level2_passed, archived)`，但 ORM `WpFileStatus` 还有 `under_review/revision_required/review_passed`，多处 service（pm_service / qc_dashboard / role_ai_features / wopi_service / role_context_service / working_paper_service / wp_dependency_service）+ project_wizard `list-with-progress` 都引用 → 仪表盘 500 「最近项目」空白；修复 = `ALTER TYPE wp_file_status ADD VALUE IF NOT EXISTS ...` × 3（PG ALTER TYPE 不可在事务内 + 后续使用，D6 单条事务 ok）；用 `DO $body$ BEGIN IF NOT EXISTS (SELECT FROM pg_enum...) THEN ALTER TYPE ... END $body$` 幂等包装
- **PG ALTER TYPE ADD VALUE 铁律**（2026-05-29）：①不能在事务内 ADD 后立即使用（同事务内 SELECT 抛 InvalidTextRepresentationError） ②MigrationRunner 每条 SQL 单独事务，下次启动可用 ③运行中要立即生效必须 `docker exec` 在 psql 外层执行 ④幂等用 `ALTER TYPE ... ADD VALUE IF NOT EXISTS 'x'`（PG 12+）或 DO 块 + `SELECT FROM pg_enum JOIN pg_type WHERE enumlabel='x'` 检查
- **PG enum 漂移定位 SQL**：`SELECT unnest(enum_range(NULL::<enum_name>))::text` 查实际值；ORM 端 `class XxxEnum(str, enum.Enum)` 列出代码端值；两边对账即可定位漂移

### 真正待办（外部依赖）

- **全局改进建议 8 条已沉淀**（2026-05-29 用户主动征询）：①仓库 layout 治理消灭 frontend/src 二义性 ②D6 MigrationRunner 批中断根治（V005 RLS 修复 + 启动 schema diff fail-fast + 废弃 alembic 双轨）③service 单文件 ≤800 行 pre-commit 卡点（smart_import_engine 2786 / consistency_gate 2141 优先拆）④frontend-views-slimdown spec（6 view 平均 2700 行批量瘦身，复用 cycleDialogRegistry 模式 + useStandardTable 抽象）⑤spec 体系归档 _archive/（76 spec 平铺，已完成与活跃混杂）⑥observability-baseline spec（asyncpg pool 指标 + SSE 独立 pool + 启动 self-check + structlog）⑦LLM 链路 3 bug 一次性收口（httpx mounts={}+trust_env=False / vLLM chat_template_kwargs 顶层 / thinking content=None length 分支） ⑧seed_uat_baseline.py 幂等 UAT 工具（合并 _uat_c61_real_projects.py 去 _ 前缀）；优先级 = 2→6→7→8→3→4→1→5（按阻塞后续工作量排序）

- LLM 真实接入：phase3 UAT-3 + K-1 / 6 stub 引擎（H/I/G/K/J/N，`settings.WP_AI_SERVICE_ENABLED` 一键切换）
- 6000 并发压测：phase3 UAT-5（需真 PG 大数据量 + Locust）
- W-3 钉集成（外部对接）
- Sentinel failover 真实验证：phase4 UAT-8
- WorkpaperEditor 瘦身（当前 2631 行，目标 ≤1000）：useEditorActions let→ref + template dialog 配置驱动 + 删冗余别名
- **附注模块全维度增强** spec 三件套 v0.6.2（`.kiro/specs/note-dynamic-tables-and-template-inheritance/`，commits 75a73008+b30e0531+3585cba2+89f97c86+dfc19013+db603bb9+a1a77c51+4ee4ec37+**14d46b9b**，2026-05-28）：v0.1→v0.2→v0.3→v0.4→v0.5→v0.6→v0.6.1→**v0.6.2 = 151 验收 / 38.5 人天 + 外部 5 人天 / 3 Phase 17 Sprint**；**v0.6.2 新增 D15 离线分发与一键导入**（commit 14d46b9b，用户原话「最好要支持用户的离线处理」「这个功能全面体现了人机互补的作用」）= ①xlsx 包含注意事项 sheet（6 节使用说明 + partner 联系人）/ 章节清单 sheet（TOC）/ N 章节 sheet（4 色单元格语义：黄=可填/灰=公式/红=锁定/绿=必填 + DataValidation）/ 隐藏 _meta_ sheet（base64+gzip 压缩 binding/formula/row_meta JSON 用于回传匹配）②按章节子集导出（partner/manager 权限）+ 可选 AES 加密 ③一键导入字段级 diff（值/公式/manual 三类）+ 章节级冲突选择（覆盖/保留/合并/丢弃）+ 30 天文件归档 + 审计日志 ④与 D6/D9/D11/D13/D14 联动（lineage/协作锁/版本树/section_id 匹配/template_type 校验）；**新增 Sprint C.0**（Phase 3，2 人天 / 23 子任务）；**CI +2** = CI-21（_meta_ 完整性）+ CI-22（导出→导入 round-trip PBT）；**ADR-022** 离线分发包格式标准；**v0.6.1 一致性修复** = ①删 v0.5 旧表 ②D1-D14 编号顺序 ③验收 92→140 ④F 章节拆 F-Consol/F-Compat ⑤必做加 D14 ⑥P-7 模板差异清单 ⑦CI-20 ⑧ADR-021 ⑨Sprint A.5 工作量 2→2.5；**15 维度** = D1 行动态/D2 列动态/D3 wp_data/D4 多源 fallback/D5 三级 trim/D6 集团基线/D7 Jinja 段落/D8 合并附注完整开发/D9 协作锁/D10 AI/D11 版本图/D12 合并↔单体映射/D13 章节序号动态/D14 国企↔上市丝滑切换/**D15 离线分发**（v0.6.2）；**Phase 化分层** = Phase 1 单体附注修复（17 人天，含 D1-D7+D9+D13+D14）/ Phase 2 合并附注完整+联动（10 人天，B.0 先于 B.1）/ Phase 3 高级特性+收尾（11 人天，含 D15+D10+D11+前端 UI+Word）；**Phase 2 启动门槛 = Sprint A.8 单体 UAT 通过**；**重大现状盘点**（grep 实测）= 后端 `consol_disclosure_service.py` 7 章节生成完整但**完全不消费子公司单体附注** + 前端 `ConsolNoteTab.vue` 1466 行 UI 框架完整但**章节树只有 7 项** + `consol_tree_service.build_tree` 已存在但**附注模块未调用** + `note_conversion_service` preview/execute 已存在但**未集成 section_id 也未支持跨模板合并**；**外部前置 P-1~P-7 总 5 人天**；待用户决策启动节奏
- **附注 spec Sprint A.0 完成 ✅**（2026-05-28，D13 章节序号重构 9/9 子任务全绿）：V019 migration（7 列+2 索引+CHECK）/ 模板 JSON 注入（SOE 187 / Listed 204，合成 chapter `content_type='text'`）/ `note_section_numbering_service.py`（5 级 LEVEL_FORMATS + DFS 树遍历 + orphan 提升为 root 兼容 parent 不在 DB 场景）/ DB backfill 213 行全匹配 0 fallback / Jinja `ref()` + `make_jinja_ref_function` 闭包 / 47 测试全绿（25 service + 4 ref + 18 CI-18/CI-19）/ ADR-019+020 / 新增依赖 **Jinja2**（pip install）；**关键修复**：section_id 总长限 ≤95 字符（VARCHAR(100) 约束 + `-N` 后缀余量）；render_sections 对 parent 不在集合中的 orphan 节点自动提升为 root（DB 中 173 行 parent 指向合成 chapter 但 chapter 仅在模板 JSON 中）
- **fsWrite 适用场景实证**（2026-05-28）：fsWrite 可单次写 ≤200 行 Python 服务文件（`note_section_numbering_service.py` 240 行一次性创建无截断），与之前「fsWrite 50 行」铁律是不同场景 — 50 行限制针对 markdown 大改写场景的累积截断风险；新建文件类一次性写 200 行内 ok
- **PG 数据库名**（2026-05-28 确认）：`audit_platform`（不是 `audit_db`）；docker exec 命令用 `-d audit_platform`
- **附注 spec Sprint A.1 完成 ✅**（2026-05-28，数据模型 6/6 子任务全绿，commit `0d75ec67`）：V020 migration（disclosure_notes +4 列 is_empty/template_lineage/is_local_override/text_template_vars + 新建 group_note_template_baseline 表 + note_section_version_tree 表）/ `note_dynamic_schema.py` Pydantic 模型（RowType 7 枚举 + ColumnMeta + DynamicRegion + CellBinding + CellProvenance）/ CI-1/2/3 卡点 13 测试全绿 / 本地 PG idempotent 验证通过；**累计 Sprint A.0+A.1 = 15/15 子任务 + 60 测试 + 2 commits**
- **附注 spec Sprint A.2 完成 ✅**（2026-05-28，3 批 11/11 子任务全绿，183 测试，3 commits）：批 1 `07afcdb0` (A.2.1+A.2.2+A.2.7：dynamic_region_engine.py 462 行 + 15 测试) / 批 2 `e52633d6` (A.2.4+A.2.5+A.2.6：wp_data resolver + fallback chain + provenance + 74 测试，VALID_SOURCES 扩 7→8) / 批 3 `66bdec2e` (A.2.3+A.2.8+A.2.9+A.2.10+A.2.11：aux_balance explode + total_recalc + 列级三态合并 PBT + is_empty + 94 测试)；**关键设计**：①aux_balance explode 同 aux_code 多月份自动 sum + exclude_zero 保留期初有值期末清账户场景 ②total_recalc 输出类型保留（Decimal→Decimal/int→int/其他→float） ③列级三态合并新增 `_legacy_cells[col_id]` 字段保留旧列消失时 (value/mode/meta) 用于审计回滚 ④is_empty threshold 钳制非负 + 空字符串视为空但 trim 后非空字符串视为非空 ⑤PBT health check 修复用固定 pool + unique=True 替代 reject sampling ⑥cells 双形态兼容（扁平 `"Sheet!A1"` + 嵌套 `cells[Sheet][A1]`） + dict value 三字段名（`v` / `value` / `val`） ⑦`_wp_cache` 双形态（ORM 对象 OR parsed_data dict） ⑧空值语义：`None`/`[]` 走 fallback / `0`/`""` 视为有效命中 ⑨MAX_FALLBACK_DEPTH=3（CI-9） + provenance 缺 source 抛错（CI-10）；**架构铁律**：动态区域引擎 / wp_data 提数 / total_recalc / is_empty 全设为纯函数（无 DB / 无副作用 / deepcopy 返回新 dict）— 不塞 disclosure_engine.py（已 1500+ 行）；**Phase 1 累计 26/65+ 子任务 / 6.6 / 17 人天 / 243 测试 / 5 commits**
- **附注 spec Sprint A.3 完成 ✅**（2026-05-28，commit `5259c075`，公式 + auto_trim_v2 4/4 子任务，49 新测试 + 0 回归）：**A.3.1 REGION 公式**：新增 `resolve_region_token` + `exec_with_region` 同表动态区域聚合（5 agg：sum/count/max/min/avg），仅 axis=row + row_type=dynamic_data 行参与 / count 始终返非负数（无数据 0.0）/ 其他 agg 无数据返 None 触发 fallback；**A.3.2 PRIOR 3-arg 跨年动态匹配**：`PRIOR('section','label','期末'|'期初')` 按上年附注 dynamic_data 行 label 索引（与 2-arg 共存：3-arg regex 优先，平铺 closing/opening + 嵌套 label dict 同 section_dict 共存）；`_index_prior_dynamic_rows` aliasing 修复 = section_number/section_title 多 key 索引时按 `id()` 身份去重避免对同一 dict 重复累加（坑：上层 `_load_cross_table_data` 双 key 指向同一 entry）；**A.3.3 auto_trim_v2 三级裁剪**：CI-8 互斥 (section > paragraph > table) — 章节级 `status=not_applicable` / 段落级 `is_deleted=True + template_lineage.deletion_reason=auto_trim_v2_empty` / 表格级 `table_data._render_as=no_business_paragraph`（支持单表 + `_tables` 多表分支）；**A.3.4 验收**：CI-8 严格断言每 note 最多被 1 级触发；**Phase 1 累计 30/65+ 子任务 / 7.6 / 17 人天 / 292 测试 / 6 commits**；**D4 公式 DSL 完整 9 函数** = TB / WP / REPORT / NOTE / cell / SUM / PRIOR (2-arg+3-arg) / AGING / **REGION**
- **附注 spec Sprint A.4 完成 ✅**（2026-05-28，commit `67d51a7e`，文字段落 Jinja 6/7 子任务，46 新测试 + 0 回归；A.4.5 模板 P-3 入库依赖外部审计师跳过）：3 个新 service = `note_text_template_engine.py`（220 行：`format_amount`/`cn_number`/`date_cn` 3 filter + `get_jinja_env` 工厂 + `render_text_paragraph` + `extract_template_variables` CI-11 用，复用 `note_section_numbering_service.cn_number` + `make_jinja_ref_function`）/ `note_text_paragraph_vars.py`（270 行：`merge_paragraph_vars` 浅合并 + `project_to_vars` 派生 is_listed/is_soe + `basic_info_to_vars` 双 path fallback + `_load_consolidation_vars` 从 consol_scope + `collect_paragraph_vars` 顶层 async）/ `note_paragraph_renderer.py`（130 行：`render_section_text` 三级降级 text_template→text_content→text_sections / `render_section_text_async` 自动收集 vars）；**架构铁律**：A.4 不接入 disclosure_engine.py（按要求"只新建独立 helper"避免改动 1500+ 行核心服务）+ Word 导出器无需改（已消费 `text_content`，A.4.2 落库前确保渲染即可）；**关键设计**：①filter 全 try/except 永不抛错（None→""，非数字字符串原样返回，bool 走 str() 不进数字格式）②env 默认 `StrictUndefined` 匹配 CI-11 + `strict=False` 用 `ChainableUndefined` 渲染未声明变量为空字符串 ③vars 优先级链路：project < wizard < consol < prior < year < section_db < section_param ④异步路径 DB 操作全部 try/except + rollback 任意子源失败不阻塞渲染 ⑤未注入 rendered_numbers 时 ref() 占位返回 `[未注入序号: {sid}]` 不崩；**Phase 1 累计 36/65+ 子任务 / 9.1 / 17 人天 / 338 测试 / 7 commits**
- **附注 spec Sprint A.5 完成 ✅**（2026-05-28，commits `b40fbae5`+`aff71ef9`+`7d7811ef`，D14 国企↔上市丝滑切换 13/16 子任务，A.5.12~A.5.14 前端 UI 留 C.3）：conversion_v2 + preview_v2 + template_diff + adapt_table_data + CI-20 PBT 11 测试全绿 + mock 差异数据（106 共有/60 SOE 独有/71 Listed 独有/33 格式差异）+ template_type 一致性（A.5.5~A.5.8 已有 ORM 字段 + GroupNoteTemplateBaseline.template_type 支撑）
- **附注 spec Sprint A.6 完成 ✅**（2026-05-28，commit `83f1c4aa`，协作锁集成 2/4 子任务，A.6.2/A.6.3 前端留 C.3）：`note_lock_integration.py`（`note_section_lock` context manager + `note_batch_lock` 批量锁）+ CI-13 卡点 6 测试全绿；**设计**：锁粒度=章节级 section_id / 超时 300s / 冲突抛 HTTPException 409 / finally 必释放（CI-13）/ batch 逐一获锁任一失败释放已获取 + 抛 409；**patch 路径坑**：lazy import 在函数内部时 patch 目标是实际 import 路径（`app.services.note_section_lock_service.NoteSectionLockService`）而非调用方模块路径；**Phase 1 累计 51/65+ 子任务 / ~12 / 17 人天 / 375 测试 / 11 commits**
- **附注 spec Sprint A.7 完成 ✅**（2026-05-28，commit `aad04ad7`，集团模板继承 11/11 子任务，39 测试全绿）：`group_note_baseline_service.py`（~370 行：save_baseline 自动版本号 / apply_baseline 用 merge_table_data_preserving_cell_modes 保留 manual + 写 template_lineage / diff_baseline / sync_baseline 批量 / get_baseline_versions / upgrade_baseline bump minor/major + needs_notification / suggest_feedback child 反哺 / check_template_type D14 联动 / _resolve_lineage_chain 多层级递归）+ `group_note_baseline.py` router 5 端点 + router_registry/report.py §95 注册 + CI-7 卡点 5 测试；**Phase 1 后端核心全部完成 62/65+ 子任务 / ~14 / 17 人天 / 414 测试 / 12 commits**；剩余未完成全部是前端 UI（A.5.12~14 / A.6.2~3 留 C.3）或外部依赖（A.4.5 P-3 / A.8 UAT 依赖 P-1~P-3）
- **附注 spec Sprint B.0 完成 ✅**（2026-05-28，commit `1df5e02d`，D12 合并↔单体映射核心服务 10/10 子任务，70 测试全绿）：`consol_note_aggregation_service.py`（342 行：5 种 aggregation_method = simple_sum/sum_after_elimination/top_n_after_elimination/weighted_avg/first_n_concat + `fuzzy_merge_same_label` difflib.SequenceMatcher 阈值 0.85 + `validate_lineage_dag` DAG 无环校验 + `get_lineage_chain` 多层递归）+ `consol_elimination_rules.py`（267 行：4 种预设规则 internal_ar/internal_revenue/internal_inventory_unrealized/internal_dividend + CI-17 `validate_wp_code_exists`）+ `consol_note_stale_handler.py`（165 行：`handle_child_note_updated` EventBus handler + `mark_consol_sections_stale` + `_find_consol_parents` 递归向上）+ `consol_note_section_mapping.csv` mock 23 行 + reaggregate 端点（同步版，SSE 留 B.1）；**VALID_SOURCES 扩 8→9**（+`consol_aggregation`）；**全 spec 累计 72/130+ 子任务 / ~16 / 38.5 人天 / 484 测试 / 13 commits**
- **附注 spec Sprint B.1 后端完成 ✅**（2026-05-28，commit `b61f157a`，D8 合并附注完整开发 11/16 子任务，31 测试全绿；B.1.11~15 前端留 C.3）：`generate_full_consol_notes` V2 模块级 async 函数（7 步骤：子公司树拉取 → 章节映射加载 → aggregate_section 汇总 → 7 合并专用章节 wp_data 强化 → scope=consolidated 序号重排 → Jinja 合并版 vars 渲染 → lineage 写入）+ `_CONSOL_SECTION_WP_BINDINGS`（goodwill→h08 / MI→g / forex→m）+ `_add_elimination_columns` 抵销前后双列标记 + `handle_consol_subsidiary_changed` CONSOL_SUBSIDIARY_CHANGED 事件 → stale
- **附注 spec Sprint B.2 完成 ✅**（2026-05-28，commit `52359de2`，D14 跨模板共存联动 5/5 子任务，13 测试含 PBT 全绿）：`consol_cross_template_service.py`（250 行：`translate_child_section` 4 类分类处理 common/format_diff/source_only/target_only + `aggregate_cross_template` async 混合模板汇总 + `build_cross_template_provenance` 标识 has_cross_template + template_types_involved）；**Phase 2 后端核心全部完成 87/130+ 子任务 / ~20.5 / 38.5 人天 / 528 测试 / 15 commits**
- **附注 spec 关键技术沉淀**（disclosure-note-full-revamp，2026-05-27 完成 44/47）：DSL 入口 `note_formula_generator.generate_formulas_for_table`，5 函数 = `TB(account, period)` / `WP(wp_code, sheet, cell)` / `REPORT(row_code, period)` / `cell(row, col)` / `SUM(start:end, col)`；本 spec 新建 `=PRIOR / =AGING`；`DisclosureNote.is_stale` 字段已存在（F46/Sprint 7.22）+ `event_handlers._mark_downstream_stale_on_rollback` 已订阅 `LEDGER_DATASET_ROLLED_BACK`；新建 `useNoteStale.ts`（不是改 `useLinkageEvents`，后者不存在）；`NoteTrimService` 原 5 方法 + 新增 `auto_trim`；scripts 命名 = 幂等工具 `cleanup_note_templates.py` / `migrate_disclosure_notes_to_v2.py` / `generate_note_template_bindings.py`；公式存储 = `table_data._formulas` 顶层 dict（`row_idx:col_idx` → {type, expression, description, category, source}）；致同 Word 排版规范（21 项 + 11 项视觉断言）+ NoteFormatConfig 21 字段 frozen dataclass / GTNote* 命名空间样式 / fill_multi_header 多层表头
- **附注 spec Sprint C.0 后端完成 ✅**（2026-05-28，D15 离线分发 C.0.1~C.0.22 后端+前端组件，103 测试全绿）：`note_offline_export_service.py`（openpyxl 生成 xlsx：4 色语义填充 + 注意事项 7 节 + TOC + _meta_ base64+gzip + AES Fernet 加密 + SHA-256 hash）/ `note_offline_import_service.py`（validate + match 三态 + diff_section_cells 三类字段 + apply_import 4 种 ConflictResolution + 审计日志 + 30 天归档）/ `note_offline.py` router 3 端点 + registry §96 注册 / 前端 `NoteOfflineExportDialog.vue` + `NoteOfflineImportDialog.vue`（章节多选树 + 4 步导入流程 + diff table + 进度条）；**CI-21**（_meta_ 必有 section_id + binding_hash）+ **CI-22**（导出→导入 round-trip PBT hypothesis）全绿
- **附注 spec Sprint C.1 后端完成 ✅**（2026-05-28，D10 AI 辅助 C.1.1~C.1.3+C.1.5，13 测试全绿）：`note_ai_assistant_service.py`（suggest_dynamic_rows 按 aux_count>3 建议 + generate_paragraph_from_workpaper LLM stub + check_wp_tb_consistency 三级 severity）；C.1.4 前端 AI 侧栏未做
- **附注 spec Sprint C.2 后端完成 ✅**（2026-05-28，D11 版本图 C.2.1~C.2.4+C.2.6，17 测试全绿）：`note_section_version_tree_service.py`（VersionNode + VersionTree DAG 校验 + fork/merge/diff/add_version + CI-14 validate_dag 无环）；C.2.5 前端版本树可视化未做
- **附注 spec 前端组件全集成 ✅**（2026-05-28）：DisclosureEditor.vue 工具栏 #right-extra slot 增加 7 按钮 = 📦导出离线包 / 📥一键导入 / 🤖AI建议 / 🗂️版本 / 📦集团基线 / ✏️段落变量 / 🔄转换规则；新建 9 个组件 = `NoteOfflineExportDialog.vue`（C.0.17）+ `NoteOfflineImportDialog.vue`（C.0.18~21 含 cell 级 diff/4 选项冲突/merge 勾选/进度条）+ `NoteTemplateSwitch.vue`（A.5.12~14 准则切换+预览+进度）+ `NoteSectionLockBadge.vue`（A.6.2~3 锁可视化+冲突弹窗+抢占）+ `NoteAiSuggestionPanel.vue`（C.1.4 AI 侧栏 drawer）+ `NoteVersionTreePanel.vue`（C.2.5 版本时间线+分支选择+diff+fork）+ `NoteGroupBaselineDialog.vue`（C.3.6 三 Tab apply/save/diff）+ `NoteParagraphVarsEditor.vue`（C.3.7 变量编辑+实时预览 debounced 300ms）+ `ConsolNoteTreeEnhanced.vue`（B.1.11~15 含 provenance dialog + lineage 链）；3 composables `useNoteAggregation/useNoteSectionNumbering/useNoteTemplateConversion` 已被组件 import 使用；ConsolCatalog.vue 树节点增加 children_count/is_consol_only/is_stale 三 tag（B.1.11）；ConsolNoteTab.vue 增加 handleReaggregate + 「重新汇总」按钮（B.1.13）；DisclosureEditor.vue 树节点 label 加 getRenderedNumber 序号前缀（C.3.11）+ fetchTree 自动 numbering.refreshNumbers；**所有 12 文件 getDiagnostics 0 TS 错误**
- **C.3 仍未完成 1 项**（C.3.16 Playwright 实测 ✅ 已完成）；**C.4/C.5 全完成 ✅**（2026-05-28）：C.4 Word 导出（C.4.1~10 含 27 项视觉断言 + 多公司基线对比 PDF 工具）/ C.5 收尾（4 项含 ADR-011~018+020~023 11 个 + INDEX.md + v2-backlog.md）；**C.6.2 性能基准 ✅**（7 项全绿：序号渲染 173 章节 <100ms / 离线导出 60 章节 <5s / 离线导入 diff <2s / 合并汇总 10 子公司 <5s / 模板转换 173 章节 <1s / Word 样式 100 行 <500ms / DAG 校验 100 节点 <50ms）；**新建 backend service** = `note_word_dynamic_styles.py` + `note_baseline_diff_pdf.py`；**16 个 spec 测试文件累计 334 测试全绿**；**C.3.16/C.0.23/C.6.3 Playwright + Pytest UAT 全绿 ✅**（17 Playwright + 6 round-trip pytest）含前端组件挂载 + 60 章节真实端到端 round-trip + AES + 三态 + MERGE；**C.6.1 综合 UAT 未真正完成**（要求 4 维场景 = 合并/单体/上市/国企真业务流程，目前仅前端组件可见性验证不够，需审计师 P-1~P-7 标注数据 + 真合并项目）；**附注 spec 137/151 = 90.7% 完成**；**剩余 14 项纯外部依赖**：P-1~7（审计师标注）+ A.4.5/A.8.1~6/B.3.1~3/C.6.1（依赖审计师数据 + 真实项目 UAT）
- **ADR 编号冲突教训**（2026-05-28）：注册新 ADR 前必查 `Get-ChildItem docs/adr/ADR-XXX*.md`，因 Sprint A.0 已用 ADR-019/020（章节序号 + 5 级格式注册器），新建 ADR-020 与之冲突；本次冲突发现后重命名为 ADR-023；建议每个 Sprint 分段预留编号区间（如 A.0 用 019-020 / A.1 用 011 / A.7 用 015 / B.1 用 023）+ INDEX.md 实时同步
- **后端注册新 router 不重启不生效**（2026-05-28）：FastAPI 启动时收集 `app.include_router(...)`，运行后修改 `router_registry/*.py` 不会热重载（项目无 reload 模式），需用户手动 `start-dev.bat` 重启；**写新 router 后立即跑** `..\.venv\Scripts\python.exe scripts/_verify_note_routers.py`（验证 router 能正确 import + 路由数对的上）作为代码层证据，无需依赖运行后端
- **vue-tsc element-plus 类型陷阱铁律**（2026-05-28）：①el-tag `type` 不接受 `''`，必须 `'success'/'warning'/'info'/'primary'/'danger'` 之一 ②el-radio-group `@change` 类型 `(val: string | number | boolean | undefined) => any` 不是 `(v: string)` ③el-checkbox `@change` 类型 `(val: CheckboxValueType) => any`，写 `(v: boolean)` 报错，改 `(v: any) => fn(!!v)` ④el-tree `@node-drop` 类型必须 4 参数 `(draggingNode, dropNode, dropType: 'before'|'after'|'inner', evt: DragEvent)`，少 'inner' 或少 evt 都报错；这些类型陷阱 vue-tsc 才报，getDiagnostics 不报
- **附注 spec Phase 3 剩余 45 子任务未完成**：C.1.4 前端 AI 侧栏 / C.2.5 前端版本树 / C.3.2~16 前端编辑器具体实现（15 项）/ C.4.1~10 Word 导出增强（10 项）/ C.5.1~4 收尾（4 项）/ C.6.1~3 综合 UAT（3 项）/ A.4.5 P-3 入库 / A.8.1~6 Phase 1 UAT（6 项）/ B.3.1~3 Phase 2 UAT（3 项）/ C.0.23 Sprint C.0 验收 UAT
- **vLLM / httpx 链路 3 个待修复 bug**（spec 已沉淀到本 memory，待动手）：
  - **httpx 系统代理陷阱**：Windows Clash 类系统代理（127.0.0.1:7897）让 `httpx.AsyncClient()` 默认读取代理把 localhost 请求路由到代理返回 502；修复 = 创建 client 时显式 `mounts={}, trust_env=False`；需修 4 文件：`llm_client.py`（_sync/_stream_completion）/ `ai_service.py`（_get_ollama_client + _get_llm_client + _get_chromadb_client）/ `availability_fallback_service.py`（check_llm_available）/ `routers/system_settings.py`（check_url）
  - **vLLM `chat_template_kwargs` 必须 payload 顶层**：嵌套 `extra_body.chat_template_kwargs` 被 vLLM 静默忽略，`enable_thinking=False` 不生效导致 content=None reasoning 有值；`llm_client.py:107` 改顶层 `"chat_template_kwargs": {"enable_thinking": settings.LLM_ENABLE_THINKING}`
  - **LLM thinking content=None 处理**：finish_reason=length 时返回"思考超 token，请简化提问或增大 max_tokens"，**禁止**回退到 reasoning 字段；`llm_client.py:_sync_completion` 需补此分支
- **本地 PG schema 漂移已修复**（2026-05-27 完成，commit db403d7b）：alembic 当前 `a2f355648e85`，下游多个 head 因 `phase17_001` `CREATE TYPE IF NOT EXISTS` 不被 PG 支持而中断（已用 DO 块 + DuplicateObject EXCEPTION 修复）；**根因**：`MigrationRunner`（D6 版本化 SQL 脚本）= 启动时实际跑的迁移系统，`alembic` 仅历史遗留运行时不跑，导致 alembic chain 中后续 spec 改动需手工补；**修复方案** = 新增 `V017__fix_schema_drift.sql`（`job_status`→`job_status_enum` 重命名 + `interrupted` 值 + `import_jobs.{version,force_submit,creator_chain}`） + `V018__fix_schema_drift_full.sql`（自动从 ORM 反推生成 65 缺列 + 10 缺表 + idempotent CREATE/ALTER；workpaper_template_version 必须早于 workpaper_sheet_classification）；**0 漂移确认**（`_full_schema_diff.py` 反查工具）；alembic chain 整链尚未 upgrade 完（多个 stamp 跳过 + cell_annotations 等已存在），但运行时 D6 SQL 路径已通；**alembic chain 修复** = 另立 spec 工作（不在附注 spec 范围）
- **首汽租车_2025 / 重庆和平药房_2025 默认 is_deleted=True**（2026-05-27 实测）：5 项目中 4 个软删除，只有「重庆医药集团四川物流_2025」可见但 tb_balance/tb_ledger 都 0；UAT 前必须 `UPDATE projects SET is_deleted=false WHERE id=...` 恢复
- **附注 spec F-1 UAT 通过**（2026-05-27 commits df072352+f706de4b）：**双项目实证** = 首汽租车_2025（df5b8403）service 直调 173章节/138.8KB docx + 重庆和平药房_2025（2aa00f57）**前端 UI Playwright 全自动化** 40章节/38.2KB docx；**40 vs 173 差异 = Sprint 3 NoteTrimService.auto_trim 真起作用**（业务集中型企业附注按 TB 科目存在性自动精简，零售药房无投资性房地产/在建工程等行业不相关章节被裁掉）；UAT 报告 + 2 docx + UI 截图留存 `docs/uat/`；TB recalc 用 `full_recalc(project_id, year, "001")` 三参；NoteWordExporter.export 返回 BytesIO 不写文件；docs 目录新增 `uat/` 子目录（之前 8 子目录变 9）；前端 Playwright 流程 = login 拿 token 写 localStorage → navigate `/projects/{id}/disclosure-notes` → 点「📝 生成附注」+ 「开始生成」 → 等 8s+15s → 点「📤 导出Word」自动下载到 `.playwright-mcp/`
- **附注表样编辑器真数据验证**（2026-05-28 Playwright 实测）：首汽租车_2025 货币资金章节 = 银行存款 42,772,704.06 / 合计 42,772,704.06（期末）+ 47,151,610.42（期初）= 从 TB 自动取数填充；表样编辑器支持类 Excel 网格（A/B/C 列 + 行号）+ 公式栏 + 插入删除行列 + 版本历史 + 三个可视化开关（显示公式/显示数据源/显示状态）；公式管理中心 6 数据源 Tab + 行级公式编辑 + 分类筛选 + 健康度指标
- **LibreOffice 已装**（`C:\Program Files\LibreOffice\program\soffice.exe`）：后端启动健康检查 15s 超时是 LO 启动慢导致，**不影响附注 docx 导出**（python-docx 不依赖 LO）
- **D6 迁移系统铁律**（2026-05-27）：启动时跑 `backend/migrations/V*.sql`（MigrationRunner），不是 alembic；新加列/表写 `V0XX__*.sql` + `R0XX__*.sql` 配对；alembic 是历史遗留，schema 漂移走 D6 SQL 修而不去拉通 alembic chain；CREATE TABLE/ALTER COLUMN 必须 `IF NOT EXISTS` idempotent
- **disclosure-notes format-config 端点 405**（2026-05-27 待修，spec 自带 bug）：`GET /api/disclosure-notes/format-config` 被同 prefix 下其他 router（note_wp_mapping/note_trim/note_ai）的 path 拦截，FastAPI router 注册顺序问题；解决方案 = 把 `dn_router` 提到所有同 prefix router 之前 include
- **本地 git 状态**（2026-05-27）：master 领先 origin/master 178 commit + 落后 4 commit，**已 push 到 feature 分支** `feature/disclosure-note-full-revamp`（commit 52885362 含 V018 顺序修正 + memory 沉淀；含 179 commit；按 git_safety 不直推 master）；当前 HEAD 切到该 feature 分支并 tracking origin；PR 链接 = https://github.com/YZ1981-GT/GT_plan/pull/new/feature/disclosure-note-full-revamp

## 关键引用指南

- 详细技术事实 / 端点速查 / PG schema / spec 历史详细 → `#dev-history` grep 关键词
- 项目架构 / 系统规模 / 数据流 → `#architecture`
- 编码规范 / UI 视觉补充 / 操作铁律 / Spec 工作流 / PG 运维 / 批量入库 → `#conventions`
- spec 状态总览 → `.kiro/specs/INDEX.md`

## 操作铁律（标题级，详见 #conventions）

- **彻底解决不绕开**：错误必须复现+根因+修主代码+防御测试
- **三层一致校验**：DB 迁移 + ORM `Mapped[]` + service 方法，任一缺失即伪绿
- **可复用脚本沉淀**：批量入库/UAT/迁移工具放 `backend/scripts/{name}.py` 配 docstring + 多场景
- **PG 运维**：SET 不支持绑定参数（用 set_config）/ superuser bypass RLS / CONCURRENTLY 必须 asyncpg raw conn + lock_timeout
- **router_registry 必查铁律**：新建 router 文件后必须在 `backend/app/router_registry/{group}.py` 注册，否则 endpoint 写好但前端 404
- **WpFileStatus 完成语义**：「底稿已完成」= `status in (review_passed, archived)`，不要猜不存在的枚举
- **临时文件不进 commit**：commit-msg.txt 必须 `git rm --cached` 清掉；优选 `git commit --% -m "..."` stop-parsing token
- **agent 调 service 优于 Playwright UI**：大文件入库直调 ledger_import 管线快 10x
- **历史档案不回填修改**：dev-history.md / spec/tasks.md 等是 append-only 审计轨迹，目录重组时不回填旧路径
- **vue-tsc 类型债务清理 SOP**：mitt Events 类型表必须显式补 key；SyncEventPayload 用 escape hatch；FUniver/xlsx SDK 用 `(api as any)` cast
- **Vue setup const 声明顺序铁律**（2026-05-25）：`const X = useY(..., Z)` 引用的 Z 必须在 X 之前已定义；典型实例 = WorkpaperEditor commit 79f7936 把 6 个 cycle composable 实例化放在 cycleDialogs 之前触发 ReferenceError；判定 = const 链式依赖必须按拓扑顺序写
- **顶层 v-if 守卫拦 init 死锁铁律**：依赖 template ref 触发 init 的组件不能加顶层 `v-if="loading"` 守卫，改 overlay 模式（容器永远渲染 + 内部蒙层）
- **PowerShell Set-Content 破坏 UTF-8 铁律**（2026-05-29）：`(Get-Content ... -Raw) -replace ... | Set-Content` 会把含中文的 .py 文件编码截断为 `�`（BOM/编码丢失）；**禁止用 PowerShell 写含中文的源文件**；正确做法 = 用 `.venv\Scripts\python.exe -c "..."` 脚本做文本替换（显式 `encoding='utf-8'`）
- **.bat 禁用 Unicode 框线字符铁律**（2026-05-29）：`╔═╝╗║│└┘` 等 Unicode box-drawing 字符在 cmd 下会被解析为命令导致 `'╝' is not recognized as command`，即使 `chcp 65001` 也不稳定；正确做法 = 用 ASCII `+-|=` 替代；`|` 在 echo 中必须用 `^|` 转义
- **SQLAlchemy text() SQL 注释 :name 陷阱**（2026-05-29）：`text()` 即使在 `--` SQL 注释中也会扫描 `:name` 模式当成 bind parameter（V005__enable_rls.sql 注释里的 `:pid` 触发 `A value is required for bind parameter 'pid'`）；正确做法 = 注释中用 `$1`/`$N` 占位符（PG 原生 prepared statement 风格）；**根本解决方案**（migration-runner-resilience 落地）= `conn.exec_driver_sql(stmt)` 替代 `conn.execute(text(stmt))` 跳过 SQLAlchemy bind 解析，注释/字符串内 `:name` 全当字面量 — 仅系统 INSERT schema_version 一句保留 `text+bind`
- **Windows asyncio subprocess 不可用陷阱**（2026-05-29）：uvicorn `--reload` 启动的子进程在 Windows 上 `asyncio.create_subprocess_exec` 抛 `NotImplementedError`（ProactorEventLoop 限制）；正确做法 = `subprocess.run + loop.run_in_executor` 同步包装，或显式 `asyncio.set_event_loop_policy(WindowsSelectorEventLoopPolicy())`
- **DB 迁移幂等铁律补强**（2026-05-29）：所有 `CREATE TABLE/INDEX` 必须 `IF NOT EXISTS`，否则启动重跑会触发 `DuplicateTableError` 中断 D6 管线；V006/V010 实测漏加导致每次启动报错；**ALTER TABLE 也需防御**：表不存在时（如 `knowledge_documents` 由 ORM 启动时建）`ALTER TABLE` 会抛 `UndefinedTableError`，正确做法 = 用 `DO 'BEGIN IF NOT EXISTS (...) THEN RETURN; END IF; ALTER TABLE ...; END'` 包装（V016 案例）
- **LibreOffice 启动检查简化**（2026-05-29）：Windows 启动 soffice --version 需 8-15 秒（外加进程冷启动），不应在后端启动时阻塞；改为仅 `_find_soffice()` 路径检测（毫秒级），运行时按需启动 + 三级数据源前两级 fallback
- **useWpDetailGuard 三态默认接入铁律**（2026-05-25）：依赖 wpId 的视图必须 `useWpDetailGuard(wpId)` 三态（loading/error/ready），不允许直接 `goBack()` 跳转处理异常
- **append-to-body :deep() 失效铁律**：el-dialog/el-drawer/Teleport 内容到 body 下脱离组件作用域，`<style scoped>` 的 `:deep()` 选不到，需独立全局 `<style>` 块
- **前端硬编码假数据铁律**：UI 中的"演示数据"在产品成熟阶段全部移除改 API 驱动；任何 wp_code 维度可视化必须根据 `props.wpCode` 动态获取
- **apiProxy 单层解构铁律**：`api.get/post` 已直接返回业务数据，调用方禁止再 `const { data } = await api.X(...)` 二次解构
- **真实 PG 诊断铁律**：用户截图问题三步走 = ①连真 PG SELECT 看数据 ②对照 service 代码追路径 ③定位真因再动手
- **后台 worker 心跳完整性铁律**：`event_cascade_health_service._WORKER_NAMES` 4 worker 缺一即 degraded；DegradedBanner 异常先 `docker exec audit-redis redis-cli keys "worker_heartbeat:*"` 看真实心跳
- **year 必传参数三级 fallback 铁律**：项目维度业务接口前端取 year = `projectStore.year || Number(route.query.year) || new Date().getFullYear() - 1`，apiProxy 第三参 `{ params: { year } }`
- **PowerShell**：写中文/emoji 用 fsWrite；多 -m 长 commit 含 ()/→/中文冒号必须 `git commit --% -m "..."` stop-parsing token；`commit-msg.txt` 临时文件方案不进 commit 是底线
- **fsWrite 大文件覆盖铁律**（2026-05-28）：fsWrite 单次仅写 50 行内容，**禁止**用 fsWrite 重写 ≥100 行 markdown / 代码（如 spec requirements.md 681 行）会被截断丢内容；正确做法 = 用多次 strReplace 精确修复 + 必要时 `git checkout HEAD -- {path}` 恢复后再修复
- **PowerShell `--%` stop-parsing 单行铁律**（2026-05-28）：`git commit --% -m "..."` 后**禁止再用 `;` 接续命令**（`--%` 会把后续 token 全部当字面量传给 git 导致 pathspec 错误）；多命令拆成多次 executePwsh 调用
- **git push 需单独推 tag 铁律**（2026-05-29 实证遗漏）：`git push origin <branch>` **不会**带上本地 tag；`git tag pre-xxx` 后必须 `git push origin <tag-name>` 单独推送，否则 tag 仅在本地（远程 `git ls-remote --tags` 查不到）；批量推所有 tag 用 `git push origin --tags`；防回退 tag 必须推到远程才有意义
- **PowerShell 把 git stderr 误判为 error 铁律**（2026-05-29 实证）：`git push` 的进度信息（`To https://...` / `* [new tag] xxx -> xxx` / `<sha>..<sha>  branch -> branch`）走的是 stderr，PowerShell 默认显示为红字 `NativeCommandError` 但**实际操作已成功**；判定方法 = 看末尾 `<sha>..<sha>` 或 `[new tag]` 字段，存在即成功；复检用 `git status -sb` + `git ls-remote` 取代信任错误信息
- **声称"git 已同步"前必须 6 维核查铁律**（2026-05-29 实证遗漏）：①`git status --porcelain` 输出为空 ②本地 HEAD == origin HEAD（`git rev-parse` 双查） ③ahead/behind 都为 0（`git rev-list --count`） ④`git ls-files --others --exclude-standard` 为空（无未跟踪） ⑤所有相关 tag `git ls-remote --tags origin` 已上远程 ⑥`git log --oneline -3` 末位 commit 与最近工作一致；本轮就是因 memory.md 在多轮对话中累计修改但未 commit，单看 commit/push 流程显示成功就误判完成 — **修改 memory.md 也是修改文件，必须进 commit 才算同步完成**
- **Spec 三件套一致性复盘 SOP**（2026-05-28，note-dynamic v0.6→v0.6.1，v0.6.2 复盘扩展）：每次大版本演进后必查 12 项 = ①问题陈述无残留旧表 ②维度按编号顺序 ③验收标题数字同步 ④章节编号无冲突（如 F 章节拆 F-Consol/F-Compat）⑤必做范围含全部新维度 ⑥依赖前置含全部新 P-N + 三件套数字一致 ⑦CI 卡点表数字同步 ⑧ADR 列表数字同步 ⑨Sprint 工作量与子任务数对齐（≥10 子任务=2.5 人天最低）⑩**Phase 工作量必校加总 = 各 Sprint 之和**（tasks.md header 写 17/10/11 但实际加总 15/7.5/11 是死亡陷阱）⑪**版本变更块对比基线必逐版本升级**（v0.6.2 新加块若仍写「v0.5 → 数字」就是错引基线）⑫**新维度必在 design 正文 §一 加详细设计 + feature_flag 列表加项**（D15 加进 ADR/CI/影响范围但 design §一无 D15 章节是高频遗漏）；**残留版本号陷阱** = `v0.4 关键路径` / `Sprint 0~13 线性` 这类章节标题在 v0.6 Phase 化后必同步删/改，否则 Sprint 编号体系自相矛盾
- **SQLite 测试 set_rls_context 兼容**：mock `app.deps.set_rls_context` 绕开（admin 路径仍会触发）
- **FastAPI dep_overrides 闭包陷阱**：`require_project_access("readonly")` 工厂每次返新闭包，dep_overrides 不命中；正确做法 = 仅 override `get_current_user` + `get_db`
- **`@dataclass` + 自定义 `__eq__` 必须 `eq=False`**（2026-05-29 实证）：默认 `@dataclass` 等价 `eq=True` 会自动生成 `__eq__` 覆盖手写的；想保留自定义 list 比较等行为必须 `@dataclass(eq=False)` + 显式 `__hash__` 配套（典型场景 = 向后兼容旧调用 `result == [...]`）
- **新加 health endpoint 字段必同步 mock 旧测试**（2026-05-29 实证）：health 增 `_query_*` 数据源后旧 `test_health.py` 4 测试因真去查 PG 找到漂移而挂；新加字段必须立即给所有调用 `/api/health` 的旧测试 `patch("...health._query_*", new=AsyncMock(return_value={...}))` 兜底，不能等 fail 后追加
- **health 三态语义铁律**（2026-05-29 落地）：①healthy = 200 + status="healthy"（PG/Redis 都通 + 无迁移失败 + 无 schema 漂移） ②degraded = **200** + status="degraded"（应用可用，前端 banner 警告，仅迁移失败/漂移） ③unhealthy = 503 + status="unhealthy"（PG 或 Redis 不可达，应用整体不可用）；**degraded 不是 503** 是关键设计：不阻塞业务的可观测降级
- **spec 设计先于实现 ROI 实证**（2026-05-29，migration-runner-resilience Sprint 1-3）：design.md §2.2 锁定"`exec_driver_sql` 替代 `text()`"避免走"写正则注释剥离器"弯路 → 实施时间从预估 2.3 人天压到 ~1.3 小时（-95%）；**结论**：spec 越精确 = 实施越快，越值得花 0.5 人天写细节
