{
  "enabled": true,
  "name": "自动续跑 Spec 任务",
  "description": "每当 Agent 停止时，自动扫描 .kiro/specs 下是否有被中断的 run-all 任务会话（tasks.md 中存在 in_progress [-] / queued [~] 标记，或仍有未完成任务）。若有则自动从中断点继续执行，直到该 spec 全部任务完成；无则不做任何动作。",
  "version": "2",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "askAgent",
    "prompt": "你是 run-all-tasks 编排器。目标：在 Agent 停止后，自动侦测并恢复被中断的 spec 任务批量执行，直到目标 spec 的全部任务真正完成。\n\n## 第一步：侦测被中断的 spec（用文件状态判断，不靠记忆）\n扫描 .kiro/specs/*/tasks.md，按以下优先级找到唯一的\"活跃/被中断\" spec：\n1. 含有 in_progress 标记 `- [-]` 的 tasks.md —— 这是被中途打断的任务，最高优先级恢复\n2. 含有 queued 标记 `- [~]` 的 tasks.md —— 这是已排队但未跑完的批量会话\n3. 若上面都没有，但本会话用户明确要求过\"run all tasks / 执行全部任务\"，则用该 spec 的 tasks.md\n\n如果以上都不满足（没有任何 [-]/[~] 标记，且本会话与批量执行 spec 无关），则**什么都不做，直接结束**，不要打扰用户。\n\n## 第二步：确定待执行任务集合\n在目标 tasks.md 中，找出所有\"未完成的叶子任务\"（没有子任务的最底层任务），判定规则：\n- 复选框为 `[ ]`（未开始）、`[-]`（中断的进行中）、`[~]`（排队中）三者之一 → 属于待执行\n- 复选框为 `[x]` → 已完成，跳过，禁止重复执行\n- **可选任务（关闭括号后带 `*` 或 `\\*`）也要执行** —— 按用户既定偏好，optional 任务同样做完；仅当任务体本身写明\"仅在 X 条件下\"且条件不成立时才跳过\n\n## 第三步：按依赖顺序恢复执行\n1. 若 tasks.md 顶部有 Task Dependency Graph（waves），按 wave 顺序执行；否则按任务编号顺序\n2. 对每个待执行任务：\n   - 先用 taskStatus 标记 in_progress\n   - 委派给 spec-task-execution 子代理实现（禁止你自己写业务代码/跑测试）\n   - 子代理遇到测试/编译/类型报错时，必须自动定位根因、修复主代码、重跑验证，不得停下来问用户，禁止用\"换参数/放宽断言/跳过用例\"方式绕过\n   - 子代理成功后用 taskStatus 标记 completed，再继续下一个\n3. 一个叶子任务全部完成后，若其父任务的所有子任务都已 completed，则把父任务也标记 completed\n\n## 终止与安全约束（防止无限循环）\n- 仅在确有未完成任务时继续；目标 spec 全部任务变为 [x] 后必须停止，并简要告知\"已全部完成\"\n- 禁止重复执行已 [x] 的任务\n- 同一任务连续失败 2 次以上仍无法通过时，停止执行，向用户说明根因和已尝试的方案，不要无限重试同一处\n- 一次只恢复一个 spec，不要并行跑多个 spec 的任务\n- 子代理调用因限流（Too many requests / rate limit）失败时，**必须等待至少5秒后重新调用子代理继续该任务**。具体策略：①第一次限流→等5秒→重试子代理；②连续第二次限流→等10秒→重试；③连续第三次限流→等15秒→重试。最多重试5次限流后，如果仍然失败则自行实现该任务（直接写代码），绝不跳过。等待期间不要做其他操作，专注等待后重启子代理继续未完成任务的开发。\n- 限流重试时，从上次中断的确切位置继续（如果任务已标记 in_progress 且有部分产出，子代理应识别已完成的部分并继续剩余工作）"
  }
}
