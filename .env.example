# ============================================================
# 审计作业平台 — Docker Compose 环境变量配置
# 复制此文件为 .env 并根据实际环境修改
# ============================================================

# ----------------------------------------------------------
# PostgreSQL 数据库配置
# ----------------------------------------------------------

# 数据库用户名（默认: postgres）
PG_USER=postgres

# 数据库密码（生产环境请使用强密码）
PG_PASSWORD=postgres

# 数据库名称（默认: audit_platform）
PG_DB=audit_platform

# 数据库对外映射端口（默认: 5432）
PG_PORT=5432

# ----------------------------------------------------------
# Redis 配置
# ----------------------------------------------------------

# Redis 连接地址（Docker 内部服务名，默认: redis://redis:6379/0）
REDIS_URL=redis://redis:6379/0

# Redis 对外映射端口（默认: 6379）
REDIS_PORT=6379

# ----------------------------------------------------------
# JWT 认证配置
# ----------------------------------------------------------

# JWT 签名密钥（必填，请使用随机字符串，如: openssl rand -hex 32）
JWT_SECRET_KEY=change-me-to-a-random-secret

# JWT 签名算法（默认: HS256）
JWT_ALGORITHM=HS256

# Access Token 过期时间，单位：分钟（默认: 120，即2小时）
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=120

# Refresh Token 过期时间，单位：天（默认: 7）
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ----------------------------------------------------------
# 后端服务配置
# ----------------------------------------------------------

# FastAPI 后端对外映射端口（默认: 8000）
API_PORT=8000

# CORS 允许的前端源地址，多个用逗号分隔（默认: http://localhost:5173）
CORS_ORIGINS=http://localhost:5173

# ----------------------------------------------------------
# ONLYOFFICE Document Server 配置
# ----------------------------------------------------------

# ONLYOFFICE 对外映射端口（默认: 8080）
OFFICE_PORT=8080

# ONLYOFFICE 内部服务地址（Docker 内部服务名，默认: http://onlyoffice:80）
ONLYOFFICE_URL=http://onlyoffice:80

# WOPI Host 基础地址（Docker 内部后端服务名，默认: http://backend:8000/wopi）
WOPI_BASE_URL=http://backend:8000/wopi

# ----------------------------------------------------------
# 文件存储配置
# ----------------------------------------------------------

# 文件存储根目录（默认: ./storage）
STORAGE_ROOT=./storage
