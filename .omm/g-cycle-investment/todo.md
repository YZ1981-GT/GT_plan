# 待办（G 循环）

## 需核实 / 需决策

- **🔴 科目码偏差与撞码**（见 `concern.md` §1）：G1/G4 都用 1501、G6/G8 都用 1503，且与标准科目表的
  新准则码（1101 / 1504 / 1505 / 1506 / 1507）不一致。需与科目映射 + 报表取数口径一起核，
  确认是"迁就客户旧准则账套"还是历史错误；修改会影响 TB 回写与报表，属跨模块变更
- **G3 应收股利附注章节号**未在本轮读取（`g3NoteSectionMap.ts` 只读到 sheet name），待核
- **G4/G5/G6/G7 是否有 `g{n}NoteSectionMap`** 未逐一核实（G7 按既有记录为 五、18 / 八、18）

## 已知未做（来自各科目复盘）

- **G2（应收利息）** 缺 wpId/projectId props → 抽凭引擎未接入
- **G7-18 凭证检查**为只读 htmlData 模型，需改可编辑模型后才能接抽凭引擎
- **G4** 利率合理性校验（有效利率 vs 票面利率差 >200bp 告警 + IRR 反推）、到期日预警与逾期检测
  （关联 ECL 阶段升级信号）、跨底稿勾稽面板（G4-1↔G4-2 合计 / G4-ECL 减值↔G4-1 减值小计）、
  截止测试改为跨期利息检查
- **G12** 净敞口套期的适用性自动裁剪
- **G 循环附注结构化推送**：多数科目已有 `buildG{n}SyncPayload` + columns 并登记覆盖率守卫，
  但生产环境 `sub_table_data` 实际为 0（推送链未被使用），属全平台"披露同步未跑起来"问题
