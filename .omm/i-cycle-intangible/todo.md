# 待办（I 循环）

## 需核实 / 需决策

- **I6 与 K9 共用 6602 的回写口径**：两者是否都做 TB 回写、如何避免互相覆盖，需明确单一回写方
- **I2 保存路径是否仍有缺 `/api` 前缀的残留**（历史 `PUT checklist-responses` 404 静默；曾在 I2 出现过）
- I1/I2 的 `useI{n}FormData` 与 `useI{n}Adjudication` 各自重复定义同名科目码常量 → 建议收敛为 `i{n}Constants.ts`

## 已知未做

- **I3 商誉减值的资产组认定阻断**：无资产组/未分摊时应阻断减值测试或强提示，当前可能产出无归属数值
- **I2 资本化五条件与 I6 费用化的分流合理性对比**（同一项目研发投入在 I2/I6 之间的分配是否合理）尚无自动勾稽面板
- **摊销年限税会差异 → N1/N3** 的自动带入（"购入摊销年限小于/大于税法规定的资产"是附注固定行，
  目前需人工在 N1/N3 录入）
- **I 循环附注结构化推送**：I1~I6 已有 `buildI{n}SyncPayload` + columns 并进覆盖率守卫，
  但生产环境 `sub_table_data` 为 0（全平台"披露同步未跑起来"问题）
- I4/I5 的 `shared/` 目录内容与 I3 `shared/` 的复用边界未逐一核实
