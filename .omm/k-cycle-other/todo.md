# 待办（K 循环）

## 需核实 / 需决策

- **三组同码的回写口径**（K1坏账/K2=1231、K5/L5=2701、K9/I6=6602）：确认单一回写方，避免互相覆盖
- **K1 其他应收款坏账进 K11（6701）还是 G14（6702）**：坏账属信用减值性质但历史进 K11，需按客户报表口径确认
- **K2 与 K1 的 1231 边界**：K1 用 1231 作坏账备抵、K2 用作科目主体，取数是否互扰

## 已知未做

- **K10-2 导入后 reload**（父级 reload plumbing）；K10-2 ⚙列设置 / 引导弹窗
- **K11-2 P&L 双列完整 E-O 核对**（需重写 useK11Detail formula 引擎，属 spec 级；当前 allowance rollforward 已含核心核对价值）
- **K12-2 月度宽表重建**（需 spec，跨前后端 >500 行）
- **K13-2 从序时账取数 + 月度列结构对照源模板核验**
- **K5/K6/K7 专项检查表的完整源模板对齐**（K5-4 三年历史保修率对照 / K5-5 完整性 / K5-6 已计提差异 /
  K6-4 19 列估值表 / K6-6 处置组减值分摊）部分已重建，其余待核
- **K 循环附注结构化推送**：多科目已有 `buildK{n}SyncPayload` + columns 并进覆盖率守卫，
  但生产环境 `sub_table_data` 为 0（全平台"披露同步未跑起来"问题）

## 已完成（本平台历史）

- K1~K13 审计目标认定 alert 全部补齐
- K 循环 persistence 四连环 bug 全修（数据首次真正 round-trip 落库）
- K8/K9/K10/K11 双模式「拉取成功」+ AI 接线 + 附注死链 + 目录 E1 标准重建 + 带入调整
- K1~K13 目录页按 E1 标准逐一重建 + 每科目编制/使用手册
