# 待办（H 循环）

## 需核实 / 需决策

- **🔴 H3 的 1503/1504 与 G6/G8 撞码**（标准科目表里 1503=可供出售金融资产、投资性房地产应为 1521）：
  三个底稿对同一 TB 科目取数回写会互相覆盖，需与科目映射 + 报表口径一起核，不擅自改
- **H2 无独立科目码常量文件**（1604/1605 散在 Tab 与手册里）→ 建议收敛为 `h2Constants.ts`
- **H9 附注章节号**（五、47 / 八、52 为既有记录）与 H8（五、25 / 八、26）需持续对齐权威 matrix

## 已立 spec 未实现

- **`h1-four-table-extraction`**（三件套齐）：H1-2 明细分类级从 tb_balance 叶子取数（Persist_First + 溯源面板）/
  明细增减 ↔ 序时账核对 / H1-12·13 折旧账面数取数 / `related_parties`+`bs_date` 注入（H1-18 漏项告警、H1-17 年检）/
  H1-1 试算核对走 `report_account_mapping`；灰度 `H1_FOUR_TABLE_EXTRACTION_ENABLED` 默认 False。
  **宁缺勿造边界**：卡片级明细（aux 无卡片维度）与折旧费用归属（counterpart 填充率 9%）不做自动取数

## 已知未做

- **H2 利息资本化借款数据全手工**：CAS17 引擎完整但本金/利率/账面利息手填，未从 L1/L4 pull；
  `资本化利息合计 vs 账面借款费用` 差异未提到全局告警、未与利润表利息支出勾稽
- **H1 税会差异 → 递延所得税（N1/N3）联动**：H1-6 已加税会差异重算与 DTA/DTL 结果表，
  但税基/税率仍为审计师手工输入（平台无税法折旧自动源）
- **H1 购建现金 → 现金流量表勾稽**只做提示（合计 + tooltip），无真实 CFS 对接
- **H6 导入导出未含 Part(一) measurement 数据**（后端端点耦合）
- **H 循环附注结构化推送**：多科目已有 `buildH{n}SyncPayload` + columns 并登记覆盖率守卫，
  但生产环境 `sub_table_data` 实际为 0（全平台"披露同步未跑起来"问题）
